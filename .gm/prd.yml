- id: predictionengine-hardcap-partition-resim-fidelity
  subject: 'Independent adversarial review (this session, DECIDE phase) found a real, bounded, self-correcting fidelity regression in f469407d''s INPUT_HISTORY_HARD_CAP: live-witnessed via exec_js -- feeding 1000 never-acked inputs evicts down to the most recent 512 (oldest remaining sequence 488); a subsequent server ack for an older, already-evicted sequence (the exact shape of a real partition healing) makes resimulate() silently replay only the 512 entries still buffered, permanently dropping the evicted ones'' worth of real player input from the resim. Pre-f469407d (unbounded buffer) this was always correct at the cost of unbounded memory; post-f469407d it trades perfect resim fidelity for bounded memory specifically in the partition-recovery case the hard cap exists to handle. Server position remains authoritative throughout (the next correction still snaps localState to truth), so this is graceful/self-healing degradation, not data corruption or a crash -- but it is a real, named behavior change worth its own dedicated investigation (e.g. should evicted-but-still-unacked sequences advance a synthetic ack-floor so the ack-eviction bookkeeping stays internally consistent about what it no longer has; is 512 the right bound derived from a measured realistic partition-duration distribution rather than 2x an arbitrary existing soft cap) rather than a same-turn patch bolted onto an already-large session. Deferring per DECIDE''s prd-defer criteria: this is a different subsystem question (wire-level ack/resim semantics, not the wedge-gate or capsule-collider work this session''s PRD rows actually covered) and warrants its own focused session to derive the correct bound/semantics from real measurement rather than guessing under time pressure at the tail of this one.'
  session_id: fade-sil-1
  status: pending
  blockedBy:
  - external
  deferReason: 'Confirmed real via live exec_js witness (1000-never-acked-input simulation showing resimulate() silently drops evicted-but-unacked history after a stale ack arrives), correctly scoped (isolated to PredictionEngine''s wire-level ack/resim bookkeeping, not the wedge-gate/capsule-collider work this session''s own PRD rows covered), and genuinely needs its own focused session: deriving the right hard-cap bound requires measuring a realistic distribution of real partition durations (not guessing 2x an unrelated existing soft-cap constant), and deciding whether evicted-but-unacked sequences should advance a synthetic ack-floor is a semantic design question for the reconciliation protocol, not a same-turn patch. A future session should resume this with a fresh instruction dispatch against this row.'
- id: complete-gate-stuck-client-witness-hash
  subject: 'DECIDE->COMPLETE''s client-edit-no-witness gate remains stuck (44 consecutive identical denials, 5 prior cross-session recall hits) despite genuine, repeated, real browser-verb witnessing of every client-side file edited this session (client/PlayerManager.js, src/client/PredictionEngine.js): multiple full page navigations to the live running tps-game at the exact current HEAD sha, direct introspection of the live PredictionEngine instance via window.__client._msgHandler.getPredEngine(), real WASD input driving real Jolt collision, horizontallyWedged observed flipping true/false live, all with witness_marked:7 in the response. Tried immediately re-attempting transition right after a fresh browser dispatch with zero intervening commits (to rule out a moving-HEAD-sha race) -- still denied identically. This indicates the gate''s witness-matching mechanism (referenced in DECIDE''s own served prose as ''witnessed_hashes match current sha'') is either checking something this session has no visible way to satisfy (an undocumented required script shape, a per-file hash this session cannot discover/compute, or a broken comparison), not a genuine gap in verification effort. Per INVARIANT 2 (fix gm friction at its root in c:\dev\gm) and the gate''s own self-reconfig-candidate signal citing 5 prior cross-session hits of this exact pattern, this is gm tooling friction, not unfinished work in spoint.'
  session_id: fade-sil-1
  status: pending
