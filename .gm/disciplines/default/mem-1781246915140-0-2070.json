spoint fun-max RE-RUN (gm-skill, 2026-06-12, branch dna-quality-fixes). User re-issued the fun-max prompt; authorized the staged follow-on rows. Ran /fun-max fresh (wf_334e3f05, 60 agents, 37/47 NET-NEW confirmed, plan .gm/witness/fun-plan-rerun.md). SHIPPED F1 powerup loop (fun-followon-content-loop): the buff system was wired-but-DEAD (nothing emitted powerup.collected). tps-game now owns 3 glowing spinning pickups (damage/speed/rapid) spawned via ctx.world.spawn with custom:{mesh:'box',color,emissive,light,spin,hover} - NO client edit needed, EntityLoader.buildEntityMesh already renders all those custom fields. update() does proximity-collect (radius 1.7) -> ctx.bus.emit('powerup.collected',{playerId,duration,speedMultiplier,fireRateMultiplier,damageMultiplier}) [reuses the wired handler] -> ctx.world.destroy + 15s respawn. Mirrors the env hazard proximity pattern. ALSO fixed two net-new the run caught that complete it: (1) fireRate buff never applied -> fire gate now `100/(tps.boost?.fireRate||1)` + buff_applied stores fireRate, so the rapid powerup feels fast; (2) HUD kills counter was DEAD because server ctx.state.playerStats is a Map that is NOT wire-synced to the client -> track tps.kills client-side, increment on the death-killer event. Browser-witnessed live :8090: browser-19 all 3 powerups render (colors+lights+spin:1.6), browser-21 tpsErrors:[]. 35 remaining net-new findings (damage numbers, enemy gunshots spatial, damage-taken flash, respawn juice, spawn-shield, fall-death instant-respawn, client-predict recoil) + A4 streaks + A7 kill-feed-names -> PRD row fun-rerun-juice-batch. STILL STAGED: fun-followon-netcode-knockback (regression-sensitive), fun-followon-anim-polish. CAVEAT this session: background server.js procs die when their wrapper task ends (EADDRINUSE ghosts); the >5min fun-max workflow ages the instruction gate so the browser verb gets long-gap-denied - dispatch instruction first. Browser verb body is raw playwright JS; a `found` var from the node scope is NOT visible inside page.evaluate (pass it as an arg).