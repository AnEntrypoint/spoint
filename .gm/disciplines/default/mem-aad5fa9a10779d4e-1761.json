spoint RenderGraph (2026-07-07): client/app.js's render section runs through createRenderGraph (client/core/RenderGraph.js) -- previously shipped-but-never-imported (0 references; classic mechanism-without-trigger). Node contract: {id, reads[], writes[], shouldRun, required, run(ctx)}; single-writer-per-resource construction throw; Kahn FIFO = registration-order ties; marker resources for order-only edges; run()-error isolation logs once naming the node and skips the rest of THAT frame only; reentry guard throws. Persistent _graphCtx across frames makes ctx.res carry last-frame values -- the deliberate one-frame near/far lag (host-near-far node computes THREE near/far from LAST frame's planet context and publishes window.__hostNearFar BEFORE renderPlanet, because mapspinner's depth-writeback re-encode reads it synchronously mid-call; apply-to-camera happens in a separate post-planet node, matching the old inline order exactly). scene-color is required:true (disable refused). Live surface: window.__renderGraph {order, stats() per-node ms/ema/call-deltas, capture() -> one serializable frame artifact, disable/enable(id) bisection, toMermaid(), watchdogLog}; watchdogs always-on log-once (NaN camera, near>=far, autoClear-left-false, written-never-read at construction). window.__culling (client/core/CullingHub.js) aggregates sceneOcclusion/terrainOcclusion/modelPool stats lazily. Known instrument artifact: renderer.info.calls delta for the node that calls renderer.render() spans three's own info reset (reads negative) -- use absolute rendererInfo from capture() instead. Witnessed live: 6-node order exact, zero watchdogs, p50 18.8ms @57fps, no regression. NEW RENDERING PASSES MUST BE NODES (constraints.md) -- never inline animate() steps.