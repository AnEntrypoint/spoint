## Resolved mutable: m-streaming-gltf-three-cull-paths-zero-collapse

Witnessed the firing-path question is NOT resolvable headlessly: browser-126 on :8090 shows instancedMeshCount=0 (env-sillos renders via ClusterLODMesh, NOT the instanced-slot pool, so the GPU instanceBoundSphere NaN-cull + sub-pixel zero-matrix paths do NOT apply to sillos on this machine); sillos has only 1/429 degenerate cluster sphere (negligible); and the local PLAYER mesh is 'not spawned' (headless cannot click-to-play/pointer-lock to enter the game, so I cannot inspect player bones/parts at origin). So neither the sillos GPU-cull nor the player-snap is reproducible/inspectable in headless Chromium. The streaming-gltf zero-collapse mechanism is real in source (model-pool.js zero-matrix sub-pixel cull line 1546 + GPU NaN cull 431-440 + releaseSlot 216) but I cannot witness WHICH path fires for the user. Blocked on the user's real observation to target the fix without guessing.