## Resolved mutable: m-stale-instance-divergence-mechanism

CONFIRMED live: on-disk node_modules/mapspinner/gl-render.js HAS the fix (__waterDepthShared:1, md5 20228e9...), but BOTH running servers (:3001 pid, :8090 pid) serve a STALE identical snapshot (md5 846b711c..., waterDepthShared:0) frozen at their pre-edit start time. So the divergence mechanism is PROCESS FRESHNESS vs disk: a long-lived server.js resolves/serves node_modules as it was at boot; a bridged edit lands on disk but not in an already-running process. The two ports are identically stale to each OTHER -- they only look 'wrong' relative to the current on-disk code. Cure = single fresh instance + never leave stale ones running. 8090 default is set by scripts/*.mjs + .claude/workflows/editor-*.js; npm start uses 3001 -- running both = two stale snapshots.