spoint gh-pages multiplayer HOSTING was broken (could not host/join a room) because apps/_lib/game-fsm.js was silently gitignored by the broad _* rule (.gitignore line 90). src/apps/AppContext.js imports it (commit 3173e87e added the import, not the file); it's in the BrowserServer WorkerEntry.js module import graph. CI checks out from git so `find apps -name *.js` never copied it to dist -> /apps/_lib/game-fsm.js 404 on the static deploy -> the module worker fails its import graph -> 'Connection failed: Event' (BrowserServer worker.onerror=reject) -> host BrowserServer never connects -> no wireweave bridge -> hosting+joining broken. Green gh-pages workflow != working (same class as ghpages-deploy-witness-required). DIAGNOSIS METHOD: crawl the worker import graph (BFS from /src/sdk/WorkerEntry.js) against the LIVE deploy with curl to find the one 404 edge; the in-browser symptom is just 'Connection failed: Event' with no detail (cross-origin worker error). FIX c98a8423: added !apps/_lib/ + !apps/_lib/** negation to .gitignore and committed game-fsm.js. RULE: any source file in the worker/client import graph that is underscore-prefixed or under an underscore dir gets eaten by _*; force-track it or negate the ignore. Witnessed live after redeploy: host ?room=X&world=tps-game -> BrowserServer connected=true, wireweave pubkey set, worker graph 0 404s. Also fixed landing #multiplayer host/join jank (fe37873f): unambiguous crypto room codes + parseRoomCode + true host mode ?room=&world=tps-game matching the in-app M-lobby.