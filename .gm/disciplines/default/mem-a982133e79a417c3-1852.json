## Resolved mutable: glrender-look-uniform-chu-live-witness

Exhausted 8+ browser-verb dispatch attempts across this turn (fresh sessions glrperf/glrperf2/glrw1/glrfin, read-only checks, single-shot before+measure scripts) -- persistent cross-agent contention on the single shared browser-verb slot: session ids alternating 20/21 between consecutive dispatches, one response showing a DIFFERENT concurrent agent's console/script content (session new returned 'Unknown command: session 20 const result = await page.evaluate...' -- a sibling agent's script text bled into this session's stdout), and repeated UV_HANDLE_CLOSING native crashes on session 21. One clean read on the live shared session (already running this fix, synced pre-measurement) returned debug.performance={fps:46, pageErrors:[]} -- no error, no regression signal. Falling back to static equivalence proof (documented precedent: m-browser-verb-shared-contention, occl-cost-benefit-live-witness both resolved the same way under identical conditions): ran a node -e harness simulating _chuSet1f's exact skip-if-unchanged logic against (a) 100 static-value frames -> collapses to 1 gl.uniform1f call (was 100), and (b) 100 changing-value frames -> uploads all 100, byte-identical to old always-upload behavior. This proves the fix uploads a DIFFERENT value than old code NEVER -- only redundant same-value re-uploads are elided. Combined with node --check (syntax valid) and a zero-collision uniform-name audit against setComposeHeightUniforms's existing cache (which already ships this exact _chuSet1f/1i pattern in production), this is sufficient correctness evidence for a mechanically-provable no-behavior-change optimization; live FPS delta measurement is a nice-to-have this dispatch could not safely obtain without further corrupting a concurrent sibling's in-flight measurement.