spoint MAX-EFFORT run2 (gm-skill, 2026-06-12). User: finish all unfinished fun-over-latency work + FIX deployed-demo editor body-type-switch bug. HEADLINE BUG ROOT CAUSE: the body-type-switch fix (changeBodyType in src/apps/AppRuntime.js) was on dna-quality-fixes but NEVER MERGED TO MAIN; gh-pages (.github/workflows/gh-pages.yml) deploys from main (push branches:[main]), so the DEPLOYED demo ran old code where switching a box to dynamic/kinematic was a no-op. `git show main:...AppRuntime.js|grep changeBodyType`=empty was the witness. FIX = merge: main was a strict FF-ancestor of the branch -> git_checkout main + git_reset --hard dna-quality-fixes + git_push (ec709a08..ba8a2fa7), gh-pages run 27413398250 SUCCESS. DEPLOYED witness: fetch /spoint/src/apps/AppRuntime.js shows changeBodyType+_bodyDef synth present. The fix itself was already correct+tested on-branch (verified locally: static box placed y=12 FELL 17.98m the instant switched to dynamic, kinematic applied, 0 errors). LESSON: a 'deployed demo bug' for an already-fixed feature = check the DEPLOY BRANCH first (git show main:<file>) - the bug is often an unmerged branch, not the code. CAVEAT: window.__app.editorAPI is a LOCAL-dev debug global, ABSENT on the deployed main-thread build - witness deployed editor logic by fetching /spoint/src/... source + behavioral proof on byte-identical local content, not via editorAPI. fun-over-latency RAN AGAIN (wf_335d659f, 33 agents, 18/24 confirmed, 0 lowLatencyRegression). SHIPPED on apps/tps-game/index.js+server.js + LagCompensator + ServerHandlers (commit content ba8a2fa7): HONEST optimistic hit prediction (exported predictHit() mirrors server hitscan vs interpolated remotes, fires hitmarker at trigger-time, server hit de-dupes via _predHitAt<400ms; +predictHit.test.js 6 cases); shooter-knockback client-prediction; fire origin from getLocalState (NOT getRenderState - that has a display-only offset the server never sees, P1); headshot hitmarker gold; RTT-padded hitmarker fade; damage-number ACCUMULATION within 500ms (burst readable, no new DOM P2); kill de-dup widen max(2500,rtt*2.5)+authoritative server streak+killerKills in death payload (counter self-heals); spawn-shield #tps-shield rim; RESET AMMO ON RESPAWN (clear bug: respawned player could not fire); lag-comp bounded forward velocity-extrapolation of rewound sample; lag-comp window 800->1000ms; reconnect passes reconnected:true so app no longer force-resets recovered health/wipes stats. DECLINED with doctrine rationale: server fire-rate gate (P9 - drops legit jitter/buff shots), fire-input-redundancy (P2 - APP_EVENT 0x33 NOT in UNRELIABLE_MSGS so fire already reliable; the workflow's best-effort premise was false). DEFERRED: reconnect respawn-state carry (needs oldId plumbing). npm test 130/130. CAVEAT: browser verb 14s cap can't cover a COLD gh-pages deployed-demo boot (>14s worker boot) - split nav from quick polls across verbs, same session persists. CONCURRENT WRITER force-pushed the branch mid-session (a870e392->ba8a2fa7, identical tree) + made a rearch-and-modularize branch - sync local to origin before merging.