## Resolved mutable: live-editor-jank-inventory

browser-5/6/7 live (?singleplayer&world=tps-game, P opens editor; synthetic keydown must target document not window). Screenshot .gm/witness/editor-open.png at 1036x647. ROOT JANK: the editor's child panels use the kit DASHBOARD card style (createEditorInspector + createSceneHierarchy + EditPanelEditor call container.classList.add('panel','panel-inline')) so .panel (app-shell.css:488) applies border-radius var(--r-3), margin-bottom var(--space-5), box-shadow var(--panel-shadow), and .panel-head padding var(--space-4) var(--space-5) -> the Scene + Inspector panels render as FLOATING ROUNDED CARDS with big margins/shadows/padding over the viewport, NOT docked flush square panels like a real engine editor. Specific defects: (1) left Scene panel = floating rounded card x=32 (32px gutter) w=260 with 22px corners + shadow; should dock flush to the screen edge. (2) Inspector column: panel-inline head floats w=314 h=32 at y=114 then an empty rounded card then a stray dashed app-prop separator (border-top:1px dashed --rule from EditorShell hierAppMount/appMount) over a large empty void; tab body only 184px tall does not fill the 533px column; no 'Select an entity' empty-state visible. (3) status bar 68px tall (kit Status default) with a floating 'Shortcuts' pill -> far too tall for an editor status strip (~28px expected). (4) scene rows show label duplicated as title AND sub ('environment environment','tps-game tps-game') -> redundant. (5) excessive whitespace everywhere from card padding -> not dense. FIX SURFACE: add a docked editor-panel variant in c:/dev/anentrypoint-design/editor-primitives.css (square, flush, no margin/shadow, dense head, full-height) + apply via editor child containers, the SplitPanel panes background, and a dense status bar; rebuild kit, bump version, consume via unpkg @latest. The kit editor-primitives already has .ds-ep-dock + bordered .ds-ep-* chrome that is the correct docked idiom the editor should adopt instead of .panel cards.