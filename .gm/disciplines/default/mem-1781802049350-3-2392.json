spoint collider-speed session (gm-skill 2026-06-18, branch dna-quality-fixes, commit dff26243 pushed; follows pool-preassign 77d30a57). User: 'simple pooled pill/cylinder/box trunks (trunks only), convex hulls for rocks, manage rocks WITH trees, pick most performant, batch colliders if faster, speed very important'. SHIPPED + witnessed: (1) TRUNK SHAPE = thin upright BOX (was capsule). MEASURED real-Jolt 600 static bodies: box add 5.37ms < capsule 9.03ms (cylinder add 2.66ms but warmup-confounded + costlier narrow-phase); box is the cheapest convex narrow-phase for a vertical bole. VegPhysics.addTrunk: physics.addBody('box',[r,halfH,r],[x,y+halfH,z],'static',{rotation,shapeKey:trunkShapeKey(species,bucket)}). Added BoxShape shapeKey CACHING in World.addBody box branch (was uncached) so pooled box bodies share one shape (parity w/ capsule/convex). (2) ROCK POOL PRE-FILL: RockPhysics.start() now preallocatePool('convex',hulls[type][b],'rock<type>_<b>',perKey) for every (type,bucket) - symmetric w/ trunks, both pools assigned before gameplay ('manage rocks WITH trees'). WITNESS: real-Jolt full trunk streamer createsAfterStart 400 (pre-fill off-tick), createsDuringMove 0 (re-center reuses pooled box bodies, zero runtime CreateBody); 161/161 tests; singleplayer onGround no err. (3) BATCH-ADD ASSESSED + DEFERRED (collider-batch-add row): Jolt AddBodiesPrepare/Finalize/Abort + BodyIDVector + Cylinder/Box/Capsule/StaticCompoundShapeSettings ALL present (typeof function), BUT BodyIDVector has NO .data() (proto: empty/size/at/push_back/reserve/resize/clear + mangled 'Qya'), passing vec direct = 'memory access out of bounds'; StaticCompoundShapeSettings.Create() returned IsValid()=false in a quick probe (AddShape arg form unresolved). Batch create+add measured ~2.18x faster BUT pooling already makes runtime add-churn ZERO, so batch only speeds one-time off-tick startup pre-fill - NOT worth a drift-prone mangled-pointer dep (P9). The REAL broadphase win if revisited = MutableCompoundShape per region (1 body for N trunks) - needs the embind AddShape(Vec3,Quat,Shape[,userData?]) signature fixed first. STILL PENDING: unify-rock-trunk-manager (merge the two independent setTimeout(_check,300) streamers into one cadence/center - marginal now that creates are pooled away). World.preallocatePool must push to free AFTER creating all N (else addBody pool-reuses mid-loop).