project/editor-hierarchies: entity parent-child was ALREADY server-complete (AppRuntime.js parent/children Set, reparent, getWorldTransform parent-relative, _encodeEntity includes parent, getSceneGraph/_buildNode nested {children}; client/core/SceneGraph.js setParent reparents three groups; AppContext ctx.editor.reparent). The editor just did not expose it. Added: MessageTypes REPARENT_ENTITY 0x94/DUPLICATE_ENTITY 0x95/SET_LABEL 0x96; EditorHandlers handlers (reparent/duplicateEntity/setLabel + persist + broadcast nested SCENE_GRAPH, routed via ServerHandlers.js:132 no allowlist); AppRuntime.reparent cycle guard (walks up from newParent, rejects if reaches entityId; rejects self/missing; returns bool) + duplicateEntity (copy config at offset same parent) + setLabel (e._config.label). SceneHierarchy.js rewritten flat->nested: flatten() recurses server children with depth, _expanded Set (default-expand once via _seen), subtreeMatches filter keeps matching ancestors, kit TreeItem depth/expanded/hasChildren/onToggle, rows tagged data-eid post-diff for behavior rebind, C.useDraggable+useDropTarget per row (kind scene-node) onDrop->onReparent, panel-body root-drop unparent, context Unparent (disabled when no parent via _parentOf map). onReparent/onRename/onDuplicate threaded EditorShell->createEditPanel->app.js client.send. Witnessed CDP singleplayer: reparent nests (root 2->1, child indented depth1 padLeft 18px), unparent (->2 roots), duplicate (2->3), setLabel (tree label), cycle rejected, bad-parent no-op. The Rename/Duplicate menu actions were dangling before this.