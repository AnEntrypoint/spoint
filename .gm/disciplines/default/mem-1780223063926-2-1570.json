project/editor-pointer-input: spoint editor viewport input is Pointer Events, not mouse-only. client/editor/editor.js gizmo drag + entity pick use ONE C.usePointerDrag(renderer.domElement,{onStart,onMove,onEnd}) from anentrypoint-design (kit primitive: setPointerCapture on primary pointer, ignores non-primary pointer mid-drag, finish() on both pointerup+pointercancel). Gizmo build fns (_axisHitProxy/_ringHitProxy) attach invisible visible:false enlarged hit-proxies (userData.isHitProxy+gizmoAxis, fat 0.34 axis/0.2 ring on coarse pointers, thin on fine) so touch can grab the 0.04-thick handles. _setEditMode sets renderer.domElement.style.touchAction='none' in editMode (restores prior on exit). onStart: gizmo intersect first (return true=drag, preventDefault); a miss calls pickEntity()+selectEntity (tap-to-select). editor API gains destroy() to tear the pointer-drag down. EditorApps.js New-App + SceneHierarchy Rename use kit C.Dialog/TextField (promptNewAppName validates empty/invalid /^[a-z0-9-]+$/ /duplicate with toast, null on cancel) -> ZERO window.prompt/confirm/alert in client/**. usePointerDrag added to anentrypoint-design src/components/interaction-primitives.js (export in components.js), published via CI v0.0.178, consumed via unpkg @latest. Witnessed browser session 12: synthetic touch PointerEvent drag on +Y gizmo tip -> EDITOR_UPDATE(0x80) {position:[0.041,0,0]}; proxyCount=3 axes xyz; canvas touch-action=none; New-App opens kit Dialog 'New app'; @320px hOverflow=false split column; overlay pointer-events=none over viewport (VR-safe).