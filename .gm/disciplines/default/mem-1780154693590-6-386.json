project/editor-inspector-stable-host: EditorInspector app-props host must use a STABLE id (not Math.random per render) so applyDiff reconciles it; repopulate fields only when shape changes (signature=entity.id+prop keys) with replaceChildren first. Random id + unconditional appendChild duplicated fields + stole drag focus (the inspector flicker). client/editor/EditorInspector.js:104.