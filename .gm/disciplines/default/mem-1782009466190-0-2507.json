spoint terrain/foliage visual-fix run (after the OOM saga). FIXES (all on origin/main): (1) SHADING - spoint rendered the planet with a high near-overhead sun [0.4,0.85,0.3] -> washed-out flat vs ../mapspinner planet.html; matched the reference's low oblique sun (sunLatBase 0.35 ~20deg, runtime [0,0.343,0.939]) in TerrainBackdrop.js renderPlanet for 3D self-shading relief (fe94f8ea). (2) TREES WRONGLY CULLED - Vegetation.js BVH_MARGIN 8->24m: the per-instance frustum-cull box was smaller than a scaled ez-tree canopy, so a trunk-origin box just outside frustum culled the on-screen canopy; larger margin is strictly more conservative (cf07cb40). The 'stale streamed BVH' theory was WRONG - @three.ez InstancedMesh2 autoUpdateBVH=true + addInstances(n,onCreationCb) inserts streamed instances into the BVH. (3) TREES BLINK DURING LOD SWAP - shared-impostor nearCutoff D3->D3*0.92 overlaps the cutover so the impostor plane engages a few m before the branch mesh empties at D3 (no 1-frame gap where neither draws) (5e33fe06). (4) HEIGHT NOT MATCHING veg/collisions - the CPU height (height-cpu.js, veg placement + physics) is a GLSL->JS TRANSPILE of terrain.glsl (THE single source of truth) via scripts/gen-height.mjs -> src/height-gen.js, run on CPU via glsl-rt.js. This is ALREADY 'common code, not written twice' (the user's ask). The bug CLASS is height-gen.js going STALE when terrain.glsl is edited without re-running gen-height (CPU diverges from GPU -> trees float). The DEV ../mapspinner had a stale committed height-gen (md5 45f7f83d->regen 8eaf765d, golden test 3/3 pass); spoint's installed 0.1.198 transpile was ALREADY current. HARDENED against the drift: mapspinner 'npm test' now runs gen-height + 'git diff --exit-code height-gen.js' (fails on stale) + prepublishOnly/prepack regen (cc64539); spoint gh-pages regenerates height-gen.js from the deployed terrain.glsl after the node_modules copy (f3c56faa). TOOLING (the browser verb's 14s cap cannot reach spoint's ~30s veg build / OOM): scripts/heap-capture.mjs + scripts/veg-witness.mjs are self-contained direct-Playwright witnesses (own chromium chromium-1208/chrome-win64, NODE_PATH=C:/Users/user/node_modules, long timeout, survive crash, console+screenshot+profile read) - the reliable witness path for this slow/heavy page. mapspinner parity probe note: lab.mjs parity (sampleGroundM) is a COARSE staleness-inflated live probe (~26m mean), NOT the oracle; the AUTHORITATIVE lock is height-cpu.test.js golden samples via npm test.