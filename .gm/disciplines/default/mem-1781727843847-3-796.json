spoint live terrain-tuning editor panel: client/core/TerrainPanel.js (exposed window.__terrainPanel), wired in app.js (_ensureTerrainPanel, created with the backdrop in _finishLoading, shown/hidden on clientMachine.isEditor). 8 live sliders write window.__<knob> (reliefScale, octMax=feature density, hiFreqCut, landBias, detailOverlay, canyonDepth, cliffAmt, beachShelf) - the SDK reads them per-frame so the planet retunes LIVE with no restart; a radius number-input re-creates the backdrop (onRadius disposes+re-creates createTerrainBackdrop). Replaces the edit-world-def-then-restart loop for dialing terrain scale/feature-richness. CAVEAT: live changes retune the GPU LOOK only; server physics (Jolt heightfield from the cached sampler reliefScale) follows on a commit/save+reload, not live.