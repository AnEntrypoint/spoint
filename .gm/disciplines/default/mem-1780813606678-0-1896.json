spoint cold-boot re-hunt (2026-06-07, .claude/workflows/startup-rehunt.js, 26 agents, 6 boot-path surfaces swept + adversarial net-new/safe/real verify): the prior Batches A-D + WorkerEntry bundle already captured essentially the whole cold-boot envelope - the re-hunt surfaced exactly ONE net-new overlap-with-worker-boot win. SHIPPED (commit 7977bf6a): preloadAnimationLibrary(gltfLoader) was called inside initAssets (client/app.js:119, fires only after the world-module import) so the ~500ms /anim-lib.glb fetch serialized behind the import; now ALSO kicked at client/app.js:38 right after gltfLoader creation (createLoaders), overlapping the world import + the >10s cold worker boot. AnimationLibrary.js:60 guards on _gltfPromise so the later initAssets call is an idempotent reuse of the in-flight fetch. Browser-witnessed local (session 18): connected:true playerId:1 meshCount:98 no regression, anim-lib.glb resource start at/before the tps-game world-module start. ALSO shipped same commit: gh-pages.yml bundle step now exits 1 if bundled WorkerEntry.js <50KB (the bundle-after-patch no-op was 2599 bytes and shipped green once; deployed truth 89608 bytes) - a permanent guard for the worker-bundle-before-pathpatch ordering. Deployed baseline re-measured clean: WorkerEntry.js 89608 application/javascript, jolt-physics.wasm.wasm 200 application/wasm (695844, compileStreaming, no base64-wasm-compat fallback), aim_sillos .prog model.progressive.glb transfer:0 prefetch cache-HIT, raw aim_sillos.glb encoded:0 (not downloaded, .prog path engaged). LESSON: when a hunt of an already-optimized path returns ~1 finding, that is the signal the envelope is near-exhausted, not that the hunt was weak - the adversarial net-new/safe/real triad correctly rejected the rest as already-shipped or regression-forbidden (prefetchModels raw-GLB, unconditional loading fallback, client trimesh-skip).