gm-method (wrong active-count accessor on LOD'd InstancedMesh2 produces false zero): o.instancesCount is the WRONG active-count accessor for a LOD'd InstancedMesh2 and returns false 0. Distrust a code-probe that says a VISIBLE thing is absent -- screenshot-witness first to confirm it is genuinely absent before trusting the count. When a count reads 0 but the scene shows the object, check the accessor name against the library's actual API, not documentation or assumptions.