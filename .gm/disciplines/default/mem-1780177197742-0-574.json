project/ghpages-deploy-witness-required: spoint gh-pages workflow (.github/workflows/gh-pages.yml) green != deployed site works — it never tests the page. Two runtime breaks shipped green: (1) new importmap dep streaming-gltf missing from the hardcoded pkg copy list (line 69) -> 404; (2) AppModuleSystem.js used root-absolute /apps/ base -> server.js dep 404 under /spoint/. ALWAYS browser-witness the live anentrypoint.github.io/spoint/demo.html after a deploy, not just the workflow status. Any new /node_modules importmap entry must be added to the gh-pages copy list.