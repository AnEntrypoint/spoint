## Resolved mutable: m-multidraw-overrun-transient-timing

CONFIRMED browser-73 failing load R0: 8196 multiDraw overruns caught by wrapping multiDrawElementsWEBGL/ANGLE and comparing start+count*bpe vs the bound ELEMENT_ARRAY_BUFFER BUFFER_SIZE. Samples: maxEnd 396 > bufBytes 216, maxEnd 2148 > 360, maxEnd 7014 > 1710 -- draw ranges reference index bytes far past the actual GPU buffer size. = ClusterLODMesh issues coarse-LOD (stream===1) draw ranges before the combined [LOD0|coarse] index BufferAttribute is GPU-uploaded (three uploads on the render after setIndex). Transient (1 frame) but the GL_INVALID_OPERATION storm poisons the context so concurrent mapspinner terrain GPU init fails. Fix = guard _draw to skip/clamp any range exceeding the bound element buffer's live BUFFER_SIZE.