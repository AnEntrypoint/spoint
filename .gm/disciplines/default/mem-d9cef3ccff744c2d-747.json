gm-method (cold boot dominated by worker connect, wins from overlap not gate-tuning): cold boot dominated by worker connect >10s = module-graph fetch + WASM cold start, UPSTREAM of all curtain gates. The win is 'overlap X with the worker boot', not tuning the gates. Key wins: HEAD-not-GET for readiness probe, WASM physicsReady-promise + top-level warm + streaming wasm.js, parallel app loads, drop worker cache-buster, workerCount:2, warmup off curtain, env .prog prefetch into HTTP cache during boot (biggest win), VRM parallel kick, curtain seeded only for env entities. ORANGE CUBE = EntityLoader 0xff8800 box for a model-empty env entity on the cold worker, fixed by skip+defer. Local warm-cache cannot show cold-boot wins; witness deployed.