spoint 2026-06-07 maximum-effort re-architecture run (gm-skill): orient (recall+codesearch) CONFIRMED the perf envelope is near-exhausted — AdaptiveAA O(1) ring, fused app.js:198 snapshot pass, cached updateSunShadow proj, documented ModelPool workerCount:2 tradeoff are all already hand-optimized. The ONE witnessable win was a MAINTAINABILITY refactor (not perf): client/ModelPoolAdapter.js spawn() + spawnVRM() were near-identical (applyTransform closure defined twice verbatim, same scene-attach + _entities.set + handle.on('ready') body); only diffs = pool.spawn args and ready 2nd arg. APPLIED: hoisted applyTransform to module scope (takes transform obj), extracted shared _attach(entityId,handle,transform,onReady,readyArg) helper; spawn uses readyArg=(e)=>e, spawnVRM uses (_e,h)=>h.actualEntity. -46/+38 lines, behavior-preserving, node --check OK, apps/_lib unit tests 29/29 ok. CAVEAT DISCOVERED (CRITICAL for this mounted Windows folder from the Linux sandbox): (1) the Edit/Write tools left 288 trailing NUL bytes on the saved file -> node 'Invalid or unexpected token'; strip with python b.rstrip(b'\x00'). (2) The whole tree showed 173 files modified = pure CRLF churn (git diff --ignore-all-space = empty); only 2 real-content files (ModelPoolAdapter + .gm/prd.yml). User chose repo-wide CRLF normalization; added .gitattributes (* text=auto eol=lf + binary globs for glb/vrm/png/wasm/etc). (3) git add --renormalize CORRUPTED .git/index to all-NUL (bad signature 0x00000000) and the sandbox CANNOT rm/rewrite .git/index or .git/objects ('Operation not permitted') — the mounted folder denies git's atomic index ops from the sandbox. RESULT: code change is correct ON DISK but uncommitted; .git/index must be cleared on the Windows host (del .git\index then git reset) before any commit. LESSON: do git operations on the host, not the sandbox, for this repo.