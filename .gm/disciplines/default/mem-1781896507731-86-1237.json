## Resolved mutable: joiner-dc-closes-real-host

ROOT CAUSE in vendored wireweave client/vendor/wireweave/src/data.js. Live browser-35/36 on real host 8JHF3: pc.iceConnectionState='connected', pc.connectionState='connected', signalingState='stable', peer.state='connected', fsm='connected', failCount=0, participants=2 — the RTCPeerConnection is healthy — BUT peer.dc.readyState='closed' with dc.bufferedAmount=177 (queued bytes stranded on the dead channel) and bridge.data.send(host,buf) returns false. data.js _wireDataChannel (line 202) wires dc.onclose -> emits 'peer-close' but onconnectionstatechange (line 173) only recovers on pc.connectionState disconnected/failed/closed. When the SCTP datachannel closes by itself while ICE stays 'connected', NOTHING recreates the dc -> the peer is permanently stuck connected-with-no-channel; send() (line 87) returns false because dc.readyState!=='open'. This is a wireweave datachannel-lifecycle bug, not spoint app code. FIX: in dc.onclose, if pc.connectionState is connected/connecting (not an intentional close), recover — offerer recreates the datachannel + renegotiates (createDataChannel + new offer), answerer triggers the same via ICE restart so a fresh ondatachannel fires.