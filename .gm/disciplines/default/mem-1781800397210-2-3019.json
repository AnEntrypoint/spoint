spoint session findings (gm-skill 2026-06-18, branch dna-quality-fixes). (1) SHIPPED commit 77d30a57 - PHYSICS POOL PRE-ASSIGNMENT (user 'physics stops when shuffling models, all pools assigned before we start'): trunk colliders created/destroyed an exact per-instance CapsuleShape body per stream (Jolt CreateBody+broadphase = the hitch). Fix: quantize trunk scale to 5 buckets -> finite shapeKey 'tk<species>_<bucket>' (SPECIES x BUCKETS) so trunks pool like rocks; new World.preallocatePool(shapeType,params,shapeKey,count) creates+parks bodies into _bodyPool BEFORE gameplay (pre-fill in createTrunkColliderStreamer.start, perKey=max(8,ceil(cap/(SPECIES*BUCKETS)))); collider capsule uses bucket-centre scale (<=0.2m, invisible; position unchanged = client-visual parity held). World._createCount added as witness. REAL-JOLT WITNESS: after preallocatePool(16), 16 stream-ins + remove/re-add = createsDuringAdd 0, createsDuringReuse 0 (zero runtime CreateBody). World._bodyPool/_repositionBody/removeBody(park) machinery already existed; the gap was trunks had no shapeKey + no pre-fill. NOTE preallocatePool must push to free AFTER creating all (else addBody pool-reuses mid-loop). (2) DESIGN for the GPU-TREE-RENDER PIVOT (user 'dont place/draw trees in three.js, use streaming-gltf GPU placement/LOD/FOV/impostor, combine systems'): C:/dev/streaming-gltf/examples/local-progressive/ has the GPU tier system. KEY: octahedral-impostor-ez-tier.js OctahedralImpostorEzTier is a POOL-INTEGRATED far-LOD tier - interface (bakeChunk(asset,object3D,cellBudget) incremental octa atlas bake / hasAsset / acquire(entity,layer,cx,cy,cz,wr)->handle / setCenter / release / instanceIdFor / .mesh Group / _nextLayer / dispose); the model-pool.js routes its FARTHEST LOD here when opts.useImpostorEz set (per-asset InstancedMesh of unit planes, lit albedo+normalDepth MRT 1024 atlas, full-octa works any angle). Other modules: model-pool.js (the pool spoint already wraps via client/ModelPoolAdapter.js: spawn/spawnVRM/setTarget GPU-lerp far tier), batched-far-tier.js, frustum-cache.js (FOV cull), lod-unload-manager.js, multi-draw-optimizer.js/draw-call-batching.js, deferred-load-queue.js, buffer-pool.js. INTEGRATION PLAN: register each tree species (ez-tree Object3D) as a pool ASSET, feed VegPlacement.js per-instance transforms as pool ENTITIES (pool.spawn + setCenter), let the pool's tiering do GPU instancing+LOD+FOV+impostor - replacing the @three.ez InstancedMesh2 client/core/Vegetation.js path while keeping VegPlacement as the deterministic source (client visual==server collider). spoint's streaming-gltf is consumed as a pkg; these example/local-progressive modules likely need vendoring into spoint (verify node_modules vs client/vendor). PRD rows: veg-render-via-modelpool-gpu, veg-modelpool-design-read, physics-pool-preassign(done). Impostors were de-risked earlier (mem veg-impostor-derisk): they render spread+correctly, 'invisible' = spawn faces away from forest bulk + monoculture(fixed commit 9f2a89da).