spoint perf-device RESIDUAL fan-out (gm-skill 2026-06-19, branch dna-quality-fixes, commit 760d4f1b pushed in-sync). 6 subagents disjoint files. SHIPPED: octahedral-impostor-ez.js texture2D->texture in blendNormals (GLSL3 real latent bug) + D1 single-normal EZ_FAR_SINGLE_SPRITE; TickHandler.js emitStateCorrections input-silence-driven reliable STATE_CORRECTION (client streams input every frame so silence==loss; rate-limit 1/8 ticks; full players list + delta:1 entities:[]; default-safe); JitterBuffer.js burst clamp gated inter-arrival>1.5x EWMA (clean-net byte-identical); EntityLoader.js refcount-aware LRU on _parsedGltfCache cap24 (reframed: parsed Scenes RAM-only not IDB); app.js adaptive fog gated >8ms sustained; mapspinner@0.1.198 EXT_color_buffer_float fail-explicit + TerrainBackdrop try/catch flat-terrain degrade. REVERTED DPR auto-enable (latched __dprAuto on startup spikes at avg 1.68ms). SKIPPED C1 deferred world-import (BrowserServer reads worldDef sync in connect, out of scope). OPEN blockedBy-external: pd-shader-validate-hunt (intermittent pre-existing non-fatal VALIDATE_STATUS=false on a DIFFERENT empty-name ShaderMaterial, reproduced 1/3 runs, needs reliable repro), pd-p7-dpr-warmup (needs real slow-device profile). git_finalize occasionally echoes stale steps[].sha; verify via git_log.