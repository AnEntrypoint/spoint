## Resolved mutable: m-fix-served-but-flicker-persists

My disable-cluster-cull fix is CONFIRMED working live on :8090: browser-120 shows _spointNoClusterCull set on 96/96 cluster meshes (frustumCulled:false); browser-123 under REAL animate-loop rotation (282 frames, 90 yaw steps via a.camera.rotation) drawn clusters = 429 CONSTANT (min=max=429, distinctCount=1). So the per-cluster cull cannot cause appear/disappear anymore -- the geometry draws fully every frame. The 21 'culled' nodes are empty Object3D groups (no geometry, harmless). Both :3001 and :8090 serve IDENTICAL ModelPoolAdapter.js (curl --compressed both have _disableClusterCull x2). CONCLUSION: the cluster-cull cause is FIXED. If the user still sees flicker it is either (a) their browser cached the pre-fix JS (the .js IS served no-cache/no-store but a hard-refresh may be needed), or (b) a DIFFERENT visual layer (z-fighting on thin slabs, or a tier/LOD swap) -- to be checked next, but NOT the cluster frustum cull.