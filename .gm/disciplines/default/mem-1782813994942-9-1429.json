gm-method (a subagent confidently flags a 'confirmed redundant double-eval of a pure shader function, ~18% of the VS cost' as a real win): it is very likely PHANTOM because the shader COMPILER already common-subexpression-eliminates two identical pure calls with identical args. PROVE it by live A/B, don't assume either way: hoist the call to one local in the INSTALLED shader, trigger the renderer's in-place recompile hook (mapspinner planet.render.recompile re-fetches the .glsl and relinks live -- works in the consumer, returns 'ok'), and re-measure the GPU timer. Witnessed: baseline GPU p50 4.096ms -> hoisted 4.150ms -> reverted 4.109ms = within noise = FXC (ANGLE d3d11) auto-CSEs it = no win. ALWAYS REVERT the probe edit (an installed-node_modules shader edit is a divergence, not a keep) and witness the revert (recompile + assert the patch marker is gone from the served text + GPU back to baseline). A source-level hoist still has hygiene value (explicit single-eval, helps a translator that doesn't CSE) but publishing a new SDK version for zero measured frame movement is the guarded 'churn for marginal gain' failure the destructive license does NOT authorize. Tightening an already-bounded loop, deduping a CSE'd call, pre-baking off-critical-path work = the phantom class; per-frame work that needn't run at all (an unconditional gl.getError sync, an O(n) opaque sort, a redundant state set) = the real class.