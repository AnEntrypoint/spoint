det-bounded-quant (the user's 'bounded quantization for exact match + speed/memory') OUTCOME (continues [[project/veg-elev-parity-FIXED-integer-hash-2026-06-21]]): (1) the GPU PERF half ALREADY SHIPS - mapspinner has a THC (Tile Heightmap Cache): gl-render.js:336+ bakes composeHeight per-tile into an R32F pool (THC_BAKE_RES 130, 512-layer LRU), terrain.glsl:757-784 thcSample(uv,layer) makes the VS read the baked height (5 bilinear fetches + Sobel normal) instead of per-vertex composeHeight when uThc>0.5 = the shipped FPS win (recall m-bake-perf-acceptable: bakes on camera-move>10m, no hitch). (2) EXACT 0m CPU<->GPU match is NO-GO, architecturally: it needs the CPU collider to read the IDENTICAL bake the GPU renders, but spoint's authority is HEADLESS - multiplayer Node server has no GL, and the singleplayer path runs in a Web Worker that is GL-FREE BY DESIGN (the three-free mapspinner/height-cpu import). No GL on the authority -> no shared GL-bake. The THC spatial quantization does NOT shrink the residual (the ~1.3m is in composeHeight's floor(P) fp32/fp64 cell-boundary flips, upstream of any sampling). Reaching 0m would need (a) headless-GL on the authority (heavy native dep, impossible in the GL-free worker) or (b) client-authoritative terrain collision (security-model change) - both large product-architecture decisions, not code. CONCLUSION: the deterministic integer-hash 1.3m (down from 4.3m, no floaters) is the practical parity floor; the GPU THC perf already exists. mapspinner pushed (origin c9509ee, v0.1.200). Terrain look-witness PASS (lab.mjs shot: natural rolling hills, no lattice artifacts).