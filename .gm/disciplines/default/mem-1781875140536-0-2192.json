spoint perf-device RESIDUAL fan-out (gm-skill, 2026-06-19, branch dna-quality-fixes, commit 760d4f1b pushed in-sync). 6 subagents on disjoint files cleared the perf-device residuals; central witness + commit. SHIPPED: (1) octahedral-impostor-ez.js texture2D->texture in blendNormals (GLSL3 removed texture2D - real latent bug) + D1 single-normal fetch in EZ_FAR_SINGLE_SPRITE. (2) src/sdk/TickHandler.js emitStateCorrections: server STATE_CORRECTION emit, INPUT-SILENCE-driven (client streams input every frame so silence==loss), reliable, rate-limited 1/8 ticks, sends full players list + delta:1 entities:[] to avoid SnapshotProcessor wipe; default-safe (converged client never triggers). (3) src/client/JitterBuffer.js burstiness clamp gated on inter-arrival>1.5x EWMA (clean-net byte-identical, 28 tests). (4) client/EntityLoader.js refcount-aware LRU cap on _parsedGltfCache (PARSED_CACHE_MAX=24, never evicts refCount>0/inflight) - REFRAMED: parsed three.js Scenes are RAM-only, cant IDB-serialize; ModelCache already bounds GLB bytes. (5) client/app.js adaptive fog gated >8ms sustained (fast device never triggers, witnessed far stays at config ceil). (6) mapspinner@0.1.198 published from sibling ../mapspinner: EXT_color_buffer_float fail-explicit before RG16F alloc; TerrainBackdrop try/catch degrades to flat terrain. REVERTED: DPR auto-enable detector - it LATCHED __dprAuto=true on heavy STARTUP frames even at avg 1.68ms on a fast box (witnessed); needs warmup-grace+real-slow-device profile. SKIPPED: C1 deferred world-import (needs BrowserServer.js edit, worldDef read synchronously in connect, out of scope). OPEN blockedBy-external residuals: pd-shader-validate-hunt (intermittent pre-existing non-fatal VALIDATE_STATUS=false on a DIFFERENT empty-name ShaderMaterial - reproduced 1/3 runs, needs reliable repro), pd-p7-dpr-warmup. WITNESS: full three.js shader error string is intermittent across browser runs; m.text() catches it ~1/3 runs. mapspinner publish: push 0.1.197 -> CI patch-bumps -> published 0.1.198 (npm view confirms), then npm install mapspinner@0.1.198. git_finalize works but echoes a STALE sha in steps[].sha occasionally - verify real HEAD via git_log.