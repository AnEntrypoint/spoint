spoint SP-JAM-ON-MOVE TRUE ROOT (gm-skill 2026-06-18, branch dna-quality-fixes, commit a8eb079a pushed). User reported 'single player server STILL jams up when moving around ever since adding vegetation' AFTER the heightfield-chunking (f21dfbd4) + collider pooling (77d30a57/dff26243) fixes. CORRECTION to the earlier belief that pooling fixed the move-jam: pooling fixed the BODY CREATE churn (createsDuringMove:0) but the REAL jam was the PLACEMENT RECOMPUTE. createTrunkColliderStreamer + createRockColliderStreamer recomputed placementsForChunk over the chunk RING TWICE per rebuild (desiredAt + keepSet); classify() calls groundHeightLocal 5x/candidate @~0.4ms. MEASURED (real mapspinner sampler): one trunk rebuild = 3133ms over 49 chunks x2 passes, fired every ~rebuildAt*radius (~19m of movement) on the worker tick thread = the jam. FIX (a8eb079a): per-streamer Map memoizes placementsForChunk/placementsForRockChunk by (cx,cz) - pure deterministic fn of (cx,cz,frame,anchorField,seed), so compute each chunk ONCE. WITNESS: cached re-center 1ms (was 3133ms), partly-new ring 312ms (only new chunks). 161/161 tests parity-preserved; singleplayer onGround+2662 instances+impostors5, no err. RESIDUAL (row collider-placement-compute-chunked): first-entry-to-BRAND-NEW territory still computes new-ring chunks synchronously (~312ms) - chunk that compute with a time budget OR pre-warm spawn-area cache behind the curtain. LESSON: when a 'still slow after the fix' report comes in, RE-PROFILE the actual hot path (the 0.4ms/sample groundHeightLocal dominates everything veg-placement-related) - don't assume the prior fix covered it. STILL OPEN from this prompt: veg-all-eztree-presets (use all ez-tree presets except Trellis - expand SPECIES table, append-only for wire-id stability), reveal-init-shot-each-cluster (aim camera at each content cluster for its first-draw compile before uncurtain), terrain-one-two-drawcalls (collapse mapspinner planet to 1-2 draws via FBO+fullscreen-quad on static frames), veg-impostors-not-visible-bug (de-risked: spread+baked correctly, it's spawn-faces-away-from-forest + monoculture, NOT a shader bug).