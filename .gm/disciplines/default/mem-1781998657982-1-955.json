OOM ROOT CAUSE (singleplayer terrain demo 'physics frozen, RAM->4GB OOM'), found via DevTools CPU profile not guesses (textures/snapshot-backlog/veg-shader were all WRONG secondary fixes). Profile: Octree.js = 47893/119586 samples (~40% CPU); jsHeapSizeUsed hit 3894MB while DOM nodes flat at 527 -> the WORKER's src/spatial/Octree.js SpatialIndex (d3-octree). d3-octree degenerates on NaN/Inf coords AND on two DISTINCT points closer than float epsilon (recurses to separate them forever -> ~4GB nodes + pinned core; pinned core starves the tick = 'physics frozen'; worker runs in page process so 4GB = page OOM). FIX (commit 8cc6e33c) SpatialIndex.insert: drop non-finite coords + QUANTIZE coords to 1cm (Math.round(x*100)/100) so near-coincident points collapse to equal -> d3-octree chains them (no infinite subdivide). LESSON: JS heap->OOM with FLAT DOM nodes/listeners = pure JS alloc; pull the trace cpuProfile self-hit ranking to name the hot lib.