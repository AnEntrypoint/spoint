spoint x mapspinner PLANET integration (gm-skill, 2026-06-17, branch dna-quality-fixes). SUPERSEDES the earlier same-day tree-demo integration: the LOCAL c:/dev/mapspinner checkout was a STALE v1.1.1 tree-demo; the REAL latest mapspinner (origin AnEntrypoint/mapspinner, npm) is a WebGL2 EARTH-SCALE PLANET SDK (createPlanet/initMapspinnerPlanet, cube-sphere quadtree LOD, GPU fractal terrain in src/shaders/terrain.glsl, pure-JS anchor-field HPF). User pivoted: 'plug in our planetary terrain renderer, coords right, sillos on it, sample it, precise physics, edit either project'. Decisions (AskUserQuestion): pivot to latest planet SDK; physics height = SINGLE-SOURCE generates both (GLSL stays the source the author tunes; a GLSL->JS transpiler generates the CPU height) - LEAST MAINTENANCE, zero drift. headless-gl rejected (WebGL1, mapspinner is WebGL2). 

MAPSPINNER (committed+pushed 5981a9e -> auto-publish): scripts/gen-height.mjs = a GLSL->JS TRANSPILER (tokenizer + recursive-descent parser + type-inferring codegen) for the composeHeight SUBSET of terrain.glsl - extracts height fns by name, emits src/height-gen.js calling a GLSL runtime shim src/glsl-rt.js (vecN as arrays + add/sub/mul/div for operator overloading, mix/fract/smoothstep/clamp/dot/normalize/tanh, mat3+mat3mul, swizzle sw(), out-params->multi-return arrays, HCache struct->object, uint via >>>0/Math.imul). KEY simplifiers: vtxDisplace()=0 (micro-relief off -> no uint hash live), hpfSample REPLACED by the pure-JS anchor-field (createAnchorField.sampleUV(face,fu,fv,BAKE_MAX_LEVEL=log2(hpfTexRes=128)=7)) so NO texture-array. src/height-cpu.js createHeightSampler({radius,hpfTexRes,seed,uniforms})->heightAt(dir)/surfacePoint(dir); THREE/GL-FREE (Node+worker). HEIGHT_UNIFORM_DEFAULTS MIRROR gl-render EXACTLY (uHiFreqCut:0.25, canyonDepthMul:2.0, uDetailOverlay:6, uBeachShelfM:0->600, uLandBias:0, cliffAmt:1, octaves 12/4/8/3); HPF BAKED to RES^2 + quintic-bilinear inset (matches the GPU bake; continuous diverged up to ~640m). index.js + ./height-cpu ./anchor-field ./glsl-rt ./planet-orchestrator subpaths; FIXED initMapspinnerRender re-export (lives in gl-render). gl-render.js: shaders+textures fetched relative to import.meta.url (EMBEDDABLE) - was page-relative './src/shaders' which 404s from a host's node_modules. Tests src/height-cpu.test.js; npm test regenerates then node:test. 

SPOINT: src/terrain/PlanetFrame.js = world<->planet coord frame (single source render+physics+placement). spoint plays a LOCAL TANGENT PATCH at worldDef.terrain.anchorDir ([-0.641,0.2558,0.7237]=land h~203m); <=510m patch sphere drop <1cm so spoint KEEPS its flat frame (local up=+Y => sillos NO reorientation). groundHeightLocal(x,z)=heightAt(localToDir(x,z))-anchorHeight+offsetY puts anchor surface at local y=0. src/terrain/TerrainPhysics.js rewritten: loadPlanetSampler (dual-import 'mapspinner/height-cpu' Node / '/node_modules/mapspinner/src/height-cpu.js' worker) + setupTerrainStreaming builds frame + samples groundHeightLocal into a Jolt heightfield FOLLOWING the player-centroid (re-center+atomic-swap). Wired WorkerEntry + ServerAPI. client/core/TerrainBackdrop.js renders the planet in spoint's WebGL2 (import('mapspinner/planet-orchestrator') initMapspinnerPlanet(renderer.getContext()); renderPlanet maps spoint local cam->ECEF eye/target/up via PlanetFrame, planet.frame(...) inside renderer.resetState()); app.js composites planet-first then autoClear=false+clearDepth()+scene, scene.background=null; deferred in _finishLoading. importmap mapspinner->src/index.js + /planet-orchestrator + /height-cpu; dep ^0.1.164. world tps-game terrain{anchorDir,radius:6360000,offsetY:0,physics:{extent:256,resolution:8}}. 

WITNESSED (:8090 SP cache-disabled): planet RENDERS (readPixels varied terrain colors not sky-blue, glErr=0, hasBackdrop, anchorH=203.2), player onGround on the heightfield, worker built N=32, zero errors/404. npm test 134/134; mapspinner 3/3. 

CAVEATS: (1) cp -r src node_modules/mapspinner/src INTO an existing dir NESTS src/src -> the gl-render fix never landed -> page-relative shader fetch 404 -> shader compiled the 404 body ('not' syntax error). rm-then-cp. (2) git add -A sweeps .gm/.plugkit-browser-profile -> gitignore it (mapspinner does now). (3) ES module cache: persistent browser profile caches old gl-render; witness via CDP Network.clearBrowserCache+setCacheDisabled. (4) planet heightAt ~0.38ms/sample -> N=32 ~400ms one-time boot build; streaming per-tick chunking perf-tunable. (5) GPU sampleGroundM probe unsuited to batch arbitrary-dir parity (stale-fence+camera-dir contamination); integration witness (player onGround on rendered surface) is the real parity proof. gl-render frame(camWorldPos,camTarget,fovy,displayMode,sun,time,up,surfElev) is the clean render entry.