NEGATIVE RESULT (continues [[project/veg-elev-cpu-gpu-parity-rootcause-2026-06-21]]): the user chose 'fp32 emulation in glsl-rt' to close the spoint veg/collider-vs-rendered-land ~4m gap. ATTEMPTED TWICE, both env-gated FP32_NOISE in mapspinner glsl-rt shash3/snoise3, measured via lab.mjs parity-patch at the spoint anchor patch r63600: (1) naive per-op Math.fround -> WORSE (full 4.287->5.184m). (2) fma-aware fp32 (fma32(a,b,c)=fround(a*b+c), exact since fp32*fp32 fits fp64; used on GLSL dot()/mix()) -> full mean 4.287->4.193 (max 12.37->9.81, marginal) but broad-shape-only (--skip) REGRESSED 1.93->2.46m. CONCLUSION: CPU emulation does NOT converge to the GPU. Root reason it CAN'T: the lab oracle is SwiftShader (its own fma/codegen/rounding), and real spoint clients run VARIED GPUs each diverging differently from the single fp64 CPU collider -> there is no single target to match; perturbing the CPU toward one GPU moves it away from others and from fp64-correct. Both experiments REVERTED (git checkout glsl-rt; gen:check + golden 3/3 green). The ONLY deterministic fix is the lever the user declined: lower uDetailOverlay (terrain-defaults detailOverlay 50, demo-baked, was SDK 6) on BOTH the GPU render and the CPU sampler -> the divergence scales linearly with it, so 50->~8 shrinks the gap ~6x to <~0.5m, cost = less fine high-freq surface crinkle (broad landforms unchanged). DIAG tooling committed to ../mapspinner @729ed22 (lab parity-patch + _PROBE_ uProbeSkipCarves). spoint side: no code changed (only scripts/elev-witness.mjs + float-shot.mjs added, server witness). DECISION NEEDED from user: accept the uDetailOverlay reduction (working, look-tradeoff) or accept the residual ~4m / mitigate.