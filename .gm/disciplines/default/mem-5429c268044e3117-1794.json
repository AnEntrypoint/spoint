spoint workspaces merge (2026-07-07): mapspinner + streaming-gltf moved IN-REPO to packages/mapspinner (0.1.264) + packages/streaming-gltf (2.0.22) as npm workspaces; node_modules/<name> are junctions into packages/ so every import specifier (bare + /node_modules/ URL) is unchanged. The registry-tarball install path (package-lock resolved registry.npmjs.org) was the version-lag mechanism -- workspace links end it. patch-deps.mjs postinstall now carries ONLY the two @three.ez/instanced-mesh text patches; every mapspinner/streaming-gltf patch was verified native in the merged source before deletion (grep the marker before deleting a bridge patch -- two were confirmed via globalThis.__thcBakeReadback and _fr = Math.fround markers). StaticHandler serves through the junctions via its isNodeModulesLink realpath exemption (test.js:77 witness). streaming-gltf models/ (308MB) and examples/local-progressive/output_* transient bakes (1.62GB!) are EXCLUDED -- gitignore blocks output*/ regeneration pollution; the global *.png gitignore rule needed a !packages/**/*.png exception or mapspinner's runtime textures would silently drop from git. Parity bracket witnessed the cutover alone: p50 25.9->23.2ms, screenshots pixel-class identical, version deltas 261-263 (hostNearFar depth-writeback fix, shadow bridge, look-uniform dirty-cache) + 2.0.21-22 (occlusion raw-GL box draw, budget round-robin) reviewed via sibling git_log. GET /__identity (StaticHandler route) + boot banner report pid/cwd/sdkRoot/HEAD-sha/package-realpaths -- read it before trusting any port. Worktree trap: packages/* edits inside a worktree are NOT live through the whole-node_modules junction (it resolves to main's packages); worktree-setup.mjs prints this and derives MAIN_ROOT via git rev-parse --git-common-dir.