---
key: mem-daee40b6bfe90fb1-4947
ns: default
created: 1790085539779
updated: 1790085539779
---

## Resolved mutable: webgpu-node-material-specular-aliasing-vs-dither-injection

CAMERA-PINNING FIXED (real, reusable): client/core/camera.js update(localPlayer,...) line 278 'if (mode==="custom"||mode==="fixed") return' -- calling window.__app.cam.setMode('fixed') genuinely no-ops the per-frame TPS/follow update against the live rAF loop. Live-verified via gm browser verb: camera position/quaternion delta measured 0/0 across 1.5s of real gameplay post-pin (http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, real Chromium D3D12/Dawn). Procedure: setMode('tps'), wait ~4.5s for spawn-drop settle to y~11.5, THEN setMode('fixed') (pinning mid-fall freezes the falling pose -- must settle first). Screenshots .gm/witness/specularaa-pinned-pose.png and specularaa-confirmed-pose-final.png show the hatch clearly on two differently-oriented concrete floor/platform panels (grass/sky/player unaffected), reproducing the original bug screenshots' lobby location exactly (pos [-15.35,11.53,-14], matching prior sessions' spawn pose).

METRIC RE-RUN AT CONFIRMED WORST PIXELS (not estimated): raw GPU copyTextureToBuffer readback of the real pre-tonemap rgba16float resolved scene buffer (renderer._getFrameBufferTarget()+backend.get(tex).texture, the literal buffer RenderOutputNode/_renderOutput samples), 64x64 patches, half-float decoded, gradient energy summed per direction. hatchRightFloor (screen 1150,700): horizE=1.86, vertE=17.98, diagE=17.49, antiE=18.19 -- vert/diag/anti ~9.4-9.8x horizontal, confirming strong directional anisotropy. hatchLeftFloor (150,680): horizE=33.25,vertE=25.78,diagE=37.82,antiE=31.30 -- high in all directions (~40-200x control magnitude), consistent with the OTHER floor panel's texture tangent frame being rotated ~90required, both panels anisotropic in their own frame. Controls: grassControl horizE=0.163,vert/diag/anti~4.9-5.1x (tiny absolute magnitude, noise-floor); skyControl horizE=0.74,vert/diag/anti ratios 0.93-0.98 (isotropic, noise-floor). Real signal, ~10-200x control magnitude, confirmed anisotropic at real hatch location.

SPECULAR-AA HYPOTHESIS DECISIVELY FALSIFIED, not forced: raycast-identified the actual hit material at both confirmed worst pixels (matType MeshStandardNodeMaterial, matName 'texture-array-atlas-material-tsl', from packages/streaming-gltf/src/texture-array-atlasTSL.js -- material.colorNode=texture(arrayTexture,uv()).depth(int(layerIndex)), roughness=seedMaterial.roughness??0.8 but live-read roughness=1, hasNormalMap=false). At roughness=1 (max rough, broadest GGX lobe) with zero normal map, there is no meaningful specular BRDF term to alias against -- the geometric-roughness/normal-derivative widening mechanism this hypothesis named has nothing to widen. Captured the REAL compiled WGSL fragment shader live (monkeypatched GPUDevice.createShaderModule, forced material.needsUpdate+render, isolated the 52KB MeshStandardNodeMaterial shader by size/content among 24 captured fragment shaders): the array-texture color sample is 'nodeVar0 = textureSample( nodeUniform0, nodeUniform0_sampler, nodeVarying7, i32( nodeVarying8 ) )' -- automatic derivative-based LOD (textureSample, NOT textureSampleLevel), matching node_modules/three/src/renderers/webgpu/nodes/WGSLNodeBuilder.js's _generateTextureSample fragment-stage branch (isUnfilterable()===false for this rgba8unorm-srgb non-MSAA linear-filtered texture, traced through getComponentTypeFromTexture/isSampleCompare/minFilter/getTextureSampleData -- all four disqualifying conditions false). Live-inspected the actual bound GPUTexture (renderer.backend.get(texVal).texture): mipLevelCount=10 (full chain for 512x512), depthOrArrayLayers=45, format rgba8unorm-srgb, dimension '2d' (array) -- mipmaps ARE fully generated and bound, ruling out a missing-mipmap defeat-of-LOD theory too. No BRDF-LUT-based specular-AA widening node (dot(normalView,V) vs roughness-derivative-scale term) exists anywhere in the captured shader beyond the standard environment-BRDF-LUT sample (nodeUniform14, itself just the ordinary IBL split-sum LUT lookup, also automatic-LOD). CONCLUSION: neither dither-injection (falsified prior session) nor specular/normal-map-aliasing-missing-geometric-roughness-widening (falsified this session, on the actual confirmed material at the actual confirmed worst pixels, with real numbers) explains the anisotropic hatch. Real remaining candidates, narrower and un-explored: (a) the atlas texture's own baked texel content at this layer (never read back -- could be legitimate high-frequency source art amplified differently by WebGPU's sRGB/tonemap combine vs WebGL's), (b) a WebGPU-vs-WebGL difference in default sampler anisotropy/LOD-bias for texture_2d_array specifically (anisotropy is unset, defaults to 1, on both backends -- unlikely but unverified live side-by-side at THIS exact material). Filed narrower: webgpu-texture-array-atlas-texel-content-vs-sampler-lodbias-parity.
