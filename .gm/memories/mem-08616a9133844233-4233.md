---
key: mem-08616a9133844233-4233
ns: default
created: 1790059520127
updated: 1790059520127
---

## Resolved mutable: mut-1790056440331

(a) CONFIRMED: inherent chaotic fp32 amplification, not a WGSL translation bug. Structural check first: terrain-composeheight-wgsl.js/terrain-height-wgsl.js's value_ridged_fbm_rot and terrain.glsl/height-gen.js's (both read in full) are op-for-op IDENTICAL -- same signal=offset-abs(snoise3(p)) -> pow(max(signal,0),exponent) -> v+=signal*w*a -> w=clamp(signal,0,1) -> a*=gain -> p=rotate_domain(p*2,i*0.5236) sequence, same octave counts (10/18/18 via noiseLayer0/1/2), same constants. terrain.glsl's uNoUnroll unroll-defeat term (nb=numOctaves+(uNoUnroll-64)) is a documented no-op at its default uNoUnroll=64 (recall mem-418f1e708786fefe-399), so it does not create a divergence either -- no algorithmic difference found anywhere in the port.

Mechanism, live-witnessed via exec_js (not guessed): implemented the CPU fround-per-op oracle faithfully (mirroring glsl-rt.js's _fr=Math.fround-after-every-scalar-op convention) and instrumented value_ridged_fbm_rot to log v/w/a/p/ns per octave. (1) Position magnitude doubles every octave by construction (p*=2 in rotate_domain) -- measured pmag growing 5.78 -> 758153 over 18 octaves, i.e. exactly 2^17x, confirming the recurrence is isomorphic to the classic doubling map (Lyapunov exponent ln2/octave), a known chaotic/sensitive-dependence dynamical system. (2) Seeding a single 1-ULP perturbation in the input position and propagating it through the same 18-octave loop: the raw per-octave noise sample (ns) diverges by ~1e-6 at octave 0 growing to ~0.01-0.05 by octave 14-17 (5 orders of magnitude amplification over 17 octaves, matching the doubling-map prediction), but the RUNNING WEIGHTED SUM (v, what actually reaches the output) stays ~1e-7 throughout because the amplitude weight a=gain^i=0.5^i decays exactly as fast as the position error grows, so late-octave chaos alone cannot explain a large OUTPUT divergence. (3) The real amplification path is the domain WARP coupling between layers: eval_layer0's low-octave (large-a, undamped) result h0 sets warpOff=pCoords*1.6*h0, so a modest change in h0 relocates 'warped' -- the shared input to BOTH the 18-octave value_fbm (h1) and 18-octave value_ridged_fbm_rot (h2) -- into an uncorrelated region of the noise field. Swept perturbation size at p0 (input to h0's own 10-octave loop) from 1 to 262144 ULPs and measured dH2 growing from 3e-6 to -0.164 to -0.29-scale outputs, i.e. a perturbation on the order of 1e-5 to 1e-2 absolute (well within real GPU-vs-CPU fp32 divergence for cos/sin/FMA in rotate_domain across 10-28 chained octaves) reproduces output divergences of the SAME ORDER OF MAGNITUDE as the reported maxAbsDiff=0.291.

Live cross-check against a REAL GPU (not the JS oracle alone, addressing the mutable's own concern about ANGLE/FXC-class divergence being a separate GLSL-only phenomenon): dispatched the exact WGSL fractalTerrainH kernel on a real WebGPU device via the gm browser verb (Chrome/153, real adapter/device, not software) for dir=[0.929,0.252,-0.271]n (the originally-flagged outlier) and two other directions. Real GPU output vs the same-direction CPU fround oracle: outlier dir absDiff=0.0454, a 'typical' dir [0.4,0.6,0.7]n absDiff=0.0326, and dir=[0,1,0] absDiff=0.2036 (LARGER than the flagged outlier -- mechanistically explained: at this pole, y stays an exact power-of-2 multiple every octave while x/z sit exactly at the snoise3 lattice's floor()-boundary at EVERY octave via rotate_domain, so GPU-vs-CPU cos/sin sign-of-epsilon differences flip the sampled hash cell at every single octave, the maximally sensitive case of the same mechanism). This confirms the divergence is a real, direction-dependent, bounded (does not diverge to infinity, saturates once octaves fully decorrelate), EXPECTED property of the algorithm on real hardware, not a defect in this WGSL port -- both the WGSL/real-GPU field and the CPU/glsl-rt.js field remain individually valid, self-consistent samples of the same underlying procedural function; they are simply not bit-identical at these chaotic-sensitivity directions, exactly as recall mem-56f2c0c0016f50c4-339 already flagged glsl-rt.js's fround emulation as 'heuristic, not bit-exact across vendors/FMA'.
