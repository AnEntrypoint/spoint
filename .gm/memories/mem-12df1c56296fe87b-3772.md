---
key: mem-12df1c56296fe87b-3772
ns: default
created: 1790074217885
updated: 1790074217885
---

## Resolved mutable: mut-1790073757038

Static audit (Read, not codesearch -- codesearch verb was broken this session, dream-rsi circuit-breaker stuck refusing every dispatch after an early malformed-regex call; fix subagent dispatched to C:/dev/gm, fell back to exec_js+git-ls-files+Read per gm's own friction-fix invariant) of every packages/mapspinner/src/webgpu/*.js file and client/core/GrassTSL.js, PlayerVATTSL.js, PlayerLODTSL.js, WebGPUInstancing.js, packages/streaming-gltf/src/instanced-slot-cull-tsl.js + model-pool-instanced-slot.js found no unguarded per-frame device.create*: planet-orchestrator-webgpu.js's ensureDepth/ensureOffscreen (lines 126-144) early-return on unchanged size before device.createTexture; pipeline-cache.js's getPipeline (line 85-86) hits a Map cache keyed by state+shader+format before device.createRenderPipeline; water-render.js/vdrs-composite.js/sky-render.js all gate createBindGroup behind a `this._bindGroupTex !== srcTexture` identity check inside constructors, never inside render(); instanced-slot-cull-tsl.js's resizeInstancedSlotCull (the only place that recreates instancedArray/uniformArray GPU buffers) is called only from model-pool-instanced-slot.js's _grow(), itself only invoked when acquireSlot() finds nextSlot>=capacity (capacity-doubling, bounded amortized growth), never per frame. LIVE-VERIFIED via gm browser verb against the real WebGPU device in the real tps-game world (http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, backend.isWebGPUBackend confirmed): patched GPUDevice.prototype.{createTexture,createBuffer,createBindGroup,createRenderPipeline,createComputePipeline,createShaderModule,createSampler} to count real calls, logged per-second FPS + per-method call counts via a self-sustaining rAF loop into window.__fpsLog, polled across ~150s of real wall-clock browser time (38 one-second samples spanning page-relative t=38.4s to t=124s). RESULT: after the initial scene-load burst (t=38.4s sample: 168 createBuffer+42 createBindGroup, one-time asset/pool setup) and one bounded mid-session capacity-growth burst (t=112.7s: 16 createBuffer+25 createBindGroup, matching InstancedSlot._grow doubling), EVERY OTHER per-second sample across the full window shows zero calls to all 7 tracked GPU-object-creation methods -- no per-frame texture/buffer/bindgroup/pipeline/shadermodule/sampler churn exists in the current live render path. FPS across the window: min 2 (first-second load transient), max 61, last-5-sample average 45 -- fluctuating 37-61 and trending upward over time, never degrading to and holding at ~30 as the original 106s report described. performance.memory.usedJSHeapSize held flat at 214-220MB throughout (matches the original report's 220-290MB, confirming no JS-heap leak either). CONCLUSION: the originally-reported FPS degradation does not reproduce against the current tree -- the hypothesis (unbounded per-frame GPU resource allocation) is disproven by direct live instrumentation, and the resource-bound obligation (steady-state GPU object creation rate bounded near zero) is satisfied as measured. Most plausible explanation: one or more of the 5 commits already landed earlier this session before this task started (87097ba6 'stuck-pipeline permanence bug...oversized uniform buffers', e2a5eaef 'global shadow-casting bug; port InstancedSlot GPU-cull...to TSL', 280893d3 'WebGPU depth-target mismatch') fixed the underlying churn as a side effect -- 87097ba6's own description ('oversized uniform buffers') is a concrete candidate mechanism for exactly this class of per-frame GPU cost. No code change made in this resolution pass since the measured defect no longer exists; re-open with a fresh mutable if a future live session reproduces the degradation again.
