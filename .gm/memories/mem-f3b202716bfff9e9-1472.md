---
key: mem-f3b202716bfff9e9-1472
ns: default
created: 1789394078445
updated: 1789394078445
---

project/fog-band-and-night-ambient-silhouette: 'content fades to silhouette after a few minutes' = day cycle reaching sunset. Two stacked defects (2026-09-14): (1) THREE linear fog inversion -- the time-of-day node proposes FogController ceiling mult 0.45 below the horizon; tps-game fogNear 10000/fogFar 20000 -> far 9000 < near -> smoothstep(near,far,d)=1 -> every THREE object painted flat fog colour (mapspinner sky/terrain/grass unaffected, they skip THREE fog). Fix: FogController (client/core/FrameMetrics.js) is the only writer of fog.near AND fog.far; near = far * configured near/far ratio every tick, perf pull-in lives in its own perfFar so the far plane recovers on the first tick after sunrise. (2) THREE night ambient was lum(0x0d1428)*0.22 ~= 0.0016 irradiance (three 0.185 ambient contributes albedo*irr/PI) vs mapspinner terrain.glsl ambientFloor 0.14*albedo -> THREE content ~280x darker than terrain at night; night keyframes now derive intensity so lum*intensity = 0.14*PI (TimeOfDay.js ambientIntensityMatchingTerrainFloor). Day keyframes already sat at 0.31-0.45. Fixing only (1) turns content pure black. Mapspinner sky stays near-daylight at night: gl-render.js skyExposure mix(48,14,clamp(sunElevDot,0,1)) is max below the horizon (terrain night ratio ~0.9 too), so night brightness is a mapspinner design choice. Witness method: off-screen HEADED chrome (--window-position=-3000,0) because gm's agentplug reaper kills foreign --headless chromes.
