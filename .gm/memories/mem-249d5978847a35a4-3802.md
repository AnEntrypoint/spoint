---
key: mem-249d5978847a35a4-3802
ns: default
created: 1790060661937
updated: 1790060661937
---

## Resolved mutable: mut-1790056440108

Built packages/mapspinner/src/webgpu/patch-grid-render.js: real WGSL vertex+fragment shader pair reusing TERRAIN_COMPOSEHEIGHT_WGSL's composeHeight/fractalTerrainH/continentalBias functions verbatim (string-sliced before its @group(0) binding(0) marker, not reimplemented), wired through pipeline-cache.js's terrain-opaque-cull-none state key via MapspinnerPipelineCache.getPipeline with TERRAIN_PATCH_VERTEX_BUFFERS' real vertex layout. faceFrame()/faceWarp() ported 1:1 from terrain.glsl lines 280-313; vertex shader computes dir0 from instanced (ox,oy,l,level,face) exactly as gl-render.js's terrain-opaque draw, calls composeHeight for displacement, and reproduces terrain.glsl's exact camera-relative RTE math (vRel=(dir0-camDir)*R+dir0*h-camDir*camAlt; gl_Position=viewProjNoEye*vRel) per the mapspinner-splat-uv-camera-relative invariant, using a WebGPU zero-to-one-NDC perspective (NOT gl-render-mat4.js's GL-style [-1,1] perspective, which would clip everything given WebGPU's [0,1] NDC z-range) combined with the real M4.lookAt/mul from gl-render-mat4.js. Grid/index buffer generation and the 6-float instance layout (ox,oy,l,level,face,layer) mirror gl-render.js:946-967/1489-1495 exactly (same hashed-diagonal triangulation), draw is drawIndexed(indexCount, instanceCount) replacing gl.drawElementsInstanced. LIVE-VERIFIED via gm browser verb against a real GPU (Chrome, adapter vendor=nvidia architecture=ampere, real navigator.gpu device, zero WebGPU validation errors via device.popErrorScope, gl.errorTotalCount 0): created the real render pipeline through MapspinnerPipelineCache with the concatenated real WGSL source, built 64 real instanced patches (8x8 grid, face 4, 62500m patches) with real GPUBuffers, ran a real GPURenderPassDescriptor (color attachment = real canvas webgpu context configured with COPY_SRC, depth attachment = real depth24plus texture), issued drawIndexed, then read back REAL pixels via copyTextureToBuffer+mapAsync (bytesPerRow padded to 256 after first attempt correctly surfaced a real WebGPU validation error demanding it). Scanline at canvas mid-height (BGRA byte order confirmed against the exact clear color 0.02/0.02/0.08 = bytes 5,5,20): scan[0]=[20,5,5,255] is exact unrendered background; scan[1..5]=[8,22,10],[14,40,17],[16,46,20],[29,32,35],[19,56,24] are REAL distinct rendered colors (green-dominant at low composeHeight per the lowColor mix, grayer/tan at higher composeHeight) forming a real silhouette boundary against background -- genuine GPU-rasterized, per-vertex composeHeight-displaced terrain, not a placeholder fill. DEFERRED (noted, not silently assumed): fragment shading is a minimal derivative-normal lambert + height-color-ramp (real geometry/displacement, placeholder shading only, matching the task's explicit allowance); hpfPool/sculptTex bound as zero-filled placeholder storage buffers (continentalBias term = 0, sculptOverride disabled) since real hpf/sculpt texture readback wiring from the live orchestrator is separate scope; no LOD morph/skirt (single-level synthetic patches only, morph/skirt are LOD-crack-hiding refinements orthogonal to 'is a patch grid visible'); quads are synthetic test data, not real quadtree.js leaves (orchestrator integration -- feeding real leaves from planet-orchestrator.js's frame() into this renderer -- is separately scoped per webgpu-orchestrator-cullmatrix-glue's own classification of gl-render.js's render() as the remaining ~366-line blocker for that step). Filed webgpu-water-pass-draw and webgpu-sky-and-vdrs-upscale-composite as separately-scoped follow-on mutables for the water sub-pipeline and sky/VDRS-upscale/depth-writeback composite, both explicitly out of scope here. File: packages/mapspinner/src/webgpu/patch-grid-render.js.
