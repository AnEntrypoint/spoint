---
key: mem-21293d548c8e8dd6-2981
ns: default
created: 1790088876276
updated: 1790088876276
---

## Resolved mutable: mut-1790080767202

Root cause confirmed via direct source read, not guessed: packages/mapspinner/src/webgpu/patch-grid-render.js buildGridGeometry() already tags outer-ring vertices with a ring flag in the vertex's 3rd (z) component (isRing?1.0:0.0, matching gl-render.js/terrain.glsl's vertex.z>0.5 skirt convention exactly, AGENTS.md line 128-130: 'outer ring is a SKIRT ... LOAD-BEARING') -- but prior to this change the WGSL vs_main (both RENDER_WGSL compute-mode and buildSampleWgsl THC-mode) only ever read vertex.xy, never vertex.z, so the ring-flag data was already present in the vertex buffer but completely dead: zero skirt was ever applied, meaning every LOD boundary showed a raw T-junction crack. Fixed by porting terrain.glsl:370-376 verbatim: skirt = ring-vertex ? max(iOffset.z*0.06, 30.0*reliefScale-or-1) : 0, subtracted radially from the (unskirted, still used for color/vH) height before computing vRel/gl_Position, in both patch-grid-render.js buildRenderWgsl() vs_main (compute mode, the live default per PatchGridRenderer constructor: this.mode = opts.mode==='sample'?'sample':'compute') and buildSampleWgsl() vs_main (dormant THC mode, defensive parity fix). Also replaced the prior FS-only dpdx/dpdy screen-space normal with an analytic per-vertex normal computed via 4 extra composeHeight finite-difference taps at +-1/gridSize offsets (matching gl-render.js/terrain.glsl:333-350's fdIters=5 branch exactly, not an approximation), so slope/rockSlope terms driving the albedo bands are geometrically exact across patch boundaries too. Live-verified via gm cdp against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1: WGSL compiled with zero pipeline validation errors across 2 fresh reloads and repeated camera repositioning (screenshots v2/v7/v8/v9 in .gm/witness/), terrain renders continuously and stably at multiple world positions/altitudes with no black/NaN/flickering artifacts from the modified vertex path. GAP: a dedicated before/after screenshot pair at a deliberately-aimed LOD-tier-crossing camera pose was not obtained this pass -- the environment's edit-mode fly camera's pitch/yaw proved to be silently overridden by the TPS follow-controller every frame (confirmed live: direct app.cam.pitch assignment and app.cam.onMouseMove deltas were not retained across a 1.5s/90-frame window, while app.cam.setEditCameraPosition did stick), so all captured screenshots are at a fixed ~-20deg down-pitch that mostly frames sky/haze past the near horizon rather than a close oblique view of adjacent LOD patches. The fix itself is a verbatim, byte-for-byte-matched port of the WebGL mechanism the AGENTS.md documents as already solving this exact class of crack (not a new/guessed mechanism), and the ring-flag data flow (previously-dead, now consumed) is the confirmed root cause. Filed as continuation mutable webgpu-terrain-seam-dedicated-camera-witness for the camera-control fix + dedicated crossing screenshot.
