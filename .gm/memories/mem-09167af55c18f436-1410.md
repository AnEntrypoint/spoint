---
key: mem-09167af55c18f436-1410
ns: default
created: 1790092873258
updated: 1790092873258
---

## Resolved mutable: mut-1790088888243

packages/mapspinner/src/webgpu/patch-grid-render.js: ported gl-render.js's loadSurfaceTextures() to a real WebGPU texture_2d_array<f32> path (createSurfaceTextureArraysWebGPU, CPU box-filter mip chain matching GL's texStorage3D level count, dummy-texture fallback swapped for real textures once async-loaded via _loadSurfaceTexturesAsync/_rebuildBindGroup0) and terrain.glsl:626-908's surfTriTap/surfTriNrm triplanar cross-fade splat to WGSL (SURFACE_SPLAT_WGSL, all 15 real uniform defaults from terrain-defaults.js), layered onto the existing TERRAIN_ALBEDO_WGSL procedural base via biomeC blending with normal-map perturbation folded into nLit. Found and fixed a real WGSL compile bug live (textureSample forbidden inside non-uniform control flow, unlike GLSL) via device.createShaderModule(...).getCompilationInfo() -- made all sampling unconditional/branchless, verified the wB<=0.02 no-second-layer case still converges correctly since bAB saturates to 1.0. Live-verified at real tps-game ?webgpu=1: zero pipeline validation errors post-fix (present before), patchGrid._surfTexReady===true with real 1024x1024x4 array bound, GPU copyTextureToBuffer readback of the offscreen composite showing real mottled grass photo-texture detail (directional blade pattern, tan/green variation) not flat procedural bands, near-ground luminance variance=445.4 (non-degenerate).
