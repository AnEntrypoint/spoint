---
key: mem-9cca2def06066b53-2085
ns: default
created: 1790079275390
updated: 1790079275390
---

## Resolved mutable: webgpu-terrain-hardclip-seam-not-viewport-or-cull

Premise re-witnessed and falsified, not silently proceeded on: the task's framing (bug lives in WebGPU's collectQuads/quadtree leaf generation, a hand-written per-renderer implementation diverging from WebGL's planet-orchestrator.js/quadtree.js) does not hold. Diffed WebGPU's collectQuads against WebGL's frame() call site precisely: both call the SAME shared Quadtree class (quadtree.js, one file, imported by both), the SAME shared cull primitives (worldToFaceLocal/quadOutsideFrustum/extractFrustumPlanes/FACE_FRAME from planet-orchestrator-cull.js), and construct near/far via structurally identical formulas (nearFarForCam in webgpu vs the inline copy in gl-render.js:1019-1032 -- same altAboveTerrain/horizon/near/far arithmetic). The only genuine divergence found (WebGPU always passes cullScratch to qt.updateQuadtree; WebGL gates it behind an hcOn near-nadir check) was tested and is NOT the cause: see new mutable webgpu-terrain-hardclip-is-gpu-pipeline-not-quadtree for the 5 live-witnessed tests (recursion-cull disabled, leaf-cull disabled, both disabled simultaneously, camera rotated 90deg, coarse-quads excluded, sky disabled) that conclusively rule out every quadtree/cull JS-side mechanism -- the seam is fixed at the identical screen column (1037/1297, NDC x~0.5995) under every condition, including a full camera rotation that changes all world content on screen, and reproduces even with zero JS-side culling applied. No fix was applied to quadtree/cull code because none is warranted there -- forcing an edit to disprove-code would be a false-completion (Section: 'No admit, no deferral' / 'Overturned premise'). All diagnostic source edits made this session were reverted (git diff clean on packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js, verified via git_diff). This mutable's original claim (quadtree bug) is CLOSED as disproven; the real remaining work is re-scoped under webgpu-terrain-hardclip-is-gpu-pipeline-not-quadtree (GPU-pipeline/clip-space level, not quadtree).
