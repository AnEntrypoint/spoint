---
key: mem-6062277c1a35fc27-404
ns: default
created: 1789393022682
updated: 1789393022682
---

project/auth-compare-length-mismatch-decoy: src/sdk/authCompare.js timingSafeTokenEqual runs a self-compare (bufA,bufA / a,a) on a length mismatch before returning false, so every path costs one constant-time compare and token length is not leaked by early return. Do not 'simplify' it away. The non-Node XOR fallback is only adequate for the Worker's in-trust-boundary compares, not network-facing auth.
