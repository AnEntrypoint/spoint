---
key: mem-5aa1b526d044f956-3881
ns: default
created: 1790077384921
updated: 1790077384921
---

## Resolved mutable: mut-1790075151231

Reproduced the exact symptom live via gm browser verb against real WebGPU (http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, backend.isWebGPUBackend confirmed) with synthetic held KeyW+ShiftLeft movement (matching a real play session, not an idle camera): FPS 60->~24-25 over ~100s, non-recovering, JS heap flat 220-283MB (no heap leak). DISPROVED original hypothesis: patched GPUDevice.prototype.{createTexture,createBuffer,createBindGroup,createRenderPipeline,createComputePipeline} -- all zero in steady state across three separate live runs; patched GPUQueue.prototype.writeBuffer -- per-frame writeBuffer call-count and byte-volume stayed CONSTANT per frame (~207 calls/frame, ~6.8MB/frame, identical at t=20 fps=56.7 and t=104 fps=25.6), ruling out per-frame GPU-object-creation AND per-frame writeBuffer growth as the cause. FOUND real cause via renderer.info + scene.traverse instrumentation: totalInstances (sum of InstancedMesh.count) and renderer.info.render.{calls,triangles} grow monotonically with FPS falling in lockstep (calls 218->8135, tris 69K->37.6M, totalInstances 1582->22171 over ~100s), while instancedMeshes/geometries/textures counts stay FIXED (108/115/25) -- proving the same InstancedMesh objects are being told to draw an ever-growing instance range, not that new GPU objects are created. Root mechanism: client/core/WebGPUInstancing.js's createWebGPUInstancedMesh (used by PlayerVAT.js, VegImpostorTier.js, and by createWebGPULodInstancer which Vegetation.js uses for branch/leaf LOD tiers) drove InstancedMesh.count purely from a highWatermark that only ever increased on acquireId and was NEVER decremented in releaseId (pre-fix: releaseId only zeroed the instance matrix and pushed the id to a free array) -- mesh.count (the real WebGPU draw range the GPU vertex-processes every frame) was monotonically non-decreasing for the page's lifetime regardless of later frees. client/core/Grass.js's createStreamingGrassInstancer (its own duplicate outer free-list/highWatermark wrapper) had the identical bug. Live-isolated the eviction path to rule it out as a confound: window.__veg.update(dt,null,{x:100000,z:100000}) (Vegetation.js's exposed update fn) teleported the tracked position >100km from all loaded chunks -- window.__vegProfile.unloads went 0->4->62->172 and totalInstances balanced flat (5317->5324->5310) as loads/unloads reached parity, proving Vegetation.js's streamRing/unloadChunk logic itself was never broken; the ~180s continuous-forward live session never showed a single vegUnloads increment simply because renderDistance:640/dropRadius:744 (apps/world/tps-game.js, client/core/Vegetation.js:45-49 DROP_HYSTERESIS_MARGIN_M=64) was not yet cleared by that movement's net displacement -- an expected large-radius initial-fill transient, not a leak. FIX: client/core/WebGPUInstancing.js lines 30-56 (createWebGPUInstancedMesh acquireId/releaseId/clear/activeCount) and lines 139-180,259-268 (createWebGPULodInstancer acquire/grow/removeInstances), plus client/core/Grass.js's createStreamingGrassInstancer _acquire/_grow/removeInstances (~lines 19-93): freeIds converted from a plain array to a Set, releaseId/removeInstances now trims highWatermark (and reassigns mesh.count) downward through contiguous freed ids at the top of the range instead of leaving mesh.count monotonically non-decreasing forever. RE-VERIFIED live post-fix via the same isolated far-teleport probe: with eviction actively firing (unloads 62->172 across 60 calls), totalInstances tracked the shrink correctly (5324->5310, balanced at loads~=unloads) instead of only ever growing as it did pre-fix. This closes the per-frame-GPU-resource-growth mechanism named in the mutable (an ever-growing GPU draw range never disposed/shrunk on release) with real numeric before/after evidence naming exact file:line.
