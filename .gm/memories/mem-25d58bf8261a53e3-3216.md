---
key: mem-25d58bf8261a53e3-3216
ns: default
created: 1790064242485
updated: 1790064242485
---

## Resolved mutable: tsl-batchedfartier-relies-on-batchedmesh-raw-glsl-chunks

Re-verified against the currently-installed three@0.185.1 (unchanged from prior session): node_modules/three/src/nodes/accessors/Batch.js still exports only `batch`/`batchColor`, NOT `getIndirectIndex`/`getBatchingColor` (both remain module-private consts, lines 18/35). node_modules/three/src/materials/nodes/NodeMaterial.js:790-806 setupPosition() confirms the exact ordering: if object.isBatchedMesh, batch(object) runs FIRST (mutates the symbolic positionLocal register via positionLocal.assign(batchingMatrix.mul(positionLocal))), THEN if this.positionNode!==null, positionLocal.assign(subBuild(this.positionNode,...)) -- so a custom positionNode that itself reads positionLocal (as ours does via positionLocal.toVar()) receives the ALREADY-BATCHED position for free, no NodeMaterial hook needed for the base transform. No new public hook was added for BatchedMesh+TSL in this three version (checked git-tracked node_modules directly, matching the pinned version; no changelog fetch needed since the installed version is unchanged from the prior session's finding). REAL PORT LANDED (not a fallback): packages/streaming-gltf/src/batched-far-tier-tsl.js reimplements ONLY the small, self-contained getIndirectIndex texel-lookup algorithm locally (4 lines, copied from Batch.js's own non-exported version, reading batchMesh._indirectTexture the same internal-but-plain-JS-property way the OLD raw-GLSL onBeforeCompile patch already read gl_DrawID->getIndirectIndex via THREE's own internal chunk -- same coupling class as before, not a new risk) to index into batched-far-tier.js's OWN _lerpTex, then expresses the GPU lerp as a POST-BATCH ADD: positionLocal.addAssign(mix(p0.xyz,p1.xyz,t).sub(p0.xyz)) instead of mutating batchingMatrix[3].xyz mid-computation -- mathematically identical result (for an affine matrix, replacing the translation column T with T' before M*p is equivalent to (M*p with original T) + (T'-T), which is exactly p0->p1 lerp offset). colorNode reimplements the pow(vColor.rgb,2.2) gamma correction via vertexColor(). packages/streaming-gltf/src/batched-far-tier.js now branches in the BatchedFarTier constructor on pool.renderer.isWebGPURenderer: WebGPU path calls makeBatchedFarTierMaterialTSL and wires uNow via the returned TSL uniform node in updateNow(); WebGL path is byte-identical to before. LIVE-WITNESSED via gm browser verb against a real WebGPU device: a real THREE.BatchedMesh (1 triangle instance, real vertex-color geometry) rendered through the ported material with a lerp target from (0,0,0)@t=0s to (6,0,0)@t=2s -- at uNow=0 the lit pixel cluster centroid was cx=127.5 (minX=115,maxX=140); at uNow=2 (t=1, full lerp) the SAME live renderer/scene re-rendered with cx=204 (minX=192,maxX=216) -- a real ~76px rightward shift matching the world +6 unit offset, zero WebGPU validation errors, confirming the position offset is genuinely computed and applied per-frame from the TSL positionNode, not a static/no-op path. This supersedes the sibling finding tsl-batched-far-tier-onbeforecompile-silent-noop (its 'safe no-op, feature-less' fallback verdict is now replaced by a real working port).
