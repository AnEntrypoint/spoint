---
key: mem-b3663f2255dd56b5-4026
ns: default
created: 1790067105380
updated: 1790067105380
---

## Resolved mutable: mut-1790059481818

Live-measured on a real GPU (Chrome via gm browser verb, http://localhost:8080/planet.html, nvidia/ampere adapter, navigator.gpu + EXT_disjoint_timer_query_webgl2 both present, gl.errorTotalCount 0 throughout). METHODOLOGY: the 2026-06-15 baseline (mapspinner/AGENTS.md THC entry, planet.html __diag.gpuTimer) profiled the MAIN DECK RENDER FRAME's VS/FS split (full vs uFsCheap short-circuit, EXT_disjoint_timer_query_webgl2 TIME_ELAPSED_EXT, median-of-N-samples) -- that exact scene/mechanism does not apply to a bake-tile microbenchmark (bake is an on-demand side-channel, not part of the per-frame deck draw), so per this mutable's own re-scoped text ('Measure VS/FS-cost-split of height-bake-compute.js against gl-render.js ensureBake/drawBakeTile') I measured the BAKE-PRODUCTION pass itself, reusing the SAME TIME_ELAPSED_EXT mechanism and median-of-samples convention as the original gpuTimer. N=64 tiles, THC_BAKE_RES=130x130 R32F (matches gl-render.js/height-bake-compute.js's shared constant), varying (ox,oy) per tile, face=0, defRadius=6371000. WEBGL2 (real production entry point globalThis.__thcBakeReadback -> drawBakeTile fullscreen-triangle draw + PBO readback + getBufferSubData, TIME_ELAPSED_EXT bracketing each call): 64/64 samples valid (no disjoint), medianMs=0.7107, meanMs=0.6286, maxMs=1.8198. WEBGPU (height-bake-compute.js's real createHeightBakePipeline/bakeMain WGSL kernel, identical composeHeight call, timestamp-query feature present on this adapter so used real GPUComputePassDescriptor.timestampWrites, NOT the wall-clock fallback, for the GPU-side number): pure compute-pass GPU time 64/64 samples, medianMs=0.0655, meanMs=0.0563, maxMs=0.1311 -- ~10.8x lower median GPU time than WebGL2's draw+copy for the identical per-texel composeHeight workload (16900 invocations/tile either way), consistent with a fullscreen-triangle FS draw paying rasterizer/ROP fixed-function overhead a compute dispatch does not. Also measured WEBGPU WALL-CLOCK round-trip (GPU dispatch+copyTextureToBuffer+submit+mapAsync, CPU-observable issue-to-data-in-hand latency, the fair per-call comparison to WebGL2's synchronous blocking readback): medianMs=4.03, meanMs=10.21 (skewed by one 377.75ms outlier, min=1.28ms) -- SLOWER than WebGL2's 0.71ms synchronous path at this one-tile-at-a-time granularity, because WebGPU's mapAsync/task-queue Promise round-trip imposes real fixed per-call overhead that a blocking WebGL2 readback avoids; this overhead is an artifact of reading back one tile synchronously and is not representative of the live hot path (gl-render.js's real per-frame usage is bakeTileToLayer, drawing straight into a GPU-resident pool array layer with ZERO CPU readback -- not exposed as a global so not directly measurable here, but structurally identical to the timestamped compute-pass-only number, not the mapAsync wall-clock number). VERDICT (bake-production only, the bar this mutable's own text sets): net-POSITIVE for the compute port's GPU execution cost (~10x faster for equivalent work), net-NEGATIVE for a naive one-tile-at-a-time synchronous-readback usage pattern (~5.7x slower wall-clock) -- the sign flips entirely on which consumption pattern is used. RECOMMENDATION: adopt height-bake-compute.js as THC's live-path replacement ONLY via the GPU-resident pool-layer pattern (mirroring bakeTileToLayer, no per-tile CPU readback), never via a synchronous per-tile mapAsync round-trip. CRITICAL CAVEAT, filed as new mutable webgpu-thc-sample-consumption-path-untested: the 2026-06-15 net-negative finding was about the MAIN RENDER's FS-cost-shift from SAMPLING the baked pool (texture() fetch replacing per-vertex composeHeight), not about bake-production cost -- patch-grid-render.js's fragment shader does not sample any height pool yet (placeholder shading only), so this resolution does NOT determine whether WebGPU sampling would repeat the original regression; that requires a separate build+measure pass before full live-path adoption.
