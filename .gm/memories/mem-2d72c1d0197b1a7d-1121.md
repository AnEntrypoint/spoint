---
key: mem-2d72c1d0197b1a7d-1121
ns: default
created: 1789417753422
updated: 1789417753422
---

project/app-motion-streams-all-bodytypes (807c4e39+dee0e366, supersedes 'moving apps must declare kinematic/dynamic'): app writes to entity.position stream for every bodyType. TickHandler calls appRuntime.trackStaticMotion(snapHz*1s) per snapshot: a static entity whose transform changed without a _staticVersion bump joins _movedStaticIds -> getUnmanagedDynamicIds -> delta cache; 1 s idle demotes it and bumps _staticVersion so the static tier carries the rest pose to late joiners. Kinematic bodies are app-authored: activation callbacks skip them, getUnmanagedDynamicIds includes them, _syncDynamicBodies pushes entity->body (float32 compare) instead of reading back. Shared cell encodes tier by the nearest player in the cell (was cell centre: 1/16 rate for everything). A tier skip never applies without a viewer baseline. Prop 15 Hz cap = solver-simulated dynamic bodies only. Dynamic Jolt bodies still need ctx.physics/world.setPosition (bare writes are overwritten by syncDynamicBody). Harness: Node WS client + SnapshotProcessor, --import loader that serves HEAD copies of chosen src files for before/after A/B.
