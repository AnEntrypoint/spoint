---
key: mem-3008617ad651c5c8-3227
ns: default
created: 1790092010273
updated: 1790092010273
---

## Resolved mutable: webgpu-fullglobe-sweep-91to149m-source-unaccounted

ROOT CAUSE FOUND AND FIXED: real parameter-wiring mismatch, same class as the prior reliefScale-threading bug. packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js's initMapspinnerPlanetWebGPU() built patchGrid's composeHeight opts (line 119) WITHOUT landBias/beachShelfM, so createComposeHeightParams (patch-grid-render.js:428) fell back to its own defaults landBias=0, beachShelfM=150 -- but the CPU sampler (height-cpu.js:15-21, HEIGHT_UNIFORM_DEFAULTS spreads SHAPE_UNIFORM_DEFAULTS from terrain-defaults.js) and the live WebGL render (gl-render.js:626, g('landBias', TD.landBias)) both use TD.landBias=-100000.0 (RenderControls.js:175-176 flags this exact field 'Parity-critical'). WGSL composeHeight (terrain-composeheight-wgsl.js:240, verified byte-identical to terrain.glsl:204's `h = frac*750000+cbias+uLandBias`) consumes landBias pre-clamp, then multiplies by reliefScale (0.001 for tps-game): a landBias delta of +100000 (0 vs -100000) times reliefScale 0.001 = +100m, exactly matching the reported +91m-149m GPU-always-higher band (variance from the nonlinear beach-shelf clamp near h=0 shifting which samples land in/out of the clamp band). Also found and fixed a second real mismatch of the identical class: planet-orchestrator-webgpu.js:102 defaulted hpfRes (anchor-field bake resolution) to 64 while height-cpu.js:18 and the live WebGL planet-orchestrator.js:43 both default to 128. FIX: imported SHAPE_UNIFORM_DEFAULTS into planet-orchestrator-webgpu.js, threaded landBias/beachShelfM (opts.landBias ?? SHAPE_UNIFORM_DEFAULTS.uLandBias, opts.beachShelfM ?? SHAPE_UNIFORM_DEFAULTS.uBeachShelfM) into the patchGrid composeHeight opts, and changed the hpfRes default 64->128. LIVE RE-VERIFIED via gm browser verb (real Chromium/Dawn WebGPU, http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, SW unregistered + caches cleared, confirmed swCount:0 cachesLeft:0 before test): (1) direct GPU buffer readback (copyBufferToBuffer+mapAsync) of the LIVE patchGrid.composeHeightParams.scalarA post-fix reads [-100000, 150, 0, 0] (was [0,150,0,0] pre-fix) and paramsU.y (hpfRes) reads 128 (was 64); (2) re-ran the original 6-face+8-diagonal (14-direction) GroundProbe sweep dispatching the SAME compute pipeline directly against the LIVE patchGrid.composeHeightParams buffers (paramsU/scalarA/scalarB/basis/hpfPool/sculptTex -- byte-identical by construction, not copied) vs window.__terrain.heightAt(dir) (the live CPU height-cpu.js oracle TerrainBackdrop actually constructed for this world): meanAbsDiff dropped from the previously-reported 91-149m systematic band to 2.25m, maxAbsDiff 9.00m, mixed-sign (GPU sometimes higher sometimes lower, e.g. dir=[-0.577,-0.577,0.577] cpu=51.79 gpu=49.55; dir=[0.577,0.577,-0.577] cpu=-350 gpu=-350.00003) -- fp32-rounding-class, consistent with the already-characterized sub-5-9m mixed-sign divergence from the resolved fractalTerrainH/composeHeight port mutables, confirming the systematic +91-149m divergence is fully accounted for and closed. Files: packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js (landBias/beachShelfM threading + hpfRes default fix), no comments added.
