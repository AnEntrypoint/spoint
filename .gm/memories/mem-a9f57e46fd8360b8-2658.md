---
key: mem-a9f57e46fd8360b8-2658
ns: default
created: 1790086095022
updated: 1790086095022
---

## Resolved mutable: mut-1790081375993

PARTIALLY VERIFIED, rescoped -- not fully closed, real gap named precisely rather than assumed passing. (1) CPU composeHeight sanity across all 6 cube faces: live window.__terrain.heightAt(dir) sampled at the 6 canonical face-center directions (+-X,+-Y,+-Z, i.e. the center of each of the 6 cube-sphere faces) plus 2 diagonals on real tps-game (radius 63600): all 8 return finite, sane values in [-268.5, +51.8]m (no NaN, no degenerate blowup on any face) -- +X=0.81, -X=-62.88, +Y=-220.70, -Y=-146.04, +Z=-232.57, -Z=-268.54, diag=-128.22, diag2=51.79. (2) Formula-level WGSL==GLSL==CPU identity for composeHeight/vRel/camAlt was already established this session's earlier resolved mutables (mem-955ca45d1cb0cd8c-3669, mem-8546529adc441e6f-3670) via direct source diff, not re-derived here; this identity is face-agnostic (composeHeight has no per-face branch; face-specific logic lives only in the shared FACE_FRAME table used identically by quadtree culling, placement, and the WGSL renderer). (3) One-face live numeric parity reconfirmed fresh this session: 12 real vegetation instances (render/placement-adjacent) match CPU exactly (delta=0). GAP NOT CLOSED: a genuine live per-face 3-way numeric diff (WebGPU-rendered height vs CPU height-cpu.js vs the REAL physics collider's query result) across multiple non-spawn-adjacent faces was NOT performed -- no client-exposed physics-collider query surface exists (TerrainPhysics.js's real accessor runs server/worker-side with no debug bridge to window; ColliderDebug.js is a CPU-groundHeightLocal-driven visual overlay, not an independent collider query, so it cannot catch a genuine collider-vs-CPU divergence by construction), and building a live GroundProbe (packages/mapspinner/src/webgpu/ground-probe-compute.js) with correctly-baked hpf/sculpt buffer data matching the render's own bake was out of budget this pass (attempting a raw GPU buffer readback of the render's own composeHeightParams failed -- those buffers lack COPY_SRC usage, confirmed via a live GPUValidationError, and briefly corrupted the frame requiring a reload to recover, so that path is also not viable as-is). Filed as a new narrower mutable (webgpu-fullglobe-parity-needs-collider-debug-bridge-and-copysrc-probe-buffers) naming both concrete blockers for a follow-up pass: (a) add a TerrainPhysics debug bridge exposing the real collider height query to window in singleplayer, (b) add COPY_SRC usage to patchGrid.composeHeightParams buffers (or build a parallel probe-only buffer set) so a live GPU readback/GroundProbe cross-check is actually possible without re-baking from scratch.
