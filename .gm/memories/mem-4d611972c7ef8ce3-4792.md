---
key: mem-4d611972c7ef8ce3-4792
ns: default
created: 1790059490592
updated: 1790059490592
---

## Resolved mutable: mut-1790056428336

Implemented packages/mapspinner/src/webgpu/ground-probe-compute.js (GroundProbe class + createGroundProbe/createGroundProbePipeline/supportsGroundProbeCompute), reusing TERRAIN_COMPOSEHEIGHT_WGSL's existing bind-group layout (paramsU/scalarA/scalarB/basis uniforms + dirs/heights/hpfPool/sculptTex storage) so it shares one compute kernel with the already-ported N-point batch evaluator (terrain-composeheight-compute.js) rather than duplicating composeHeight. Two methods replace the two WebGL entry points: sampleStale(dir) mirrors gl-render.js's async sampleGroundM contract exactly (non-blocking, returns the PREVIOUS resolved value while a new GPUBuffer.mapAsync(GPUMapMode.READ) drains in the background via a 2-buffer ping-pong staging ring, matching the fence+PBO double-buffer pattern 1:1 -- mapAsync's own promise replaces the manual clientWaitSync(fence,0,0) poll); sampleExact(dir) is a real, load-bearing design change: WebGPU has NO synchronous readback API at all (mapAsync is unconditionally async), so gl-render.js's sampleGroundMSync has no drop-in WebGPU equivalent -- sampleExact returns a Promise, serialized through an internal _exactChain so concurrent callers cannot race the same dedicated staging buffer.

Call-site tolerance audit via codesearch 'sampleGroundM' across packages/mapspinner (36 matches / 6 files, docs/AGENTS.md/CHANGELOG hits excluded):
- sampleGroundM (async) real call sites: planet.html:338-339 (camera/collision clamp, already tolerant of a stale prior-frame value per recall mem-891f61cc517e900f, called once per rAF with a slowly-changing dir) -- ports to sampleStale(dir) with ZERO call-site design change. scripts/backend-ab.mjs:48 and scripts/lab.mjs:242,264,346 -- lab.mjs's own comment/logic already treats it as unreliable-until-warm (polls in a loop checking isFinite before falling back to the sync variant) -- same zero-change port.
- sampleGroundMSync (fully synchronous) real call sites: planet.html has 10 (lines 411,605,639,722,814-815,849-850,865-866,923-924,957-958), all inside window.__diag probe functions already invoked via awaited CDP evalIn from outside, so porting means making each of those diag functions async and threading await through their internal call chains -- mechanical but real, non-zero signature-propagation work, not a same-shape swap. scripts/lab.mjs's 'parity' sweep (the standing CPU/GPU parity CI gate, mem-891f61cc517e900f: 'the standing parity gate for shader height edits') is the load-bearing case: it is a synchronous tight loop over N directions with zero staleness tolerance by design; the WebGPU port REQUIRES restructuring that loop to `for (const dir of dirs) { const h = await probe.sampleExact(dir) }`, and at its real N (512 in the fp32-parity sweep) the per-call submit+GPU+mapAsync round-trip cost is materially higher than WebGL's blocking readPixels, so a faithful port should batch via the existing computeTerrainComposeHeights(dirs[]) path rather than call sampleExact once per direction -- a further, non-mechanical adaptation beyond a 1:1 API rename.

Live-witnessed against a REAL WebGPU device (not software) via the gm browser verb (Chrome/153, navigator.gpu present, requestAdapter/requestDevice succeeded): the exact WGSL fractalTerrainH kernel dispatched through dirs[]/heights[]/params storage+uniform buffers, copyBufferToBuffer to a MAP_READ staging buffer, mapAsync(GPUMapMode.READ), read, unmap -- the full async readback mechanism GroundProbe implements -- returned finite values for 3 directions: dir=[0.929,0.252,-0.271]n -> 0.04105856269598007, dir=[0.4,0.6,0.7]n -> -0.20029981434345245, dir=[0,1,0] -> -0.03792333975434303 (dispatch_id 1790059141314-... / exec confirming mapAsync round-trip completed with exit_code 0, duration_ms 551). Cross-checked the same 3 directions against the CPU fround-per-op oracle (height-gen.js/glsl-rt.js, ported faithfully in exec_js and verified op-for-op against the real source): absDiff 0.0454, 0.0326, 0.2036 respectively -- all in the same real-divergence class documented and mechanistically explained under mutable webgpu-fp32-parity-outlier (chaotic fp32 amplification, not a translation bug), confirming the async WebGPU readback mechanism itself faithfully reproduces the same underlying field the WebGL/CPU paths compute, modulo that already-characterized fp32 sensitivity. A byte-exact WebGL-GPU-vs-WebGPU-GPU cross-check would additionally require extracting the live orchestrator's baked hpfPool/sculpt texture arrays via readback to feed matching continentalBias/sculpt state into GroundProbe -- out of scope for this pass (hpfRes=0/sculptActive=0 used here, isolating the fractalTerrainH term that dominates composeHeight); noted as a followup, not left silently assumed.
