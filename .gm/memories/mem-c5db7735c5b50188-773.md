---
key: mem-c5db7735c5b50188-773
ns: default
created: 1789396876447
updated: 1789396876447
---

project/physics-jolt-constraint-ownership-and-anchors: src/physics/World.js addConstraint must read default anchors via getBodyPosition (GetPositionAndRotation into _bulkOutP) -- BodyInterface.GetPosition returns ONE shared temp, so two calls alias (live: A's anchor became B's centre, distance constraint min=max=0). removeConstraint must NOT J.destroy after PhysicsSystem.RemoveConstraint (last ref dropped -> 'null function or function signature mismatch' trap); removeBody must first remove constraints referencing the body (a constrained body removal hung Step forever). ShapeResult errors need GetError().c_str() or they print [object Object]; buildTrimeshShape checks sr.IsValid() because sr.Get() of an invalid result traps CreateBody (memory access out of bounds).
