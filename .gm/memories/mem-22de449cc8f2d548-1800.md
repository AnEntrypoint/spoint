---
key: mem-22de449cc8f2d548-1800
ns: default
created: 1790067904461
updated: 1790067904461
---

## Resolved mutable: webgpu-planet-depthwriteback-empty-colorattachments-mismatch

Confirmed STALE, superseded by a later sibling-session fix -- not a live bug. Read current packages/mapspinner/src/webgpu/pipeline-cache.js:76,97 (getPipeline): 'const depthOnly = !!opts.depthOnly' and 'targets: depthOnly ? [] : [{format:colorFormat,...}]' -- fragment.targets is already [] when depthOnly is set. vdrs-composite.js DepthWriteback._pipelineFor (line ~310) calls pipelineCache.getPipeline('depth-writeback-colormask-off', {..., depthOnly:true, ...}), so its pipeline has fragment.targets:[], matching planet-orchestrator-webgpu.js frame()'s depthPass colorAttachments:[] (line 250) exactly -- zero count mismatch. The mutable's second concern (DepthWriteback.render() silently dropping opts.depthFormat via destructuring) is also already fixed: render(passEncoder, {srcDepthTexture,...,depthFormat}) destructures depthFormat and uses it via this._pipelineFor(depthFormat || this.depthFormat) (vdrs-composite.js line ~320), and the call site passes depthFormat: sharedDepthTexture.format explicitly. Live-verified via gm browser verb against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (real Chromium D3D12/Dawn backend) with window.__hostNearFar genuinely set ({near:0.3,far:2000}, confirmed live) so the depthPass branch executes every frame: captured full console output across two independent page loads and 1500+ real device.queue.submit calls -- zero WebGPU validation/compile errors mention 'depth-writeback', 'vdrs-', 'fullscreen-blit', or 'patch-grid' pipeline labels; all captured errors (MeshBasicNodeMaterial DXC E_FAIL, oversized uniform buffer bindings) are pre-existing, independently-tracked, unrelated issues per mem-c7568d0f1fd78ff1-3104 and mem-e9a22e8f3f32501f-5861.
