---
key: mem-a16f9b6d0489519a-923
ns: default
created: 1789414408207
updated: 1789414408207
---

project/hotreload-releases-context-via-releaseappcontext: src/apps/HotReloadQueue._execute and AppRuntime.detachApp share AppRuntime._releaseAppContext(eid, ctx) (disposers, EventBus.destroyScope, clearTimers, proximity and player-contact watches). Hot reload then rebuilds _updateList/_rebuildCollisionList, or update() keeps running with the torn-down ctx. A released bus scope refuses late on/once (a still-running async setup), and a setup's completion only clears _pendingSetupIds when rt.contexts.get(eid) is still its ctx. _safeCall resolves after logging a sync throw: the rejection was unhandled and would crash a Node server with no unhandledRejection handler. ctx.onShutdown and ctx.time.* are still not tied to the ctx (AppContext.js, PRD appcontext-untracked-registrations-survive-release). Witness harness pattern: real AppRuntime + AppLoader, queueReload + _drainReloadQueue, count bus._handlers per channel.
