---
key: mem-bcbe7f0e68ac0311-2889
ns: default
created: 1790064222403
updated: 1790064222403
---

## Resolved mutable: tsl-grass-streaming-webgpu-port

client/core/Grass.js: added createStreamingGrassInstancer(scene,geo,material,initialCapacity,attributeSchema) (lines 15-106), an id-stable adapter over client/core/WebGPUInstancing.js's createWebGPUInstancedMesh exposing InstancedMesh2's own call-site surface (addInstances/removeInstances/setUniformAt/setVisibilityAt/resizeBuffers) so commitChunk/unloadChunk/streamRing/update/applyOcclusion in createGrass() are unchanged between the WebGL and WebGPU branches. Own freeIds/highWatermark bookkeeping (bypassing rec.acquireId/releaseId) plus _matrixData/_attrData/_visibleData snapshot maps let _grow() replay every active id into a larger createWebGPUInstancedMesh instance at the SAME id (never renumbering, since loaded.entries[].id is held externally) -- mirrors PlayerLOD.js's ensureCapacity discard+rebuild pattern per the mutable's own instruction, extended for id stability. client/core/GrassTSL.js: replaced attribute('instWorldXZ','vec2') (which had no populated source) with instWorldXZ = instanceMatrixNodeFor(builder.object).mul(vec4(0,0,0,1)).xz computed inline in displacedPosition and threaded into bendField(instWorldXZ)/scorchField(instWorldXZ) (now parameterized instead of reading a module-level attribute) -- this is the exact same instanceMatrix[3].xz translation-column read the raw GLSL shader (GrassMaterial.js line 96: 'vec2 gWXZ = instanceMatrix[3].xz') already used, so both renderer paths now derive world-XZ identically with no extra per-instance buffer. Grass.js's WebGPU branch (renderer.isWebGPURenderer) now builds real geometry/material via makeGrassMaterialTSL + createStreamingGrassInstancer for both im/imMid instead of the prior fail-open stub, and update() calls syncGrassMaterialTSL(grassNodes,wind) each frame so wind/bend/decal uniform nodes (detached copies per GrassTSL's uniform() calls) stay live. LIVE-WITNESSED via gm browser verb against a real WebGPU device (Chrome, navigator.gpu, WebGPURenderer.init() succeeded, zero WebGPU validation/compile errors beyond a benign 'normal not found' TSL warning): a single WebGPUInstancedMesh with 2 real instances (id0 pos=(0,0,0) scale4, id1 pos=(1,0,0) scale8) rendered via real GrassTSL positionNode/colorNode -- twoBlades lit=13602 minX=0 maxX=255 avg=[179,211,122] (real grass-green); setVisibleAt(id1,false) dropped it to oneHidden lit=8761 minX=0 maxX=82 (exactly matching the blade0-only footprint, proving id1 was genuinely removed from the draw, not a caching artifact); setVisibleAt(id1,true) restored lit=13602 exactly (bothAgain), confirming acquireId/setMatrixAt/setAttributeAt/setVisibleAt all round-trip correctly live through the real render pipeline. dispatch via mcp__gm__gm verb=browser, url=http://localhost:8877/_scratch/witness.html serving the real repo tree (node static server), console RESULT JSON captured directly.
