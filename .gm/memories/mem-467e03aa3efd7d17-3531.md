---
key: mem-467e03aa3efd7d17-3531
ns: default
created: 1790071730703
updated: 1790071730703
---

## Resolved mutable: tsl-instancedslot-gpu-cull-patch-blocks-far-tier-webgpu

Built packages/streaming-gltf/src/instanced-slot-cull-tsl.js (createInstancedSlotCullTSL/applyInstancedSlotCullPositionNode/resizeInstancedSlotCull/syncInstancedSlotCullBounds) using three/tsl instancedArray-backed per-instance vec4(center.xyz,radius) storage buffer + uniformArray frustum planes (live reference to CachedFrustumPlanes.planes, auto-synced every render frame per UniformArrayNode.updateType=RENDER, confirmed via node_modules/three/src/nodes/accessors/UniformArrayNode.js:118). packages/streaming-gltf/src/model-pool-instanced-slot.js InstancedSlot constructor (lines 39-52) now branches on material.isNodeMaterial: WebGPU/TSL materials skip the dead onBeforeCompile patch and the GPU-instance-matrix-texture readback (which was previously leaving every instance at a zero/degenerate matrix under WebGPU since onBeforeCompile is a silent no-op on NodeMaterial -- instances were invisible), always writing real transforms via mesh.setMatrixAt/instanceMatrix (natively supported by WebGPURenderer+NodeMaterial), and instead clones the material (avoiding corrupting GlobalMaterialPoolTSL._farMaterial, a singleton reused across many InstancedSlot instances) and wires a positionNode that assigns positionLocal to a fixed off-frustum sentinel (1e7,1e7,1e7, a degenerate-point trick portable to WGSL without relying on NaN-division semantics) when the per-instance bound-sphere fails all 6 frustum planes, mirroring the WebGL onBeforeCompile patch's discard logic 1:1. flushMatrixUpdates() and _grow() (lines 214-255) sync/resize the cull buffer each frame and on capacity growth respectively. LIVE-WITNESSED via gm browser verb against a real WebGPURenderer (Chrome, backend.isWebGPUBackend=true): constructed InstancedSlot(pool,null,0,0,boxGeo,GlobalMaterialPoolTSL(renderer)._farMaterial) -- the exact production far-tier TSL material -- confirmed material.isNodeMaterial=true and slot._webgpuCull truthy (branch taken, old onBeforeCompile path skipped). Two instances (A at x=-1.5, B at x=+1.5, boundSphere radius 0.4) rendered via readRenderTargetPixelsAsync: baseline (real camera frustum planes, both comfortably inside) rendered 1444 total lit pixels, bit-for-bit matching a control scene of two plain THREE.Mesh instances at the same positions (1444). After overwriting one frustum plane (planes[0] = (-1,0,0,1.0), chosen so only B's bound-sphere test fails: dist=-0.5 < -radius=-0.4) and re-rendering with camera/geometry unchanged, lit pixel count dropped to exactly 722 with bbox Y:153-181 -- bit-for-bit identical (pixel count AND bounding box) to a control scene rendering ONLY instance A in isolation via a plain THREE.Mesh (722, bbox Y:153-181). This proves the TSL cull mechanism removes exactly the targeted off-plane instance while leaving the surviving instance pixel-perfect vs ground truth. Growth also verified live: acquiring 40 more slots grew capacity 32->64, slot._webgpuCull.capacity tracked the new capacity (growCullCapacityMatches=true), and a subsequent render completed with zero WebGPU errors (postGrowRenderOk=true). model-pool.js's vertex-color instanced fallback (createVertexColorNodeMaterial, also a MeshLambertNodeMaterial) reaches the identical isNodeMaterial branch, so it is fixed by the same change. Dispatch: gm browser verb, url=http://127.0.0.1:8879/_scratch/witness-instancedslot.html serving the real repo tree via a local node static server (torn down after verification, no residual files).
