---
key: mem-1397e6681c186234-3209
ns: default
created: 1790074118827
updated: 1790074118827
---

## Resolved mutable: mut-1790073645732

No independent double-draw defect exists in entity/model pooling, scene.add() call sites, or terrain frame() call sites -- exhaustively checked and ruled out: (1) codesearch of every isWebGPURenderer/isNodeMaterial branch added this session (client/core/Grass.js:138-161, Vegetation.js:34-131, PlayerLOD.js:204, PlayerVAT.js:112-113, Rocks.js:99, model-pool.js:264,1382,1803-1886, WebGPUInstancing.js createWebGPUInstancedMesh/createWebGPULodInstancer) are all clean single-branch if/else: the non-taken WebGL path's InstancedMesh2/scene.add() never executes when the WebGPU path runs, and vice versa -- no dual-add found. (2) codesearch for '.frame(' across the whole tree: planet.frame( is called from exactly ONE production call site, client/core/TerrainBackdrop.js:186, inside renderPlanet(); TerrainBackdrop.js:33-44's isWebGPU branch that picks initMapspinnerPlanetWebGPU vs initMapspinnerPlanet is a real if/else executed once at init, not two paths that can both run per frame. (3) packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js's frame(): ensureOffscreen/ensureDepth destroy-then-recreate correctly on resize, createView() calls are all live per-frame (not cached against a stale destroyed texture), single mainColorTex/sceneCopyTex/depthTex owned per orchestrator instance -- no bind-group/texture-view staleness found. (4) Live browser witness (?webgpu=1, real tps-game, localhost:8099): screenshot .gm/witness/doubledraw-1.png and doubledraw-2.png, plus scene.traverse() over all Mesh/InstancedMesh/BatchedMesh objects (209 total, 116 geometry-groups) -- every 'duplicate geometry' group found is either (a) invisible pooled slots at [0,0,0] (model-pool preallocated capacity, expected) or (b) multiple legitimate instances of the same asset at DIFFERENT world positions (normal instancing, not a duplicate-at-same-transform). No two mesh objects share both geometry AND matrixWorld position while both visible. CONCLUSION: the user-observed 'double-drawn/ghosted' visual is the SAME root cause as the already-tracked webgpu-planet-camera-rotation-flipped / webgpu-planet-terrain-worldspace-wrong / webgpu-planet-landscape-not-visible mutables (owned by sibling session gm-tsl-convert-20260922-cameraworldfix), not an independent defect: the live screenshot shows the mapspinner-webgpu terrain/sky pass rendering as a disconnected rectangular teal region confined to roughly the upper half of the canvas (straight axis-aligned screen-space edges, not a 3D-positioned floating quad) while THREE's own correctly-positioned entity/prop scene (player, concrete platform, rocks) renders normally below/around it -- exactly the signature of a broken view/projection (wrong FOV/aspect/Y-flip in patch-grid-render.js's hand-rolled WebGPU perspective, per the sibling camera-rotation-flipped mutable) confining the terrain draw to less than the full screen rather than two independently-drawn copies of the same geometry. Not fixed here (sibling session owns that file to avoid a collision); this mutable is resolved as 'same root cause, not a separate bug' per the task's explicit instruction to check the overlap before treating them as independent.
