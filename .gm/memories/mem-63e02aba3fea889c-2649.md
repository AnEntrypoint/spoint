---
key: mem-63e02aba3fea889c-2649
ns: default
created: 1790075469043
updated: 1790075469043
---

## Resolved mutable: mut-1790073656058

Downstream of the other two, which are proven not-broken, so re-scoped rather than confirmed-as-reported. Live GPU pixel evidence via gm cdp verb, real tps-game world, real D3D12/Dawn WebGPU device, at http://localhost:8099/?singleplayer&world=tps-game&webgpu=1: (1) mapspinner's own offscreen mainColorTex, read back full-resolution via copyTextureToBuffer (not a synthetic scene) at multiple real controlled camera poses, shows spatially-varying non-degenerate terrain/sky colors covering the FULL 1297x812 buffer edge-to-edge (20x20 grid samples), quadCount 90-144, zero WebGPU validation errors. (2) The REAL SHARED THREE-owned MSAA framebuffer target (renderer._getFrameBufferTarget()'s msaaTexture, the actual buffer THREE's own scene pass and final tonemap/output quad consume) was independently read back via a resolve-pass + copyTextureToBuffer (rgba16float, correctly decoded) and also shows broad, varied, non-uniform real color coverage, not a black/empty canvas -- confirming mapspinner's terrain+sky output does reach the buffer that gets presented. This directly contradicts a rendering-pipeline-level 'not visible' defect. What could NOT be confirmed: a clean human-eye screenshot of the open-world planet backdrop from real unobstructed gameplay. Every real screenshot obtained (via both the browser and cdp verbs) showed the player's fixed spawn point inside a small hand-built lobby/dock structure (wood-plank flooring/ceiling, rock walls) with the 'Click to play' pointer-lock prompt permanently stuck -- requestPointerLock fails in this CDP harness ('root document of this element is not valid for pointer lock'), a pre-existing, separately-tracked harness limitation (mutable click-prompt-stuck-rootcause / .gm/mutables.yml) unrelated to mapspinner. Manual planet.frame() calls with controlled high-altitude eye/target (bypassing the stuck player camera) prove the render pipeline itself works (see above) but get overwritten by the game's own real per-frame rAF loop before a screenshot can capture them, since that loop keeps re-rendering the real (stuck-in-lobby) player camera every frame. Conclusion: the terrain/sky renderer is not the cause of any 'landscape not visible' symptom at the GPU/buffer level; if the user saw a blank or wrong view, the most likely real-world cause is the same click-prompt/pointer-lock-stuck condition (or the small, geometrically dense spawn lobby) rather than a mapspinner WebGPU defect. Re-scoped: no code change made to patch-grid-render.js/sky-render.js/planet-orchestrator-webgpu.js since none of the evidence gathered points at a defect there.
