---
key: mem-daf58d3d9f2f04c7-2547
ns: default
created: 1790086310176
updated: 1790086310176
---

## Resolved mutable: mut-1790085488796

WHAT SERVES 3000: exec_js netstat+Get-CimInstance shows PID 14160 = node.exe server.js LISTENING on 0.0.0.0:3000 (started 15:56:01), sibling PID 15116 = same binary/args on :8099 (started 10:15:50) -- both raw `node server.js`, both served via src/sdk/StaticHandler.js. curl -I to both /app.js gave byte-identical ETag 1a0c8abc69a.f74/Content-Length 157209 -- port 3000 and 8099 serve the IDENTICAL dist/client/app.js from disk. STALENESS: dist/client/app.js mtime=08:22:21, predates ALL 8 of today's commits (earliest 13:57:41; dacd45ca 14:29:33 = the exact 'reliefScale never threaded, ~1000x-too-large heights' symptom class; e4899dc7 14:53; fcdddfa2 15:26). Confirmed by content: app.js (minified/chunked) lacks '_fwdYSigned' (pitch fix) and '_ensureInstanceBuffer' (instance-buffer fix). RENDERER DEFAULT: client/app.js:168 _webgpuOptIn requires literal ?webgpu=1 -- default is WebGL2, matching 'the way it used to be'. LIVE REPRO (gm browser verb, real Chromium, localhost:3000/?singleplayer&world=tps-game, WebGLRenderer confirmed): camera settled [-15.35,3.60,-13.90]; real THREE.Raycaster cast down through the live scene hit terrain mesh at y=2.6-2.8, within ~1m of eye height -- normal, no mismatch. Also tested ?webgpu=1 on same port: screenshot .gm/witness/port3000_webgpu_default.png shows correctly-scaled continuous terrain, no hard-clip/spike -- because packages/mapspinner/src/webgpu/*.js (the real composeHeight/reliefScale fix) is served UNBUNDLED live via the /node_modules/mapspinner/ symlink regardless of stale app.js; its own reliefScale default-fallback covers the stale bundled call-site gap. CONCLUSION: bundle staleness is real (confirmed by mtime + absent fix-strings) but does NOT currently reproduce 'land too high' on port 3000 in either renderer at the real default entry point -- not a live bug, no code fix needed, distinct from the still-pending full-globe vegetation-parity mutables. Attempted npm run build:client as best-effort hygiene: FAILED on a pre-existing unrelated break (client/core/VegImpostorTier.js imports streaming-gltf/octahedral-impostor-display-tsl, not in that package's exports map -- this session's own concurrent uncommitted TSL-port work per git_status: Grass.js/GrassTSL.js/Weather.js/WeatherMaterialsTSL.js/WebGPUInstancing.js/water-render.js). Left unfixed as out of scope/another session's in-flight work; flagged not silently bypassed. Servers need no restart once rebuilt (StaticHandler serves live from disk, no-cache + network-first SW).
