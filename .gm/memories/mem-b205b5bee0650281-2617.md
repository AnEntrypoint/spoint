---
key: mem-b205b5bee0650281-2617
ns: default
created: 1790086074721
updated: 1790086074721
---

## Resolved mutable: mut-1790081577310

RE-VERIFIED fresh at the exact frozen pose (pos [-15.372218837779094,11.5050810843717,-13.997920055344787], quat [-0.000008423315782903722,0.9999915444363446,-0.003032039182050657,-0.0027780790594282637]), controlling for the confound my first pass missed (weather/time-of-day differ per session start): forced window.__weather.setType('clear')/setIntensity(0) and window.__timeOfDayApi.setFraction(0.4) (elevationDeg=54.2) identically on both WebGL and WebGPU before screenshotting (.gm/witness/webgl_pose_t04.png, webgpu_pose_t04.png). RELIEF: now closely matches -- same dock structure, same rolling-hills silhouette/rock placement on both, confirming the stale-buffer fix (e4899dc7d4) resolved candidate (1) (LOD/split-factor divergence). SKY HALO (candidate 2): STILL DIVERGES -- WebGL shows a soft diffuse zenith glow; WebGPU shows a distinctly harder-edged, larger white-to-blue arc cut off at the top of frame; not fixed this pass, root cause not isolated (sky-render.js WGSL atmosphere port vs atmosphere.glsl). WATER (candidate 3): STILL INVISIBLE under WebGPU at this pose. Investigated root cause: (a) found and FIXED a real bug -- packages/mapspinner/src/webgpu/water-render.js's _ensureInstanceBuffer had the identical stale-buffer skip-guard class already fixed in patch-grid-render.js (e4899dc7d4) but never applied to water-render.js; removed the `if (quads === this._quadsRef) return` early-return so water's instance buffer now rebuilds every frame like terrain's. Live-verified instanceCount changed 135->144 after the fix (previously frozen). (b) Despite the fix, water remains invisible. Ruled out via live tests: vertex-position formula is algebraically IDENTICAL to GL's (terrain.glsl:354-376 hR=(swell-0.5)*0.8*oceanAmp vs water-render.js:90 hR=(swell-0.5)*0.8*oceanParams.oceanAmp, same vRel construction); culling is not the cause (temporarily forced mainStateKey to 'water-blended-cull-off', reverted after, no visual change); CPU ground-truth sampling across the visible frustum at this exact pose (window.__terrain.heightAt via unprojected NDC rays) confirms real underwater terrain IS in view (heights down to -288m, well below sea level, at multiple screen positions including the WebGL water-horizon location). Root cause narrowed to the WebGPU depth-test/pipeline-state path in water-render.js's 'water-blended-cull-front' pipeline (depthCompare/format wiring) rather than geometry/math -- filed as a new narrower mutable (webgpu-water-invisible-despite-correct-vertex-math-and-fresh-instances) for the remaining depth-pipeline investigation.
