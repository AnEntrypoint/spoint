---
key: mem-48184a47628c0772-1533
ns: default
created: 1790075330366
updated: 1790075330366
---

## Resolved mutable: mut-1790073649291

Re-derived from real source, not assumption. packages/mapspinner/src/webgpu/patch-grid-render.js vs gl-render.js: both compute vRel = (dir0-camDir)*defRadius + dir0*h - camDir*camAlt (patch-grid-render.js:67,160). This is algebraically exact, not an approximation: camDir=eyeW/camDist, camAlt=camDist-R (planet-orchestrator-webgpu.js:185-186), so camDir*(R+camAlt)=eyeW exactly by construction, making vRel = worldPoint - eyeW exactly, matching PlanetFrame.js's localToWorld convention (recall project/planetframe-exact-render-inverse) since eyeW = frame.localToWorld(p.x,p.y,p.z) (client/core/TerrainBackdrop.js:163) feeds camWorldPos identically for both WebGL and WebGPU paths (same TerrainBackdrop.renderPlanet call site, not per-backend). faceFrame cube-face basis matrices are byte-identical across terrain.glsl:280-288, gl-render.js _faceFrames:246-249, and patch-grid-render.js WGSL faceFrame:24-31/105-112 (all 6 faces, all 9 components each, verified by direct comparison). Live GPU readback (gm cdp verb, real tps-game world, real D3D12/Dawn WebGPU device) of mainColorTex via copyTextureToBuffer at a controlled real camera pose (eye=up*(R+8000), tgt=eye+east*4000-up*1500) shows spatially-coherent, non-degenerate terrain-green (8,24,10 / 18,52,22 / 21,60,26) and sky colors filling the full 1297x812 offscreen buffer edge-to-edge (20x20 grid sample, quadCount=90-144 across multiple real poses), not a degenerate/collapsed/misplaced pattern. World-space construction is correct.
