---
key: mem-7a62a2aa2c8138d2-347
ns: default
created: 1789391628227
updated: 1789391628227
---

project/mapspinner-splat-uv-camera-relative: terrain.glsl splat UV = (vTexRel + uTexCamFrac)/uTexTileM; vTexRel is the VS camera-relative vRel, uTexCamFrac = camWorld mod uTexTileM computed in fp64 on the CPU (gl-render). Never build texture UVs from absolute vWorld (~6.4e6 m quantizes to ~0.4 m in fp32) -> texture UVs jump every frame up close.
