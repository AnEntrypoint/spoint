---
key: mem-ec91e2be183638ae-1502
ns: default
created: 1789455892200
updated: 1789455892200
---

src/client/PredictionEngine.js predict()/resimulate() integrate position with zero collision awareness (state.position[i]+=velocity[i]*dt, no geometry). While a player holds movement into real Jolt-resolved static geometry (server position frozen, velocity nonzero -- TickHandler.js processPlayerMovement deliberately restores pre-collision wished velocity for broadcast), every resimulate() replay of unacked buffered inputs re-advances localState.position through the wall by velocity*tickDuration per replayed tick, producing a periodic exact-repeat divergence in getLocalState().position at server-correction cadence (measured live: 0.1094m/tick on near-zero-latency loopback at v=7,60Hz; unbounded under any ack/backlog slowdown since PredictionEngine.INPUT_HISTORY_SOFT_CAP only evicts already-ACKED entries, never unacked ones -- this is the grass-vegfocus-periodic-snapshot-jitter PRD row's confirmed mechanism, only reproduces when predictionEnabled=true i.e. URL has ?predict; client/app.js:789 defaults it OFF). Fixed by tracking `horizontallyWedged` in onServerSnapshot (compare consecutive authoritative X/Z positions: onGround && posDeltaSq<1e-8 && velSq>1e-6 => wedged) and gating predict()'s horizontal position integration on it (Y/gravity/jump/crouch untouched). Live-verified: 620 consecutive wedged samples went from ~7% divergent (dz=+0.11 or +0.22) to zero outliers, bit-identical position every sample; unwedging/re-wedging at a different wall re-engages correctly, no permalock.
