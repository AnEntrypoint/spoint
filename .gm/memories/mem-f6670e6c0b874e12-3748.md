---
key: mem-f6670e6c0b874e12-3748
ns: default
created: 1790090482948
updated: 1790090482948
---

## Resolved mutable: webgpu-texture-array-atlas-runtime-mip-lod-selection-vs-webgl

HYPOTHESIS FALSIFIED by structural source read + spec + live exec_js, not a guess. SW unregistered + caches cleared + confirmed empty this session (browser dispatch witness_marked:18, swCount:0, cachesLeft:0) against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (real WebGPURenderer confirmed via console log). (1) packages/streaming-gltf/src/texture-array-atlasTSL.js:14-15: material.colorNode=texture(arrayTexture,uv()).depth(int(layerIndex)) -- layerIndex is attribute('layerIndex','float'). Read node_modules/three/src/nodes/accessors/TextureNode.js:575-606,495-535: with no explicit level/bias/grad, generate() falls to builder.generateTexture(texture,textureProperty,uvSnippet,depthSnippet,offsetSnippet) (automatic-LOD path). node_modules/three/src/renderers/webgpu/nodes/WGSLNodeBuilder.js:343-374 _generateTextureSample emits `textureSample( tex, tex_sampler, uvSnippet, depthSnippet )` -- depthSnippet (i32 array index) is a SEPARATE positional WGSL argument, structurally outside the `coords` argument that undergoes implicit derivative-based LOD computation; this exact form was independently confirmed live-compiled in a prior session's captured shader (mem-daee40b6bfe90fb1-4947): `textureSample( nodeUniform0, nodeUniform0_sampler, nodeVarying7, i32( nodeVarying8 ) )`. (2) WebGL side is NOT the same TSL graph recompiled to GLSL -- packages/streaming-gltf/src/texture-array-atlas.js:110 is a hand-written onBeforeCompile GLSL shader: `texture( uArrayMap, vec3( vMapUv, vLayerIndex ) )`. Per GLSL/GLSL-ES spec, the array-layer (last) component of the P argument to a sampler-array texture() call is excluded from the implicit derivative/LOD calculation (only the spatial .xy participates) -- conformant on every WebGL2 driver, so no cross-backend asymmetry exists here either. (3) Decisive live witness: exec_js dispatch (dispatch output exit_code:0) actually imported and ran the real packages/streaming-gltf/src/texture-array-atlas.js tagGeometryLayer(geo,41) against a real THREE.PlaneGeometry(16 verts): result {vertexCount:16,attrCount:16,min:41,max:41,uniform:true} -- layerIndex is filled with an IDENTICAL scalar across every vertex of a mesh's geometry by construction (new Float32Array(count).fill(layerIndex)), so its screen-space interpolant is provably constant (dFdx/dFdy==0) within any single primitive on EITHER backend, regardless of API-level derivative-exclusion rules. Three independent falsifications (WGSL argument structure, GLSL/GL spec exclusion, live-executed constant-fill invariant) all agree: the layer-index cannot corrupt automatic mip/LOD selection on either backend. This was a real, well-known footgun class in general, but it does not apply to this codebase's specific construction. A live WebGPU-vs-WebGL screenshot gradient-energy A/B at the exact pinned worst-pixel pose was attempted this session (SW/cache-clear done, WebGPU scene loaded and confirmed) but the camera-pin repro from mem-daee40b6bfe90fb1-4947 did not reliably reach the settled spawn pose this run (app.cam.setMode/update sequencing flaked, y stayed at the pre-spawn default 1.25 instead of settling ~11.5) -- not pursued further since the source+live-exec evidence above is already conclusive and orthogonal to camera framing. No fix applied; nothing broken found in this narrower mechanism. This closes the mip/LOD-selection branch of the webgpu-texture-array-atlas-* investigation chain; the WebGPU-only dither/hatch artifact's remaining unexplained root cause (if the artifact is even still live given layer-41 baked source-art grain already explains most of the magnitude) has no further named candidate from this session.
