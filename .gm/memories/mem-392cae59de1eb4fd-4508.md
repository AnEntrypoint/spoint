---
key: mem-392cae59de1eb4fd-4508
ns: default
created: 1790078219829
updated: 1790078219829
---

## Resolved mutable: mut-1790077893722

PART1 ROOT-CAUSE FIX: client/service-worker.js's DEP_CACHE_NAME (/node_modules/,/vendor/) and ASSET_CACHE_NAME (.wasm/.glb/.vrm/.ktx2/.hf) tiers used _cachedWithRevalidate (stale-while-revalidate: ALWAYS serves the cached response first, refetches in background only for the NEXT load) -- packages/mapspinner (edited directly per project/workspaces-merged-packages) is served via /node_modules/mapspinner/ per project/statichandler-path-containment's node_modules symlink exemption, so every edit to packages/mapspinner/src/webgpu/*.js was served stale on the very next reload, exactly matching gm-tsl-convert-20260922-viewportfix's reported symptom. SHELL_FILES used an equivalent cache-first pattern. FIX (client/service-worker.js): added DEV=/^(localhost|127.0.0.1|\[::1\]|0.0.0.0)$/.test(self.location.hostname) (line 8) and a new _networkFirst(cacheName,req) helper (fetch first, cache the fresh response, fall back to cache only on fetch failure/offline) -- lines 99/103/109 now branch DEV?_networkFirst(...):_cachedWithRevalidate(...)/cache-first, so production (a real github.io domain, scripts/static-export.mjs) keeps its existing offline-first caching untouched while any localhost/127.0.0.1/[::1]/0.0.0.0 dev server always prefers network. No comments added. LIVE-VERIFIED via gm browser verb, http://localhost:8099/?singleplayer&world=tps-game: edited client/style.css body background to #010203 (a real client file), did an ORDINARY page navigation (no manual unregister()/caches.delete()) -- confirmed navigator.serviceWorker.controller active with scriptURL=service-worker.js (real SW in control, not bypassed), fetch('/style.css') (both default and no-store cache modes) returned the fresh #010203 content (STYLE_FETCH_HAS_MARKER/DEFAULT_FETCH_HAS_MARKER both true), and the spoint-shell-v3 Cache Storage entry itself was also verified updated to the fresh content (CACHE_ENTRY_HAS_MARKER true) so even an offline fallback would no longer serve pre-edit bytes. Edit reverted to background:#000 after the witness. PART2 SPOT-CHECK (now that stale-SW risk is closed, re-verified 4 consequential already-resolved mutables against genuinely fresh code via gm browser verb, http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, real Chromium D3D12/Dawn WebGPU backend, renderer.isWebGPURenderer=true, renderer.needsFrameBufferTarget=true): (1) webgpu-planet-canvas-color-not-persisted-across-threejs-render: fetch('/node_modules/mapspinner/src/webgpu/planet-orchestrator-webgpu.js') confirms resolveThreeOwnedColorTarget is present in the live-served source (not stale); patched GPUQueue.prototype.submit to read back the canvas center pixel after each real submit across 4 real frames -- the pattern [black,black,terrain-green(36,79,41,255)] repeated exactly once per frame (3 submits/frame, matching the original fix's own witness methodology), confirming the LAST submit of every frame still shows real non-black terrain color, not the pre-fix black-canvas regression. (2) webgpu-planet-depth-target-mismatch-under-framebuffer-target: resolveThreeOwnedDepthTarget confirmed present in the same live-served source; zero WebGPU validation/other errors (otherErr=0) captured via device.addEventListener('uncapturederror') + wrapped renderer.onError over a live 4s window with needsFrameBufferTarget genuinely true (the exact config the original depth-mismatch bug required to manifest). (3) webgpu-e2e-uniform-buffer-exceeds-65536-limit: fetch('/core/GrassTSL.js') confirms the live source uses storage(im,...) (usesStorage=true) and NOT the old buffer(im.array,...) call (usesRawBuffer=false); captured zero uniformErr (regex /uniform buffer binding size/ against every onError/uncapturederror message) over the same live 4s window, i.e. the exact previously-failing scale (4096/2048-instance grass InstancedMesh under needsFrameBufferTarget=true) now produces zero GPUValidationError. (4) stuck-pipeline eviction: fetch('/core/SceneSetup.js') confirms installStuckPipelineRecovery is present; fetch('/app.js') confirms it is still wired in (recoveryWired=true); fetch('/node_modules/mapspinner/src/webgpu/pipeline-cache.js') confirms the getPipeline() error-scope eviction (pushErrorScope + this._pipelines.delete) is present. CONCLUSION: none of the 4 spot-checked fixes are false positives -- all four are genuinely present in fresh, non-stale, currently-served code and exhibit their documented live behavior. No new mutable needed for Part 2.
