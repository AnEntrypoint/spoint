---
key: mem-699d7a01510fa747-3062
ns: default
created: 1790084670914
updated: 1790084670914
---

## Resolved mutable: mut-1790083912630

Re-witnessed premise, not assumed the same bug. (1) codesearch confirms vRel formula and camAlt sign are IDENTICAL between GL and WebGPU and untouched by the fwd.y pitch fix: terrain.glsl:376 vRel=(dir0-defCamDir)*defRadius+dir0*(hR-skirt)-defCamDir*defCamAlt vs patch-grid-render.js:68/161 vRel=(dir0-frame.camDir)*defRadius+dir0*h-frame.camDir*frame.camAlt (byte-identical structure); camAlt=camDist-R in both gl-render.js:1416 and planet-orchestrator-webgpu.js:186 (same sign, no inversion). client/core/TerrainBackdrop.js:163-173 shows eyeW=frame.localToWorld(p) feeds camWorldPos/camAlt/camDir identically for both renderers -- the isWebGPU-gated fwd.y fix (line 165) only affects fE/_tgt (view ROTATION via viewRel=M4.lookAt), never eyeW/camAlt/camDir (pure translation/position terms), so the two axes are structurally independent in the source. (2) LIVE altitude-only test via gm browser verb against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (SW unregistered/0 cached keys confirmed, real Chromium/D3D WebGPU device isWebGPURenderer:true): added temporary window.__campose publish in planet-orchestrator-webgpu.js frame() (reverted after, git_diff on the file now clean), used window.__app.cam.setMode('fixed') + direct camera.position/quaternion API (window.__app.camera) to hold orientation (quaternion) BIT-IDENTICAL (quatMatch:true every sample) while varying ONLY camera.position.y (local-up, confirmed 1:1 with frame.up per _ensureWorldToLocalM4 basis) across dy=[0,100,500,900,300,1500,6000]. Captured REAL runtime camAlt tracked local-Y delta EXACTLY 1:1 (52.2->152.2->552.2->952.2m for dy 0/100/500/900, and 52.2->352.2->1552.2->6052.2 for dy 0/300/1500/6000) -- correct sign, no inversion possible in the position/altitude term. Real center-screen pixel readback (canvas 2D drawImage+getImageData, not a mock) at fixed pitch -10.2deg showed a clean MONOTONIC recede: warm terrain color (192,159,118) at alt=52m/dist=290m -> progressively hazier aerial-perspective gray-blue (209,222,224) at alt=552m/dist=3069m -> (207,221,224) at alt=952m/dist=5292m, i.e. terrain visibly recedes/hazes away as altitude increases, the textbook-correct non-inverted behavior. At alt>~1020m (dy=1500,6000) the same fixed -10.2deg ray correctly stops intersecting the sphere at all (horizon dip angle arccos(R/(R+h)) exceeds 10.2deg beyond h~1020m for R=63600m) and the canvas legitimately shows nothing/black there -- matches the trigonometric prediction exactly, confirming correct spherical-camera geometry, not a rendering defect. CONCLUSION: no separate camAlt/altitude-inversion bug exists; the user's 'camera flies up, terrain goes down' report is the SAME already-fixed pitch/view-rotation bug (commit fcdddfa24b, TerrainBackdrop.js isWebGPU-gated fwd.y sign) observed via a stale pre-fix session or the natural pitch/altitude correlation of flight controls, not a distinct camAlt term defect. No code change made (temporary window.__campose diagnostic added and fully reverted, git_diff clean).
