---
key: mem-0cd7728153028ab0-2305
ns: default
created: 1790075448236
updated: 1790075448236
---

## Resolved mutable: mut-1790073652993

Rigorously disproven, not assumed. (1) patch-grid-render.js's perspectiveZeroToOne (lines 240-248) matches node_modules/three/src/math/Matrix4.js makePerspective's WebGPUCoordinateSystem branch EXACTLY: c=-far/(far-near)=far*nf, d=-far*near/(far-near)=far*near*nf, identical to te[10]/te[14] in Matrix4.js:1171-1174 -- no Y-flip exists anywhere in THREE's own WebGPU coordinate-system perspective construction (checked node_modules/three/src/math/Matrix4.js:1147-1191 in full), so patch-grid-render.js needs none either and has none. (2) M4.lookAt (gl-render-mat4.js) is byte-identical to gl-render.js's own cullMatrix lookAt call (same function, shared file) -- not a separate hand-rolled construction diverging between WebGL/WebGPU. (3) sky-render.js's computeCamRotCols (sky-render.js:240-246) returns viewRel's ROWS (not columns despite the name), which is exactly transpose(viewRotation) applied via column-combination in the WGSL fragment shader (sky-render.js:191, viewRay = c0*dirView.x+c1*dirView.y+c2*dirView.z) -- algebraically verified this equals camToWorld*dirView (camToWorld = transpose(view) for a pure rotation), the mathematically correct world-space ray reconstruction. (4) Definitive live numeric test via gm cdp verb against real tps-game frame.east/north/up basis vectors (window.__terrain.frame): computed east x north = (0.640989,-0.255773,-0.723679) = -up, i.e. this world's basis satisfies north x east = +up (opposite of textbook ENU east x north = up -- an internal labeling convention, not a bug). Built two real camera poses through the exact M4.lookAt+perspectiveZeroToOne code path: pose A forward=east (reference point ndcX=3.16e-8, correctly centered), pose B forward=east+0.3*north (a rotation that is NEGATIVE about the up axis given this basis's chirality, i.e. a rightward camera turn) gave ndcX=-0.268 (point shifted LEFT). This exactly matches the theoretically-derived correct behavior for a standard right-handed lookAt (right=fwd x up, verified against THREE's own default-camera convention where forward=-Z,up=+Y gives right=+X): a rightward/negative-about-up turn must shift a previously-centered point to the LEFT on screen. Symbolic derivation and live numeric result agree exactly -- camera rotation is NOT flipped.
