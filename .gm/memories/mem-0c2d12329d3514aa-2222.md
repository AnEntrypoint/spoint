---
key: mem-0c2d12329d3514aa-2222
ns: default
created: 1790056342666
updated: 1790056342666
---

client/core/UnderwaterTintTSL.js and client/core/WetnessTintTSL.js port UnderwaterTint.js/WetnessTint.js's global THREE.ShaderChunk.fog_* patches to TSL by composing onto material.outputNode (mix/multiply .rgb, preserve .a), falling back to vec4(material.colorNode,material.opacityNode) if outputNode is unset — call AFTER colorNode/opacityNode are assigned, since the helper overwrites outputNode. Shared uniform() objects (seaUniform packs seaY/radius/enabled at x/y/z exactly like original's spointSea vec4; seaShiftUniform mirrors spointSeaShift) replace raw Float32Array + global ShaderChunk/UniformsLib patching — TSL materials just reference the same JS uniform-node object, no global-patch/adoption step needed. WetnessTint's per-light specular boost (spoint_wetSpec, originally in lights_fragment_begin's RE_Direct loop) is exposed as an exact-math standalone Fn (wetSpecularContribution) but is NOT wired into any light loop — that needs a TSL LightingModel.direct() override, same risk class as CascadeShadowSelect.js's flagged non-trivial light-loop port; applyWetnessTintNode defaults specSumNode to float(0.0) (no-op) until that lands as its own PRD row. Verification method worth reusing for future TSL ports in this project: node_modules/three's REAL GLSL->TSL transpiler at three/examples/jsm/transpiler/{GLSLDecoder,TSLEncoder,Transpiler}.js (three/addons/transpiler/* resolves there via package.json exports — 'three/addons/transpiler/X.js' import specifiers fail with ERR_MODULE_NOT_FOUND against node_modules directly; use the examples/jsm path, or import via the 'three/addons/...' bare specifier through a bundler/resolver that honors package.json exports) — feed it the exact original GLSL as a standalone function via `new Transpiler(new GLSLDecoder(), new TSLEncoder()).parse(glslSource)`, cross-check its emitted TSL against any hand-derived port before trusting the hand derivation; also confirms exact three/tsl export names/availability for the pinned version (checked here via a live `import('three/tsl')` key-presence probe, all needed symbols present in three@0.185.1: Fn, uniform, positionWorld, cameraPosition, mat3, modelWorldMatrix, normalLocal, sqrt, pow, smoothstep, etc.).
