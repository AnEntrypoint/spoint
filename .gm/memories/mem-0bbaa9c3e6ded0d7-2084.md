---
key: mem-0bbaa9c3e6ded0d7-2084
ns: default
created: 1789451062417
updated: 1789451062417
---

## Resolved mutable: grass-churn-postcondition-verdict

NON-REPRODUCIBLE as originally reported: two independent real-WASD sessions (55s KeyW held, 55s KeyD held; no __tpOverride anywhere) against a live server, sampling ctx.res.vegFocus's actual px/pz via a monkeypatch on grass.update() (the real consumer, client/core/RenderGraph.nodes.js foliage-lod-sync) yielded 2830+3388 samples. cCx=round(px/32)/cCz=round(pz/32) NEVER left the single chunk the player occupied in either run (all samples in {(0,0)} then {(-1,0)}) -- zero non-adjacent chunk-index jumps, confirming the original report's (4,-6) vs (0,0) jump (a 100+m divergence) does not occur under real input and remains best explained as a window.__tpOverride/teleport testing artifact (mesh vs authoritative-state divergence), not a real gameplay bug. SEPARATE, SMALLER real finding (not the reported symptom, not chunk-crossing): while WEDGED against level geometry (velocity nonzero, position frozen by real collision), vegSamples show a strictly periodic (~257ms cadence, e.g. 328927.94/329185.52/329442.21/329699.06/329956.71) exact-repeat divergence of a fixed alternate position (dx~0.3m, dz~4.3m from the resting position) interleaved with the resting value. Traced candidate mechanism: src/client/BaseClient.js getLocalState() returns this._snapProc.getPlayerState()/pred.localState (same object as pm.playerStates.get(lid), confirmed identical by reference in this session); src/client/PredictionEngine.js onServerSnapshot->applyCorrection+resimulate() is synchronous so no direct race is possible there, but the periodic fixed-value repeat matches the singleplayer snapshot/keyframe delivery cadence (~4Hz) rather than the 64Hz tick rate. Never crossed a chunk boundary in this test (both values round to the same chunk, CH=32 >> observed ~4.3m divergence), so it does not reproduce grass-chunk-churn-flicker and is out of this row's scope to fix -- filed as a new PRD row (grass-vegfocus-periodic-snapshot-jitter) for a dedicated future investigation, per the no-orphaned-findings rule, rather than fixed here.
