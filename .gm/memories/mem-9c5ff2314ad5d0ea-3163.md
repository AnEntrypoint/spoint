---
key: mem-9c5ff2314ad5d0ea-3163
ns: default
created: 1790081033268
updated: 1790081033268
---

## Resolved mutable: mut-1790075551154

RE-VERIFIED 2026-09-22 in a fresh session (gm-tsl-convert-20260922-dithercheck), same setup as sibling mutable mut-1790073755079 (SW unregistered + caches cleared, confirmed empty; fresh WebGPU load post both a7068d9f and dacd45ca fixes; real Chromium D3D12/Dawn backend). Part (1) of this mutable, 'a dense diagonal dither/hatch pattern covers ALL visible wall/floor geometry', is CONFIRMED STILL PRESENT: fresh screenshots .gm/witness/dithercheck-cam2.png and dithercheck-combined.png show it immediately (tick 686-2104, healthy FPS 53-61), at the SAME lobby/rock-wall location described in the original screenshots. NEW decisive evidence this pass adds: A/B comparison at the identical camera pose confirms the pattern is WebGPU-renderer-specific (absent under plain WebGL, .gm/witness/dithercheck-webgl-final.png, same URL minus webgpu=1) -- so this is not fixed by, and not caused by, the reliefScale terrain-height fix (dacd45ca), since the pattern also covers non-terrain GLB model geometry (entity_4_model.progressive.glb), not just mapspinner terrain. Console stayed clean throughout (no DXC-compile-fail or uniform-buffer-overflow errors captured), ruling out this project's two already-tracked WebGPU pipeline bugs (webgpu-e2e-meshbasicnodematerial-433-dxc-permanent-fail: flat magenta signature, not a match; webgpu-e2e-uniform-buffer-exceeds-65536-limit: would throw a captured validation error, none seen) as the direct cause of what's visible here. Part (2), the two floating black-bordered picture-frame panels with wrong sky/pylon/magenta-noise content, was NOT re-encountered this session: pointer lock fails under the gm browser/CDP harness (pre-existing, separately-tracked limitation), so free-cam exploration via app.cam.setPosition/setTarget stayed confined to the immediate spawn structure and never reached the world location the original panels were screenshotted at. Investigated where such a panel COULD be wired: scene.traverse(app.scene) across 209 meshes found zero raycast hits on the visible wall geometry from the camera's forward ray, and enumerated only 8 unique textures bound to any THREE.Material, all plain HTMLImageElement sources (isRenderTargetTexture/isFramebufferTexture/isCanvasTexture/isVideoTexture all false on every one) -- no THREE.Material anywhere in app.scene is bound to a mapspinner render-target texture. This means IF the panels are still real, they are drawn by mapspinner's own WebGPU passes directly onto the canvas (packages/mapspinner/src/webgpu/sky-render.js, vdrs-composite.js, patch-grid-render.js), not a wrongly-bound THREE.Material -- ruling out one entire class of 'wrong material.map' hypothesis and narrowing where a future pass should look. RE-SCOPED rather than closed: part (1) reconfirmed persistent and newly isolated to WebGPU via direct A/B; part (2) neither reproduced nor ruled out this pass. Both the confirmed hatch defect's root-cause-and-fix and a renewed, wider-ranging panel hunt are filed as new mutable webgpu-dither-hatch-distinct-from-tracked-pipeline-bugs for the next session, now with the mapspinner-direct-draw narrowing above.
