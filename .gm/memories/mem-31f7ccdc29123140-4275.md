---
key: mem-31f7ccdc29123140-4275
ns: default
created: 1790061856487
updated: 1790061856487
---

## Resolved mutable: webgpu-sky-and-vdrs-upscale-composite

Built packages/mapspinner/src/webgpu/sky-render.js (drawSky WGSL port) and packages/mapspinner/src/webgpu/vdrs-composite.js (VDRS upscale/depth-writeback port). SKY: ported atmosphere.glsl's atm_skyRadiance/atm_marchRadiance/atm_scatteringLUTSample/atm_transmittanceLUTSample chain 1:1 to WGSL as hand-written fullscreen-triangle WGSL (matching patch-grid-render.js's established raw-WGSL+MapspinnerPipelineCache convention, not TSL/QuadMesh -- checked concretely: FSR1WebGPU.js and every other mapspinner webgpu/ file are architecturally incompatible, see VDRS note below), using ptr<function,vec3<f32>> for GLSL's 'out vec3 transmittance' params and textureSampleLevel against the real transmittanceLUT/scatteringLUT textures created by atmosphere-lut-compute.js (reused verbatim, not rebuilt). Two pipeline variants (sky-depth-tested LEQUAL/no-write, sky-no-depth) both built through pipeline-cache.js's pre-existing state keys. WITNESSED live via gm browser verb against a real WebGPU device (adapter vendor=nvidia architecture=ampere, device.features has float32-filterable=true, required explicitly at requestDevice -- first run without it hit a real validation error 'sampleType Float vs UnfilterableFloat' which is the correct root-caused fix, not a workaround): baked real transmittance/scattering LUTs via atmosphere-transmittance-lut.js/atmosphere-scattering-lut.js, rendered patch-grid-render.js terrain + sky composited into one depth-tested pass (real distinct RGB gradient read back via copyTextureToBuffer, zero validation errors), and the no-depth variant into a standalone color target (also real distinct gradient, zero errors). VDRS: BilinearUpscale (ported upFsSrc 1:1) and Fsr1Upscale (EASU two-pass ported from gl-render.js's own easuFsSrc/rcasFsSrc raw GLSL, confirmed byte-for-byte equivalent math to client/core/FSR1WebGPU.js's TSL buildEasuNode/buildRcasNode -- concretely verified FSR1WebGPU.js cannot be wired in directly: it requires a THREE.WebGPURenderer/QuadMesh/MeshBasicNodeMaterial instance, whereas every mapspinner webgpu/ file operates on a raw GPUDevice/GPUCommandEncoder with zero THREE dependency per mapspinner/AGENTS.md's explicit 'ZERO three.js/WebGPURenderer/TSL' architecture decision -- so only the EASU/RCAS math was reused, ported to WGSL text, not the class) both live-witnessed upscaling a real 64x64 source to 256x256 with distinct sampled pixels and zero validation errors. DepthWriteback and UpscaleDepthWriteback port gl-render.js's dwFsSrc/udwFsSrc depth-linearize-then-reproject formula, correctly adapted for WebGPU's zero-to-one NDC convention (the original GLSL assumes OpenGL's [-1,1] NDC and does zNdc*2-1 before the eye-space inverse; WebGPU's NDC z is already [0,1] so that remap is dropped and wgpuZeroToOneEyeDepth/wgpuZeroToOneNdcDepth instead invert perspectiveZeroToOne's own m10=far/(near-far), m14=far*near/(near-far) terms, algebraically verified at the zNdc=0->near and zNdc=1->far boundary conditions by hand before coding) via @builtin(frag_depth); texture_depth_2d requires an integer (not float) mip level for textureSampleLevel (a real WGSL compile error caught live and fixed) and a non-filtering sampler distinct from the filtering sampler used for the paired color texture in UpscaleDepthWriteback (a real WebGPU bind-group validation error caught live and fixed by splitting into colorSampler+depthSampler bindings). Final live run (synthetic single-triangle low-res source with per-vertex depth 0.05/0.9/0.45 to decouple the depth-math witness from patch-grid-render.js camera framing): DepthWriteback readback min=0.2551 max=0.8949 belowClearFrac=1 (real non-default, non-uniform, correctly clamped [0,1] depth values written via frag_depth from a real texture_depth_2d sample, not a placeholder); UpscaleDepthWriteback readback showed the SAME min/max depth range plus three distinct real color samples (145,153,59)/(102,153,102)/(53,153,151) confirming the combined color+depth pass. Zero WebGPU validation errors on final run (device.popErrorScope() returned null). Files: packages/mapspinner/src/webgpu/sky-render.js, packages/mapspinner/src/webgpu/vdrs-composite.js. Neither gl-render.js nor any water-pass file was touched.
