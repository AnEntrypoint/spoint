---
key: mem-d7d2017f8ecc8e35-5307
ns: default
created: 1790071972819
updated: 1790071972819
---

## Resolved mutable: tsl-vegetation-impostor-billboard-tier-webgpu-port

Built packages/streaming-gltf/src/octahedral-impostor-display-tsl.js (makeOctahedralImpostorDisplayMaterialTSL): a MeshStandardNodeMaterial TSL port of octahedral-impostor-ez.js's createOctahedralImpostorMaterial display-side onBeforeCompile shader (IMPOSTOR_PARAMS_VERTEX/IMPOSTOR_VERTEX/IMPOSTOR_PARAMS_FRAGMENT/IMPOSTOR_MAP_FRAGMENT/IMPOSTOR_NORMAL_FRAGMENT_BEGIN from octahedral-impostor-shaders.js, read in full and hand-ported formula-for-formula: encodeDirection/decodeDirection/computePlaneBasis/projectToPlaneUV/computeSpritesWeight non-hemi octahedral math, 3-sprite barycentric-style blend with real If/ElseIf/Else branching matching the original's per-primary-sprite alpha discard, EZ_ATLAS_TILE mega-atlas indexing via a real InstancedBufferAttribute('atlasTile'), EZ_FAR_SINGLE_SPRITE optimization, EZ_PARALLAX depth-offset UV via the normalDepth atlas alpha channel). instanceMatrixNodeFor(object) (buffer(im.array,'mat4').element(instanceIndex)) reused from the established GrassTSL.js/PlayerLODTSL.js pattern to manually replicate transformedInstanceMatrix=instanceMatrix*impostorTransform and its inverse for cameraPosLocal, bypassing THREE's automatic instancedMesh() node (confirmed via node_modules/three/src/materials/nodes/NodeMaterial.js:796-806 that positionNode fully overwrites positionLocal after the automatic pass, so no double-transform). Near-cutoff dissolve-fade (VegImpostorTier.js's onBeforeCompile-injected dither-discard, IMPOSTOR_DISSOLVE_FADE_BAND_M) ported inline as a real fragment Discard() using screenCoordinate.xy for the dither hash -- TSL NodeMaterial has no onBeforeCompile hook (confirmed by the sibling resolved mutable tsl-impostor-wrap-arbitrary-material-needs-nodematerial), so this is built into the material itself via nearCutoff/fadeBandM params rather than layered on after. Lighting: confirmed via node_modules/three/src/nodes/accessors/Normal.js:99-119 that material.normalNode, when read from the FRAGMENT stage (not NORMAL/VERTEX subbuild), becomes normalView AS-IS with no further normalMatrix transform -- matching the original's raw override of normal_fragment_begin/normal_fragment_maps -- so the atlas-decoded normal (unpacked via *2-1, matching packNormalToRGB(x)=x*0.5+0.5 confirmed at node_modules/three/src/nodes/utils/Packing.js:13) is transformed world-to-view via mat3(cameraViewMatrix) and assigned directly to material.normalNode, consistent with the already-shipped octahedral-impostor-shaders-tsl.js atlas-capture material which bakes normalWorld (not the original's view-space-of-capture-camera convention). Billboard geometry: confirmed InstancedMesh2 (used by the existing WebGL path) is structurally WebGLRenderer-only (sibling resolved mutable tsl-grass-instancedmesh2-webgl-only), so VegImpostorTier.js's createSharedImpostorMesh now branches via isWebGPUInstancingSupported(renderer) to a new createSharedImpostorMeshWebGPU() using WebGPUInstancing.js's createWebGPUInstancedMesh (the same plain-THREE.InstancedMesh adapter already proven for Grass.js/Vegetation.js) with an 'atlasTile' float InstancedBufferAttribute in place of InstancedMesh2's per-instance uniform + addLOD geometry swap (the near-cutoff visual behavior is now carried entirely by the material's own fragment discard, verified equivalent live). client/index.html, client/editor/thebird-host.html, scripts/bundle-client.mjs importmaps updated with the new 'streaming-gltf/octahedral-impostor-display-tsl' bare specifier alongside the existing octahedral-impostor-ez entry. LIVE-WITNESSED via gm browser verb against a real WebGPU device (Chrome, navigator.gpu, WebGPURenderer.init() succeeded, zero WebGPU validation errors via device.pushErrorScope/popErrorScope): (1) real angle-dependent atlas sampling -- front/side/top camera views of the same billboard produced three genuinely DIFFERENT real sampled colors ([215,17,255],[255,217,255],[217,217,255]), not solid/blank, confirming real octahedral projection selects different atlas cells per viewing direction; (2) atlas tiling -- an isolated tileBase=vec2(mod(atlasTile,gridSide),floor(atlasTile/gridSide)) probe against the real production formula gave tileBase=(0,0) for atlasTile=0 and (1,0) for atlasTile=1 (colors (0,0,0,255) vs (255,0,0,255)), and the full material with NearestFilter + farSingleSprite:true correctly differentiated tile0=[214,214,214] vs tile1=[243,243,243] at the same camera pose; (3) near-cutoff dissolve-fade -- camera distance well inside (nearCutoff-fadeBand-2) rendered fully discarded (nonBlack:0 across the whole canvas), camera at/beyond the cutoff+fadeBand rendered fully opaque with real atlas color, confirmed both on the standalone material AND on the REAL production client/core/VegImpostorTier.js createSharedImpostorMesh(renderer,...) function called directly (not a reimplementation): shared.mesh.isInstancedMesh===true, tooClose nonBlack=0, beyondCutoff nonBlack=1156 with real sampled color, far (dist 30) nonBlack=324. Per-instance InstancedBufferAttribute reads verified correct in isolation (0.7 vs 0.3 attribute values produced distinct sRGB-encoded output 218 vs 149, matching pow(x,1/2.4)*255 gamma encoding exactly). No comments added to either file; no test files created.
