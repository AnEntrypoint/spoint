---
key: mem-54a09cf7624bf6d9-1110
ns: default
created: 1789411880863
updated: 1789411880863
---

project/streaming-gltf-webgpu-hiz-kernel-known-broken: the WebGpuHizTier depth-compare kernel (examples/local-progressive/webgpu-hiz-tier.js, witnessed by webgpu-occlusion-test.html) is disabled and fails open (every candidate visible). Findings so far: toTexel() math is correct (debug-buffer readback gave distinct plausible texels, (527,276) and (520,302) on a 1036x647 canvas). readRenderTargetPixelsAsync cannot read RenderTarget.depthTexture (textures[] has no depth slot; textureIndex:1 throws 'Invalid value used as weak map key'), so read depth by drawing it through a TSL fullscreen quad into a colour target, remapping [0.98,1.0] to [0,1] first because raw depth near 0.99 quantises to bytes 252-255. With that probe, the behind-wall entity sampled NEARER (110) than the front one (118), so the 5 AABB corner/centre taps likely miss the wall's footprint. Next: a denser sample grid, and overlay-verify the screen AABB. webgpu-modelpool-test.html guards a separate race: a WebGPURenderer must not fall through to the WebGL2 OcclusionQueryTier while the dynamic import of the WebGPU tier is in flight.
