---
key: mem-0048ae8abfe638db-4625
ns: default
created: 1790083486637
updated: 1790083486637
---

## Resolved mutable: mut-1790080715682

ROOT CAUSE FOUND AND FIXED, not a threshold/band-aid. Live-verified with real interactive multi-angle pitch testing via gm cdp/browser verb against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (SW unregistered + caches cleared this session, confirmed navigator.serviceWorker.controller===false and 0 cached keys before first test; real Chromium/D3D/WebGPU device, isWebGPURenderer:true; pitch driven directly via window.__app.cam.setVRPitch(v) -- the same player-look pitch API real mouse input feeds, since real requestPointerLock is unavailable in the CDP harness). BEFORE FIX: pitch=+0.52 (30deg up, confirmed via camera.matrixWorld-derived fwdLocal.y=sin(pitch) exactly, independent of any terrain code) showed ~98% mapspinner terrain filling the screen with only a hairline sky sliver at the TOP edge (.gm/witness/pitch_up30.png); pitch=-0.52 (30deg down) showed mostly non-terrain geometry with terrain reduced to a sliver (.gm/witness/pitch_down30.png) -- backwards from FPS convention. A clean SMALL-ANGLE control test (pitch 0 -> 0.15, no topology confound) moved the sky/terrain horizon from screen row y=380 to y=280 (.gm/witness/fine_pitch_0.png vs fine_pitch_015.png), i.e. UP-screen by ~100px for an 8.6deg pitch-up input; at fovy=1.2217rad over height=809px that predicts a ~99px shift, a near-exact magnitude match proving a clean scaled inversion, not terrain-topology noise. Independently proved via real captured runtime matrices (console-logged from inside the actual collectQuads() call in packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js, not a re-derivation): projecting the EXACT pitch=0 fwd direction through the EXACT real pitch=0.15 viewProjNoEye matrix gives ndcY=-0.216 (this fixed world direction should render BELOW screen-center on pitch-up, i.e. more sky) -- the JS-side M4.lookAt/perspectiveZeroToOne math in patch-grid-render.js and planet-orchestrator-webgpu.js is itself CORRECT (matches THREE's own WebGPUCoordinateSystem convention) and is NOT the bug. The actual bug: client/core/TerrainBackdrop.js's renderPlanet() builds the mapspinner-global camera-target direction as fE = east*fwd.x + up*fwd.y + north*fwd.z from THREE's real camera.getWorldDirection(), shared verbatim by both the WebGL and WebGPU init paths -- but WebGPU's patch-grid-render.js consumes this fE-derived matrix DIRECTLY as the real per-vertex clip transform (out.pos = frame.viewProjNoEye * vec4(vRel,1.0) in RENDER_WGSL vs_main), whereas WebGL's actual draw call renders through THREE's own authoritative camera.matrixWorldInverse-derived matrix and only reuses the shared fE/M4.lookAt construction for CPU-side frustum culling -- so the same latent fwd.y sign mismatch was already present for both renderers, but only WebGPU's direct real-vertex-transform consumption turns it into a full visible pitch inversion, matching the user's own observation that only the WebGPU terrain view showed the symptom. FIX (client/core/TerrainBackdrop.js renderPlanet(), isWebGPU-gated, WebGL path untouched): const _fwdYSigned = isWebGPU ? -_fwd.y : _fwd.y; fE built from _fwdYSigned instead of _fwd.y directly. AFTER FIX, re-verified live with the SAME multi-angle test: pitch=0 baseline unchanged/correct (.gm/witness/fix_test_pitch0_b.png); pitch=+0.15 now correctly pushes the horizon DOWN-screen from y~380 to y~540, revealing more sky (.gm/witness/fix_test_pitch015.png); pitch=+1.3 (near-max look-up) now shows ~95% sky/haze with only wall-tops at frame edges, a complete reversal of the pre-fix 98%-ground result (.gm/witness/fix_test_pitchup_extreme.png); pitch=-0.9 correctly shows floor filling most of the screen (.gm/witness/fix_test_pitchdown.png); yaw re-verified unaffected (setVRYaw(0.6) at pitch=0 pans horizontally, sky/ground ratio unchanged, .gm/witness/fix_test_yaw.png), confirming the fix is pitch-axis-isolated and does not regress the already-resolved yaw correctness. Two diagnostic hypotheses were tested live and disproven before landing this fix: (1) the (q.level|0)>=2 unconditional-include threshold in collectQuads' frustum-cull loop -- temporarily forced to >=0, bug persisted identically (.gm/witness/testcull_pitchup2.png), reverted; (2) a full-screen Y-flip in vdrs-composite.js's blit -- ruled out because pitch=0 already renders correctly, which a blit-level flip would also break. All temporary debug instrumentation (console logging in collectQuads, a WGSL fragment-shader debug-color override) was used to gather evidence then fully reverted; only the one isWebGPU-gated line in TerrainBackdrop.js remains changed.
