---
key: mem-965498ed2af33812-403
ns: default
created: 1789392284717
updated: 1789392284717
---

project/component-pool-plain-arrays-epoch-f64: apps/_lib/ComponentPool.js uses plain fixed-length typed arrays + copy-on-grow; resizable-ArrayBuffer length-tracking views measured ~2.5x slower (950 vs 379ms, 20k-elem loop). grow() swaps column identity, so callers cache column() and re-fetch on pool.epoch change (health.js/steering.js _sync). Date.now() fields need 'f64': f32 truncates ms by seconds.
