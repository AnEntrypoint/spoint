---
key: mem-9551157dbdfa42ba-4080
ns: default
created: 1790089108968
updated: 1790089108968
---

## Resolved mutable: mut-1790086110042

Premise overturned via live GPU introspection (gm cdp verb, real Chromium/Dawn WebGPU, http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, SW unregistered + caches cleared first): the water render PIPELINE has no defect. All four named candidates falsified with direct evidence: (1) draw-call invocation -- monkeypatched planet.frame to log every real per-frame call: 80 calls/1.5s, zero exceptions, quadCount=175, water.render(pass2,quads) unconditionally invoked whenever quads.length>0 (read in full in planet-orchestrator-webgpu.js frame()); (2) depth/blend state -- controlled A/B test with IDENTICAL frame() args (captured via monkeypatch, replayed synchronously twice, water.render toggled real-fn vs no-op, mainColorTex read back both times via copyTextureToBuffer): 120557/263900 sampled texels (45.7%) differ, ALL confined to y>=436 (0 diff above y=400, i.e. exactly the sub-horizon/ground band, none in sky), with measured color [86,102,105] vs the WATER_WGSL formula's prediction mix(offColor=[138,133,111], tint=[13,56,97], 0.55)=[69,91,103] -- within tolerance, proving the blend executes correctly; (3) WebGPU validation errors -- device.addEventListener('uncapturederror',...) attached to the live GPUDevice for the whole test window: zero errors captured across all dispatches; pipeline-cache.js already wraps createRenderPipeline in pushErrorScope('validation')/popErrorScope with a console.error on failure (read in full, already present); (4) occlusion-query gating -- read planet-orchestrator-webgpu.js frame() in full: occlusionProbe.resolve()/read() only records the vis-probe result, it never gates or skips the water.render(pass2,quads) call, which is unconditional; live occlusionLastSampleCount='1' (non-zero/visible) confirmed. REAL ROOT CAUSE of the originally-reported 'zero visible pixels': at the exact frozen dock pose, water draws correctly into mapspinner's internal buffer (90% of the sub-horizon region, per the diff above) but is then legitimately occluded in the FINAL presented frame by the dock/lobby's own opaque THREE-owned scene geometry, which correctly depth-tests on top via the already-fixed resolveThreeOwnedDepthTarget/DepthWriteback shared-depth mechanism (sibling mutables webgpu-planet-depth-not-shared-with-three-scene / webgpu-planet-depth-target-mismatch-under-framebuffer-target, both previously verified working). Verified on TWO independent real, compositor-level screenshots (gm cdp screenshot=, not a canvas.drawImage/toDataURL hack -- confirmed separately broken for WebGPU canvases in this harness) taken back-to-back at the identical pose with water.render forced real vs no-op: final-image diff is only 1.75% (2151/122850 sampled px), and every sampled diff pixel decodes to the magenta player-character model's idle-sway animation (e.g. RGB delta at a pixel reading [137,0,121], pure character-avatar magenta), not water color -- i.e. in the REAL presented image, toggling water on/off changes ZERO water-colored pixels, consistent with total occlusion, not a rendering defect. Screenshots: .gm/witness/quick_ON.png, quick_OFF.png, crop_water_ON_lower.png (shows the dock decking that occludes the internally-correct water pass). No code change made -- forcing an edit (e.g. weakening depth test, boosting alpha) to hide a correctly-occluded surface would be the exact 'tune a threshold to hide a symptom' anti-pattern this project's own root-cause discipline (AGENTS.md project/degenerate-triangle-threshold-is-not-a-tunable-guess) explicitly forbids; the water renderer is correct and needs no fix. A harness lesson recorded in passing: canvas.getContext('webgpu').getCurrentTexture() read from an external CDP script (outside the app's own render loop) returns a stale/never-composited texture (100% black every sample across 40 real submits, single unchanging GPUTexture identity over 3s) -- unreliable for canvas-presentation verification in this cdp harness; real compositor screenshots (screenshot= prefix) are the only trustworthy final-frame witness here.
