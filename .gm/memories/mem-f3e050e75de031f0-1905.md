---
key: mem-f3e050e75de031f0-1905
ns: default
created: 1790068357418
updated: 1790068357418
---

## Resolved mutable: mut-1790056425817

This row's own stated bar ('must be measured VS/FS-cost-split against the existing WebGL2 THC baseline before landing, not just made to run', scoped to the height-bake PASS/PORT itself: ensureBake/drawBakeTile/bakeTileReadback/bakeTileToLayer vs height-bake-compute.js) is discharged by sibling mutable webgpu-thc-bake-perf-vs-webgl2-thc (mut-1790059481818, status: witnessed, .gm/mutables.yml:1768-1777), whose own subject text explicitly states it was filed 'per webgpu-thc-height-bake-pass's own stated landing bar' to satisfy exactly this row. Its witnessed measurement (real GPU, Chrome, TIME_ELAPSED_EXT / GPUComputePassDescriptor.timestampWrites, N=64 tiles, THC_BAKE_RES 130x130): WebGL2 production bake (drawBakeTile+PBO readback) medianMs=0.7107; WebGPU compute-pass-only GPU time medianMs=0.0655 (~10.8x faster); WebGPU per-tile synchronous mapAsync wall-clock medianMs=4.03 (~5.7x slower than WebGL2's blocking path). This is the VS/FS-cost-split-equivalent measurement against the WebGL2 baseline this row asked for, correctly scoped to bake production (the row's own named functions), not to main-render sampling. Distinct from and NOT satisfied by this resolution: webgpu-thc-sample-consumption-path-untested (still pending, filed by the same sibling session as a SEPARATE concern -- the original 2026-06-15 regression was about FS cost from SAMPLING the baked pool during the main deck render, downstream of and architecturally separate from the bake-production pass this row covers; patch-grid-render.js does not yet sample any height pool). Per mut-1790059481818's own recommendation, this row's bar is met ONLY for adoption via the GPU-resident pool-layer pattern (mirroring bakeTileToLayer, zero per-tile CPU readback) -- a naive synchronous per-tile mapAsync readback usage remains measured net-negative and is not licensed by this resolution.
