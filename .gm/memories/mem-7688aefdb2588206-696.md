---
key: mem-7688aefdb2588206-696
ns: default
created: 1789417511949
updated: 1789417511949
---

window.__tpOverride = [x,y,z] is AUTHORITATIVE (world) space since 1bd2cc50; client/app.js converts it with floatingOrigin.toRender every frame. It moves only the local player mesh, not physics/network state. Same commit fixed the floating-origin-rebase node: it fed camera.position (render coords) into floatingOrigin.update, which expects authoritative input (rx = input - shift; shift.set(input)); any jump past REBASE_THRESHOLD_M 8000 then thrashed every frame (sim: 599 rebases/600 frames holding 9500) and continuous travel missed the second rebase. Now fed toAuthoritative(camera.position). Witness driver: headed off-screen Chrome over CDP, count window.__floatingOrigin.getRebaseCount().
