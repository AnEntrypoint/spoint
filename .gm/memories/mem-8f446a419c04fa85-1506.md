---
key: mem-8f446a419c04fa85-1506
ns: default
created: 1789414822451
updated: 1789414822451
---

project/ground-depth-cut-is-underwater-ceiling-waterline-crossing: 'ground depth map height changes with camera, occluding from the ground up' (2026-08-21b) was mapspinner's _uw underwater branch in packages/mapspinner/src/gl-render.js water pass: below the water sphere (camDist < R - 2) the two-sided underside writes color+depth and drawSky loses, a 1 m boolean snap the TPS pitch=0 camera crosses routinely. The local-Y waterline is the TRUE sphere drop R - sqrt(R^2-r^2) (measured 2026-09-14: rendered water within 1 cm to 6 km; the earlier sagitta fix was 0.64 m high at 6 km) -- every consumer goes through PlanetFrame.waterlineLocalY / spointSeaSurfaceY (project/planetframe-exact-render-inverse). Second regime: at grazing altitude (eye within ~2 m of the water sphere) the camera-relative VS projection flips the water winding so a hardcoded cullFace(FRONT) culled the ocean -> black horizon band; fixed with _waterCullFront = !_uw && !(Math.abs(camAlt)<5.0). Tooling: window.__tpOverride teleport (client/app.js, RENDER coords -- thrashes rebases past 8 km), window.__passProbe frame-stage captures, __cullMode='none' A/B, window.__cam.oceanAmplitude=0 removes swell for surface measurements. Water-only A/B: freeze time by wrapping __app.terrain.renderPlanet with a fixed elapsedSec and set the camera pose inside that wrapper. Method: out-of-band renderer.render captures wipe the raw-GL backdrop and fake a black-void artifact; init-time-only knobs (geomorphLod) need a pre-boot default flip.
