---
key: mem-327ab572fd7ce83d-2816
ns: default
created: 1790070027064
updated: 1790070027064
---

## Resolved mutable: tsl-vegetation-multi-lod-shadowlod-impostor-parity

Live gm-browser witness against real tps-game ?webgpu=1 (real navigator.gpu device, real THREE.WebGPURenderer, window.__veg.totalInstances=658). (1) LOD tier swap: Oak Large's real WebGPUInstancedMesh tier geometries have distinct triangle counts [2146,597,530] (D0/D1/D2). Directly invoked the real production window.__veg.update(dt,camera,playerPos,shadowStill) against a real instance (id=0) by monkeypatching camera.getWorldPosition to a controlled position, then reading the real instanceMatrix Float32Array of each tier mesh (rec.branch.tierMeshes[i].instanceMatrix.array) to determine which tier is non-zero for that id -- this is the actual GPU-uploaded instance buffer, not a mock: distance 5m -> tier0 (2146 tri), 20m -> tier1 (597 tri), 60m -> tier2 (530 tri), back to 5m -> tier0 again (hysteresis round-trips correctly, not stuck). (2) Shadow-only tier: at 5m and 20m shadowActive=true (inShadow Map + shadowMesh instance buffer non-zero), at 60m shadowActive=false, matching SHADOW_CAST=35 threshold in Vegetation.js. Mechanistic shadow-pass proof: monkeypatched shadowMesh.onBeforeShadow to count invocations, forced the scene's 2 DirectionalLights castShadow=true (restored after), called real renderer.render(scene,camera) twice -- onBeforeShadow fired exactly 2/2 times and shadowMesh.count was correctly set to 1 (the single near instance) each time, proving THREE's real WebGPU shadow render pass picks up and would draw vegetation's shadow-tier geometry when shadow casting is active. Found and fixed a real bug during this witnessing: client/core/VegetationBuild.js simplifyGeo's sloppy branch passed MeshoptSimplifier.simplifySloppy 5 positional args ([idx,pos,3,target,0.05]) against its real 6-arg signature (indices,vertex_positions,stride,vertex_lock,target_index_count,target_error) -- target landed in vertex_lock, failed simplifySloppy's own assert(vertex_lock==null||instanceof Uint8Array), threw, and the catch silently returned the geometry UNCHANGED. This defeated the coarsest LOD tier (b2) and the entire shadow geometry (b2shadow) for every species on both WebGL and WebGPU (shared code) -- pre-fix tier tris were [2146,597,2146] (tier2==tier0, unsimplified) and shadow tris were 2146 (unsimplified). Fixed client/core/VegetationBuild.js line ~102 to pass null for vertex_lock; post-fix tris [2146,597,530] and shadow=530, verified live as above. Scope narrowed off impostor/billboard tier (split to tsl-vegetation-impostor-billboard-tier-webgpu-port, unimplemented, separate large TSL-port scope never attempted). Also found scene-wide shadow-casting is currently off under ?webgpu=1 for an unrelated reason (filed webgpu-shadow-casting-globally-off-pipeline-breaks-on-enable) -- not a vegetation defect.
