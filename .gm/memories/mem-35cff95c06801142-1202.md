---
key: mem-35cff95c06801142-1202
ns: default
created: 1789391358277
updated: 1789391358277
---

project/degenerate-triangle-third-copy-and-immutable-cache: aim_sillos.glb degenerate-triangle fix (EPS_AREA=1e-4, derived from a measured gap in the mesh's triangle-area histogram: largest defect 7.26e-5 m^2 vs smallest real tri 1.12e-4 m^2) had to land in THREE independent copies of the same check: src/physics/ShapeBuilder.js (physics loader), packages/streaming-gltf/tools/bake-cluster.mjs (bake), and packages/streaming-gltf/src/cluster-lod-mesh.js (client runtime combine). A src/sdk/StaticHandler.js Cache-Control: immutable with no ETag on the re-bakeable asset route kept serving stale pre-fix geometry to already-visited tabs. Lessons: when a fix correct at its own layer fails end-to-end, grep the whole pipeline for another copy of the check before retuning numbers; every re-bakeable (non content-hashed) URL needs a real ETag/conditional GET; verify rendering defects on live GPU-bound geometry (window.__scene traversal of geometry.index/position), not a re-parse of bake output. Structural alternatives tried and rejected with evidence: spatial vertex welding, meshoptimizer simplifyPrune/simplify, NullGraph MeshletBuilder (no EXT_meshopt_compression support), nanite-webgpu pipeline.
