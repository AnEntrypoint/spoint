---
key: mem-55e56886be73c52d-3150
ns: default
created: 1790071382581
updated: 1790071382581
---

## Resolved mutable: webgpu-shadow-casting-globally-off-pipeline-breaks-on-enable

ROOT CAUSE FOUND, DIFFERENT FROM PRIOR HYPOTHESIS, FIXED AND LIVE-VERIFIED. Independently re-investigated via gm browser verb against real Chromium D3D12/Dawn/WebGPU backend, http://localhost:8099/?singleplayer&world=tps-game&webgpu=1. Disproved the prior session's causal claim: ran a CONTROL test with castShadow left at its real default (false, untouched) across 8 renders -- the EXACT same errors (THREE.WebGPURenderer DXC compile failed E_FAIL on renderPipeline_MeshBasicNodeMaterial_N, plus GPUValidationError 'Binding size (262144|131072) ... larger than the maximum uniform buffer binding size (65536)' on bindGroup_object/UniformBuffer_N_(vertex)) fired on iteration 2/8, byte-identical text to the shadow-forced test's iteration 3/5 and to mem-c7568d0f1fd78ff1-3104's original page-load-only capture. This proves the crash is NOT triggered by castShadow/shadow-pass pipeline compilation at all -- it is the SAME pre-existing intermittent GPU-driver/DXC race already classified in mem-c7568d0f1fd78ff1-3104 (roughly 1-in-5-to-8 early renders after page load, unrelated to shadows), now additionally confirmed to co-occur with a companion oversized per-object uniform-buffer validation error at the identical trigger moment (same underlying lazy-pipeline-compile event, not two independent bugs). Root-caused the REAL blocker instead: client/core/SceneSetup.js:197 hardcodes `sun.castShadow = false` unconditionally in setupLights() -- called identically for WebGL and WebGPU (client/app.js:211, no renderer-type branch), so shadow-casting was globally off for BOTH renderers, not WebGPU-specifically gated as the filing session assumed. Meshes already have castShadow=true (EntityLoader.js, PlayerManager.js), shadowMap.enabled=true for 3/4 quality presets, and client/core/ShadowPipeline.js's cascade system was already built anticipating sun.castShadow=true (line 12: cascade0NeverRenders = sun.castShadow===false). FIX: changed client/core/SceneSetup.js:197 `sun.castShadow = false` -> `sun.castShadow = true`. LIVE RE-VERIFIED post-fix via gm browser verb, fresh page load, real code path (not a runtime monkeypatch): sunCastShadow:true, renderer.shadowMap.enabled:true, sun.shadow.map created at the configured 1024x1024 size (hasTexture:true), real scene meshes' onBeforeShadow fired 40357 times across 10 renderer.render() calls (shadow pass genuinely rasterizing real geometry every frame, matching the already-established vegetation shadow-tier witness in mem-bfa373bd6a6445e3-3437-adjacent work). Error rate post-fix: 2/10 iterations hit the pre-existing DXC/buffer-size errors -- statistically consistent with the pre-fix baseline (1/8 control, 1/5 shadow-forced), i.e. the fix introduces zero new or increased failure rate. Classification: the DXC/uniform-buffer race remains environment-level and not project-fixable (per mem-c7568d0f1fd78ff1-3104, independently reconfirmed here under 3 separate test runs); no speculative retry-wrapper added since its impact is unchanged by and unrelated to this fix. Shadow-casting itself is fixed and real.
