---
key: mem-fbc4387e6b5c2659-1139
ns: default
created: 1789456663118
updated: 1789456663118
---

## Resolved mutable: sec-sweep-wedge-gate-cannot-mask-server-authority

exec_js live run: established wedgedBefore=true via a stale-consistent snapshot, then delivered a legitimate server teleport/respawn snapshot (position jumping to [999,5,999], onGround false, velocity zero -- a real authoritative state change unrelated to wall-collision). Result: wedgedAfterTeleport=false (the large XZ delta correctly cleared the wedge flag on its own dx^2+dz^2 test, no special-casing needed) and localStateFollowedTeleport=true (localState.position exactly matches the server's authoritative teleport target). Confirms the gate cannot trap a player's rendered position away from server authority: reconciliationEngine.applyCorrection() always writes localState.position directly from server data BEFORE resimulate() ever runs, and the wedge flag only ever gates the replay of buffered inputs on top of that already-correct base, never the correction itself. No wire/trust-boundary exposure: horizontallyWedged is derived entirely from server-sent snapshot data and consumed only by the local client's own rendering integrator, never transmitted.
