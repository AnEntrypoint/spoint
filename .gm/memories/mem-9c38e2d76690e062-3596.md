---
key: mem-9c38e2d76690e062-3596
ns: default
created: 1790090961466
updated: 1790090961466
---

## Resolved mutable: mut-1790088466240

PREMISE RE-WITNESSED AND FALSIFIED, not assumed correct from the prior session. (1) STRUCTURAL: read terrain.glsl:58-159 (h3/snoise3/value_fbm/rotate_domain/value_ridged_fbm_rot/eval_layer/sample_fractal_terrain/fractalTerrainH, noiseLayer0/1/2 consts) side by side with packages/mapspinner/src/webgpu/terrain-composeheight-wgsl.js:2-106 and height-gen.js:11-126 (CPU transpile) term-by-term: identical octave counts (10/18/18), identical constants (0.1031/0.1030/0.0973 hash, 33.33, warpStr 1.6, ridgeOffset 1.064, ridgeExp 1.005/1.1, hmin/hmax -2/2, 0.17/0.6/0.53/123-456-789/0.95-1.3/0.8/0.3-0.7-1.0/4.0), identical uNoUnroll no-op at default 64 (FXC_UNROLL_DEFEAT_LOOP_BOUND=64, gl-render.js:15,630). No structural difference anywhere. (2) LIVE NUMERIC, direct fractalTerrainH-only (bypassing composeHeight entirely): dispatched the exact WGSL fractalTerrainH via a real WebGPU compute pipeline (gm browser verb, real Chromium adapter/device, SW unregistered+caches cleared) at the 6 face-centers+2 diagonals, vs the CPU height-gen.js fractalTerrainH (fround-per-op oracle) called directly at the same directions. Max abs diff 0.0048 (diag2), typical 0.0001-0.0014 -- fp32-rounding-class, matches the ALREADY-RESOLVED sibling mutable mut-1790056440331's finding that this exact function has expected bounded chaotic sensitivity (that session measured real-GPU-vs-CPU diffs up to 0.2036 at the pole and called it expected/non-defect); this session's 8-direction sweep is smaller still. Scaled to world units (*750000*reliefScale) the WORST fractalTerrainH-only diff is ~3.6m, nowhere near the reported 91-149m. (3) LIVE NUMERIC, full composeHeight with IDENTICAL fixed params on both sides (landBias=-100000 [the REAL default -- the prior session's claim of landBias=0 was never verified against HEIGHT_UNIFORM_DEFAULTS and was wrong], beachShelfM=150, reliefScale=0.001, cbias=0): GPU compute vs CPU height-gen.js composeHeight at the same 8 directions -- diffs ranged -4.50 to +1.31m, MIXED SIGN, not the reported systematic always-positive 91-149m. (4) ROOT CAUSE OF THE PRIOR SESSION'S SPURIOUS FINDING: reproduced its exact isolation method (CPU full composeHeight output minus continentalBias(dir)*50*reliefScale, a LINEAR subtraction) against the TRUE cbias=0 composeHeight (computed by actually zeroing the hpf source, running the real nonlinear pipeline) -- the two disagree by up to 28.5m (diag2) purely because composeHeight is NONLINEAR (beach-shelf quadratic and the h<0 *1.25/-350000 clamp) in the very-negative regime that landBias=-100000 forces every one of these directions into, so subtracting a linear cbias contribution post-hoc from a nonlinearly-clamped result is invalid and manufactures a fake residual. CONCLUSION: terrain-composeheight-wgsl.js's fractalTerrainH and composeHeight are correct 1:1 ports with no code defect; no fix applied (none needed). The reported +91-149m GPU-always-higher figure does not reproduce under controlled parameter-matched live testing and was an artifact of the prior session's invalid linear-subtraction isolation method, not a genuine backend divergence. Filed narrower follow-up mutable (webgpu-fullglobe-sweep-91to149m-source-unaccounted, since 28.5m of isolation-math error + <5m of real fp32 noise does not fully account for the originally reported 91-149m) to re-audit the ACTUAL live GroundProbe parameter wiring (scalarA/scalarB uniform values actually reaching the GPU probe vs the CPU sampler in that original session) as the remaining candidate, rather than fractalTerrainH itself.
