---
key: mem-4907795ee8136d3a-4987
ns: default
created: 1790062874363
updated: 1790062874363
---

## Resolved mutable: webgpu-water-pass-draw

Built packages/mapspinner/src/webgpu/water-render.js: WaterRenderer (vis-probe pipeline via 'water-vis-probe' state key + main blended draw via 'water-blended-cull-front' state key, both from pipeline-cache.js's existing STATE_DESCRIPTORS), WaterOcclusionProbe (real GPUQuerySet type:'occlusion', begin/endOcclusionQuery wrapped around the vis-probe draw inside the SAME render pass as terrain via occlusionQuerySet in the pass descriptor, resolveQuerySet->copyBufferToBuffer->mapAsync 2-buffer ping-pong staging ring mirroring ground-probe-compute.js's async readback pattern), createSceneCopyTexture/captureSceneCopy (real GPUCommandEncoder.copyTextureToTexture, API verified against node_modules/three/src/renderers/webgpu/WebGPUBackend.js:2709/2803's own real usage before writing, not guessed). Water height/displacement formula verified against gl-render.js's ACTUAL water vertex shader branch (terrain.glsl lines 352-369, uIsWater>0.5 path): NOT UnderwaterTintTSL.js's spointSeaSurfaceY (that is client/core's separate TPS-game three.js integration reading PlanetFrame.js/waterlineLocalY -- codesearch confirmed spointSeaSurfaceY has zero references inside packages/mapspinner; mapspinner is a self-contained SDK with its own terrain.glsl water branch). Real formula ported verbatim: seaHash/seaNoise/seaOctave (terrain.glsl:228-245) feeding the swell displacement hR = (seaOctave(swellP*0.016+d0*swellTime.x,oceanChoppy)+seaOctave(...+d1*swellTime.y,oceanChoppy))*0.5 giving (swell-0.5)*0.8*oceanAmp (terrain.glsl:354-366), NOT composeHeight (which only supplies vH/climate shading in the GLSL, not the actual water surface displacement). Exported createBufferWithData from patch-grid-render.js for reuse (added to its export line) rather than duplicating. Scope boundary named precisely: water-underwater (state 'water-underwater'), water-depth-share-cull-front/off, and the half-res-water (_hrw) FBO downscale+upscale composite chain (gl-render.js:1592-1681) are explicitly OUT OF SCOPE here -- they are late-pipeline composite steps sharing infrastructure (VDRS half-res target, passUpscaleToCanvas) with sky/depth-writeback, and belong to the already-filed sibling mutable webgpu-sky-and-vdrs-upscale-composite, not duplicated here. LIVE-VERIFIED via gm browser verb against a real GPU (Chrome, adapter vendor=nvidia architecture=ampere, real navigator.gpu device): full pipeline ran to completion (steps: device-ok, terrain-frame-set, water-renderer-ready, pass1-done [terrain draw + vis-probe occlusion query in one pass], scene-copy-recorded, pass2-done [blended water draw], submitted) rendering 64 real instanced terrain+water patches (8x8 grid, face 4, 62500m patches, same synthetic quads as the already-verified patch-grid-render.js witness) sharing the SAME frameUniformBuffer/composeHeight defRadius as the terrain renderer. OCCLUSION QUERY: real non-zero sample-count data read back via mapAsync -> BigUint64Array, occlusionSampleCount='1' (genuine GPU-reported samples-passed count, not merely 'did not throw' -- the WebGpuHizTier precedent, project/streaming-gltf-webgpu-hiz-kernel-known-broken, is a real known-broken-occlusion class in this codebase, so this was verified against real returned data specifically to avoid that failure mode). PIXEL WITNESS: readback via copyTextureToBuffer+mapAsync (256-byte-padded bytesPerRow), classified a 32x32 grid of sample points into bg/water/terrain by real RGBA values, found a genuine adjacent terrain/water boundary pair at grid cells [15,15]/[16,15]: terrainPx=[50,47,41,255] (opaque tan, matches the terrain fragment shader's highColor mix vec3(0.55,0.5,0.45)) vs waterPx=[24,66,55,207] (blended, alpha=207 exactly matching the predicted blend math 0.75*0.75+1.0*0.25=0.8125*255=207.2 for BLEND_SRC_ALPHA_OVER composited over opaque dst, confirming genuine alpha-blend execution, not a placeholder fill) -- real color AND alpha distinction at the boundary as requested. 48/1024 grid samples classified water, 1/1024 terrain (camera framing at alt=2,000,000m over an 8x8 62500m patch grid puts most terrain patches off the coarse 32x32 sample grid; a finer/lower-altitude resample would find more terrain samples but the single confirmed adjacent boundary pair with real distinct RGBA is sufficient positive evidence, not a placeholder). Tooling note (real friction encountered, not a task blocker): the gm `browser` verb's eval-completion-value capture (`result` field) never populated across 4 attempts regardless of ending shape (bare await, IIFE, bare promise-returning call) -- console.log output IS reliably captured, and a bare top-level `await` genuinely keeps the page alive (isolated 800ms-delay probe confirmed), so the working pattern is top-level `await runWitness()` + `console.log('RESULT:'+JSON.stringify(...))`, not return-value capture. Recorded here since it cost significant turns; a fix belongs in the gm tool itself (out of this mutable's scope).
