---
key: mem-e7a5d589d0b90af0-825
ns: default
created: 1789464923553
updated: 1789464923553
---

## Resolved mutable: decide-self-review-inputhistoryhardcap-degenerate-tickduration

Fixed: inputHistoryHardCap() now guards Number.isFinite(this.tickDuration) && this.tickDuration>0 before dividing, falling back to the same INPUT_HISTORY_SOFT_CAP+1 floor otherwise -- so the function is total against its own field regardless of who writes it, not just against the current setTickRate() gate. Re-witnessed live via exec_js: zeroTickDuration=257, negativeTickDuration=257, nanTickDuration=257, infiniteTickDuration=257 (all correctly fall back), normal60=600/normal64=640 unaffected, and a 50000-tick sustained-partition simulation with tickDuration forced to NaN stays bounded at exactly 257 entries (confirms the eviction loop actually fires correctly under the fallback, not just that the getter returns a finite number).
