---
key: mem-1226ca9615030f56-1528
ns: default
created: 1785780432944
updated: 1785780432944
---

Confirming-pass findings on the gh-pages static deploy (same 404 family, post the big demo sweep): (1) client/editor/thebird-host.html (standalone editor page) was broken 4+2 ways -- importmap values never base-prefixed (generic html sed covers href/src ATTRIBUTES only, every html with an importmap needs a dedicated sed like demo.html's), CSS+inline import pointed at the nonexistent /client/editor tree (correct mount convention is /editor/, which dev serves via the '/' client mount and gh-pages as /spoint/editor), 4 stale importmap aliases to deleted files, an importmap missing 10 entries the shared client graph needs (webjsx killed the module graph), and stale glue calling editPanel.overlay (createEditPanel self-mounts the overlay to document.body; its API returns show/hide/toggle, no .overlay). (2) ModelPool progressive-probe 404s for any shipped map GLB whose .prog wasn't baked -- the gh-pages bake step now loops dist/apps/maps/*.glb instead of baking aim_sillos only. (3) deathrun_kosova.glb carries 8 spec-legal UNDEFINED textures (no source, no extension, still material-referenced) that null-deref @gltf-transform/core's reader (setTextureInfo on null textureInfo) -- packages/streaming-gltf/tools/bake-cluster.mjs's _stripUndefinedTextures drops them + their material refs and remaps indices before io.readBinary; three.js treats them as no-map so output is visually identical. Also: a warm gm browser profile serves CACHED html -- append a ?v=<ts> cache-buster when witnessing a just-deployed html change.
