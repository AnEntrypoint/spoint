---
key: mem-69a355bd08f4d87b-3536
ns: default
created: 1790091527270
updated: 1790091527270
---

## Resolved mutable: mut-1790088896334

OBTAINED THE DEDICATED VISUAL WITNESS, VERDICT: SEAMLESS (skirt fix confirmed working). Method: SW unregistered (1) + caches cleared (4) fresh at http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (real Chromium/D3D12/Dawn, isWebGPURenderer:true). Camera control reused the resolved camera-pinning technique verbatim (setMode('tps') -> settle -> setMode('fixed') -> explicit position/lookAt). Confirmed genuinely stable via a real live delta-check: position/quaternion sampled 1000-1200ms apart across real gameplay frames read EXACT ZERO delta (posDelta:0, quatDelta:0), twice. NEW BLOCKER discovered and worked around (not the one named in the mutable's own text): the real per-frame render path (TerrainBackdrop.renderPlanet, called every frame by RenderGraph regardless of camera freeze) reconstructs its own eye/target from camera.getWorldDirection() via an east/north/up + WebGPU-Y-flip projection, and this reconstruction is BROKEN for a manually-posed camera -- every on-screen screenshot attempt via the normal path (3 attempts, including after a 3.5s settle to rule out a stale-cache/reconcile transient) showed pure sky (live GPU pixel readback of terrain.planet.mainColorTex() confirmed near-uniform sky-blue, avgRGB~[127,212,244], minmax range <35 in G/B). Filed as new mutable webgpu-terrainbackdrop-renderplanet-fwdvector-broken-for-manual-camera-pose. WORKAROUND (exactly the mutable's own named fallback: 'driving the WebGPU planet-orchestrator's frame(camWorldPos,camTarget,...) directly ... and reading back mainColorTex via copyTextureToBuffer'): computed eyeW/tgtW via frame.localToWorld() on BOTH the local eye and local target coordinates (bypassing TerrainBackdrop's broken camera-direction reconstruction), called terrain.planet.frame(eyeW,tgtW,70*pi/180,0,sunDir,t,frame.up,0) directly -- confirmed correct via live pixel variance (minRGB dropping to real green/brown terrain colors, e.g. [122,144,71], vs the broken path's near-flat sky band) -- then read back the full 1297x812 mainColorTex with copyTextureToBuffer+mapAsync, encoded client-side via Canvas2D putImageData+toDataURL('image/png'), and wrote the PNG to disk via exec_js reading the browser dispatch's own stored JSON response. TWO independent oblique/aerial poses captured, both over real land (not spawn lobby): (1) C:\dev\spoint\.gm\witness\lod-seam-manual-readback.png -- 31 deg oblique, eye ~505m local alt, target ~600m away, quadCount 159, shows rolling hills + green patches meeting a smooth horizon; (2) C:\dev\spoint\.gm\witness\lod-seam-manual-readback-close.png -- steeper close-in oblique, eye 180m above local ground, target 500m away, quadCount 191, shows fine near-camera gully/erosion detail transitioning to a smoother, coarser far ridge and horizon. VISUAL VERDICT: in both images the terrain surface is CONTINUOUS from near (fine detail, small patches per the quadtree distance-based split) through to far (coarser patches) with NO visible crack, T-junction gap, z-fighting stripe, or any other discontinuity anywhere along the slope or horizon silhouette -- confirming the skirt fix (packages/mapspinner/src/webgpu/patch-grid-render.js buildRenderWgsl/buildSampleWgsl vs_main, verbatim port of terrain.glsl:370-376) genuinely eliminates patch-boundary cracks, not merely compiles/runs cleanly as the implementing session could only claim without a screenshot. No prior session's success claim overturned -- this is a first confirming visual witness, matching the prediction.
