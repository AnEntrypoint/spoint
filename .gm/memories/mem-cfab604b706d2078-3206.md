---
key: mem-cfab604b706d2078-3206
ns: default
created: 1790067804265
updated: 1790067804265
---

## Resolved mutable: tsl-vegetation-flicker-live-witness-pending

Polygon-offset parity re-verified structurally AND live: VegetationBuild.js's buildSpecies() (client/core/VegetationBuild.js lines 122-127, shared by both WebGL and WebGPU renderer paths, called once per species before the isWebGPU branch in Vegetation.js) sets branchMat/leafMat.polygonOffset=true, polygonOffsetFactor=TERRAIN_ZFIGHT_POLYGON_OFFSET_FACTOR(-4), polygonOffsetUnits=TERRAIN_ZFIGHT_POLYGON_OFFSET_UNITS(-32) directly on the material objects at construction time, before VegetationTSL.js's applyWindTSL/applyTintTSL ever touch them (those only set positionNode/colorNode/customProgramCacheKey, confirmed by re-reading VegetationTSL.js in full -- no polygonOffset* reference anywhere in that file). Live-verified in the real tps-game ?webgpu=1 world via gm browser: window.__veg._meshes[Oak Large].branch.material and .leaf.material both report {polygonOffset:true, factor:-4, units:-32, hasPositionNode:true, hasColorNode:true} -- the fix is structurally preserved under the real running WebGPU material, not just inspected in source. Live flicker witness (motion pixel-delta over consecutive frames, per mem-a6f1d7bd7be3412c-3784's method): located a real live Oak Large tree instance's world position from window.__veg's InstancedMesh.instanceMatrix buffer, positioned window.__camera to frame it, screen-space-projected the tree position to center a 48x48 sample patch on it, then rendered 12-14 consecutive frames via renderer.render(scene,camera) with veg.tickWind(0.033) advancing real wind time each frame (continuous wind-driven vertex displacement, the exact mechanism the original bug's fix targets), capturing each frame via renderer.domElement.toDataURL()->offscreen-canvas getImageData and computing per-channel abs-diff sum between consecutive frames. Ran twice independently: run1 deltas=[25.9,317.6,149.2,162.8,66.9,25.6,29.6,0.12,0.1,0.5,0.3], run2 deltas=[66.3,463.8,117.7,143.9,74.4,56.2,41.4,0.045,0.58,0.21,0.096,0.15,0.04]. Both runs show an initial transient (frames 1-7) that I confirmed via veg.profile.loads (178->186 in run1, 195->206 in run2) is caused by the game's own independent background chunk-streaming loop loading NEW vegetation chunks into the sampled region during capture -- NOT z-fighting: the loads counter incremented exactly across that same window in both runs. After streaming settled (loads counter flat), the LAST 4-6 consecutive-frame deltas in both runs are near the noise floor (0.04-0.6, on a 0-765 max scale) and stay flat/low despite wind time continuously advancing every frame -- this is the smooth-continuous-motion signature, not the flicker signature: real z-fighting recurs every frame as depth-precision noise flips triangle sort order under continuous wind-driven vertex perturbation, which would show as repeated large swings throughout the stable window, not a settle-once-then-flat pattern. No such recurring instability was observed in either run's stable window. Conclusion: the polygonOffset fix is structurally intact under the new WebGPU/TSL material path and the live pixel-delta measurement shows no z-fight flicker signature at the tested tree instance.
