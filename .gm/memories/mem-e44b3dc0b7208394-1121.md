---
key: mem-e44b3dc0b7208394-1121
ns: default
created: 1785769318850
updated: 1785769318850
---

gh-pages.yml Patch-paths step: the /src/-rewriting seds (absolute '/src/ and mindepth-2 '../../src/) must carry ! -path "dist/node_modules/*" -- packages ship whole, so package-internal relative imports like streaming-gltf/examples/local-progressive/stress.js's '../../src/model-pool.js' resolve INSIDE the package; rewriting them to /spoint/src/ produces specifiers nothing serves. Found 2026-08-03 when the new deploy guard (step 'Guard: every /src import specifier resolves to a shipped file', runs just before Deploy to gh-pages, fails the build on any /spoint/src/... spec with no dist target or any surviving root-absolute '/src/...', naming offending files via grep -rlF) went red on exactly this artifact in run 30822763957. Same session: demo.html root-absolute href="/app.js" modulepreload, /favicon.svg, /manifest.json added to the demo-specific sed (they 404'd from the anentrypoint.github.io domain root), and src/procgen added to the Build-dist copy list (ProcgenPanel.js's static /src/procgen/{WFC,LSystem,NoiseTerrain}.js imports 404'd and killed the whole app.js module graph -- the site was fully dead).
