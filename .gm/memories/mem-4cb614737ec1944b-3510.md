---
key: mem-4cb614737ec1944b-3510
ns: default
created: 1790059151984
updated: 1790059151984
---

## Resolved mutable: mut-1790056444430

Read planet-orchestrator.js's frame() in full (lines 135-378) and gl-render.js's cullMatrix (1018-1044), render (1397-1763), plus the helper functions render() calls per-frame -- drawSky (1073-1104), passUpscaleToCanvas (1144-1183), passPlanetDepthWriteback (1193-1211), passUpscaleAndDepthWriteback (1212-1231), setFrameUniforms (1233-1336), bindFrameTextures (1339-1367), _sortLeavesFrontToBack (1371-1395) -- covering the entire unread remainder of the 1814-line file past the prior session's line-627 stop. planet-orchestrator.js: render.cullMatrix() called L153/L247/L360, render.render() called L149/L268/L347, ALL inside frame() which runs every frame from both the cache-hit branch (L147-159, still calls both) and the rebuild branch (L161-374). gl-render.js cullMatrix(cam,scalarsIn) L1018-1044: PURE CPU-side JS matrix math (M4.perspective/lookAt/mul/translate into pre-allocated scratch Float32Arrays); its ONLY WebGL touch is a property READ gl.drawingBufferWidth/Height (L1023, aspect fallback only) -- no GL state mutation, no draw call, trivially portable (swap the canvas-size read for the WebGPU context's size accessor). gl-render.js render(quads,cam,sunDir,time) L1397-1763 (~366 lines) IS the real per-frame draw-submission entry point and is saturated with raw WebGL2 calls -- this is the actual webgpu-patch-grid-draw blocker. Per frame it: resolves cullMatrix, binds/clears the VDRS-or-default framebuffer (gl.bindFramebuffer/viewport/clearColor/clear), sets GL state (enable/disable DEPTH_TEST|CULL_FACE|BLEND, cullFace, frontFace, depthFunc, depthMask), gl.useProgram(terrain-or-debug), calls setFrameUniforms() (~90 gl.uniform*/change-hashed uniform pushes: camera, composeHeight params, biome bands, shadow matrix, ocean, texture-tiling) and bindFrameTextures() (~10 gl.activeTexture/bindTexture binds: HPF pool x2, transmittance/scattering LUT, surface albedo/normal array, shadow, height-pool, scene-copy tex), rebuilds+uploads the per-quad instance buffer when dirty (gl.bindBuffer/bufferData, fed by the pure-CPU radix sort _sortLeavesFrontToBack L1371-1395, zero gl calls itself), sets 4 vertex-attrib pointers/divisors, issues gl.drawElementsInstanced (L1509) for terrain, then runs a full water sub-pipeline (occlusion query gl.beginQuery/endQuery ANY_SAMPLES_PASSED_CONSERVATIVE L1572-1574, gl.copyTexSubImage2D scene-copy L1589, half-res color FBO pass, depth-share re-draw, composite) and, when VDRS active, upscale (passUpscaleToCanvas: bilinear or FSR1 EASU+RCAS fullscreen-quad passes) + passPlanetDepthWriteback + drawSky (own program/VAO/draw, L1073-1104). Data/uniform sourcing for a WebGPU render(): camera+cullMatrix arrive from planet-orchestrator.js as plain CPU JS objects/Float32Arrays (renderer-agnostic, already portable per the cullMatrix finding above); everything downstream (all ~90 setFrameUniforms uniforms, all bindFrameTextures texture binds, the instance buffer built from quads) exists ONLY as raw gl.uniform*/gl.bindTexture/gl.bufferData calls today with no TSL/storage-buffer equivalent -- a WebGPU port needs a parallel renderTSL(quads,cam,sunDir,time) re-expressing this same uniform/texture/instance-buffer set as TSL node-material uniforms + WebGPU buffers (mirroring the 11 already-ported TSL files' pattern), not a mechanical 1:1 translation. This classification is the exact prerequisite webgpu-patch-grid-draw was blocked on; no additional blocking mutable required beyond that already-tracked row.
