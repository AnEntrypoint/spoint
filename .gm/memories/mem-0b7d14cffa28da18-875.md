---
key: mem-0b7d14cffa28da18-875
ns: default
created: 1789397884122
updated: 1789397884122
---

project/cdp-before-witness-serve-head-files: to witness BEFORE behaviour without reverting the shared tree, serve `git show HEAD:<file>` copies through CDP Fetch.fulfillRequest, and copy the real response headers so COOP/COEP stay and crossOriginIsolated holds. Two gotchas: '?' in a Fetch urlPattern is a single-char wildcard (escape it as \? or every URL on the origin matches), and Chrome's Local Network Access check blocks ws://localhost from a document fulfilled by interception (net::ERR_BLOCKED_BY_LOCAL_NETWORK_ACCESS_CHECKS). Launch that profile with --disable-features=LocalNetworkAccessChecks. The spoint server on :3013 has a live ReloadManager that hot-reloads other agents' half-written src edits (it broke ticks with a transient 'waterlineLocalY is not defined'); restart it rather than trust it after a burst of edits. Other agents hold ports 3012/3014-3016.
