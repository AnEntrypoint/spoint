---
key: mem-6a3c34f8b2d24317-3703
ns: default
created: 1790081773347
updated: 1790081773347
---

## Resolved mutable: webgpu-dither-hatch-distinct-from-tracked-pipeline-bugs

STRONG LEAD (intentional THREE anti-banding dither pass) FALSIFIED by direct source read, not a guess: node_modules/three/src/nodes/display/RenderOutputNode.js setup() (the literal fragmentNode THREE's Renderer.js:1826 '_renderOutput' quad uses, via NodeManager.js:884 getOutputNode -> .renderOutput()) performs exactly 4 steps -- clamp alpha, unpremultiplyAlpha, toneMapping(), workingToColorSpace() -- zero dither/noise application anywhere in the chain. codesearch confirmed exhaustive 0 matches for 'Dithering|ditherNode|dithering' across all 18 files in node_modules/three/src/nodes/display/ (WebGPU/TSL node pipeline). The only 'dithering' code in three@0.185.1 is the LEGACY WebGL-classic GLSL ShaderChunk (dithering_fragment.glsl.js, gated by material.dithering + WebGLProgram's #define DITHERING) -- confirmed via codesearch never reached by WebGPU/NodeMaterial rendering (zero references in webgpu/ or nodes/ trees outside that WebGL-only chunk). mapspinner's own WGSL (packages/mapspinner/src/webgpu/**) has ZERO occurrences of the string 'dither' anywhere (patch-grid-render.js's terrain color is a plain deterministic mix(lowColor,highColor,t) height gradient, no per-pixel noise term) -- the sibling memory's 'documented dithered gradient' phrasing was an inaccurate paraphrase of this plain color mix, not real dithering code. NEW THIS SESSION, live-tested and falsified: the shared-framebuffer-target resample-mismatch hypothesis (mapspinner's offscreen mainColorTex vs THREE's internal _getFrameBufferTarget() vs canvas being subtly different pixel dimensions, which would moire-alias through the final _renderOutput blit) -- live gm browser verb against real Chromium D3D12/Dawn WebGPU backend (http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, SW unregistered + caches cleared + confirmed empty at session start): renderer.domElement.width/height (1295x808), renderer._getFrameBufferTarget().width/height (1295x808), and the backend's real GPU msaaTexture/resolved-texture dimensions (via renderer.backend.get(fbTarget.texture), both 1295x808, sampleCount 4) are ALL pixel-identical -- no scale mismatch exists anywhere in the composite chain. Also live raw-GPU copyTextureToBuffer readback (device-level, bypassing PNG/screenshot compression) of the PRE-tonemap rgba16float resolved scene buffer at 5 screen regions (floor, near-floor, two walls, sky-control) shows the visible screen streaking is ANISOTROPIC and geometry-aligned (strong vertical/diagonal gradient energy, near-zero horizontal, exactly at real panel-seam/plank-edge locations; near-zero energy on flat unmarked surfaces and the procedural sky) rather than the isotropic small-amplitude signature an injected ordered-dither matrix would produce -- the artifact is baked into the actual rendered scene color pre-tonemap, not injected by the output/colorspace conversion stage. RE-SCOPED, not left open: the intentional-dither-pass lead is conclusively dead; root cause is narrowed to either (a) WebGPU node-material specular/normal-map aliasing lacking geometric-roughness widening (three's WGSL PBR lighting model may not apply the same specular-AA the WebGL path gets), or (b) an as-yet-unisolated interaction specific to affected materials, and pinning it further requires a reliable camera pose at the exact witnessed hatch location -- blocked this pass by the pre-existing, separately-tracked pointer-lock/camera-contention CDP harness limitation (app.cam.setPosition calls did not stick; the live rAF loop kept the stuck-spawn pose). New narrower mutable filed: webgpu-node-material-specular-aliasing-vs-dither-injection.
