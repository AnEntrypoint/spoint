---
key: mem-0e5472b9f5835265-1647
ns: default
created: 1790059496612
updated: 1790059496612
---

## Resolved mutable: mut-1790059467465

Live browser verb dispatch (real Chrome, navigator.gpu.requestAdapter/requestDevice succeeded, adapter features incl. float32-filterable/float32-blendable; gl.errorTotalCount 0 throughout): created both the bakeMain compute pipeline (height-bake-compute.js's TERRAIN_HEIGHTBAKE_WGSL) and the already-verified generic composeHeight compute pipeline (terrain-composeheight-wgsl.js's main), baked one real 130x130 tile (face 0 frame [0,0,-1,0,1,0,1,0,0], ox=0,oy=0,l=1e6, defRadius=6371000, hpfRes=2 zero pool, sculptActive=0) into an r32float STORAGE_BINDING texture via bakeHeightTileTexture, read it back with copyTextureToBuffer into a 256-byte-row-aligned staging buffer + mapAsync(GPUMapMode.READ) (matches readHeightTileTexture's row-depad loop), and cross-checked 8 texels (corners+center+3 arbitrary) against the generic composeHeight compute pipeline fed the SAME direction independently derived in JS via the identical texel->direction formula: maxAbsDiff=3.21875, meanAbsDiff=1.276 against height magnitudes 6e4-2e5 (relative error ~1e-5, consistent with the already-documented fp32/chaotic-fbm rounding-divergence class in mem webgpu-fp32-parity-outlier where JS-float64-then-rounded intermediate directions vs all-float32 GPU-internal directions diverge through value_ridged_fbm_rot's 18-octave w-feedback -- not a logic defect). bakeHeightsRange over all 16900 texels = [-206796.02, 272921.75], respecting composeHeight's -350000 underwater floor with no violations. Full script + result: browser dispatch in session gm-tsl-convert-20260922-thcbake, ~932ms duration_ms, ok:true, zero GL errors.
