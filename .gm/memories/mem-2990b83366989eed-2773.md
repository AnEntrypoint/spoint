---
key: mem-2990b83366989eed-2773
ns: default
created: 1790088520500
updated: 1790088520500
---

## Resolved mutable: mut-1790086119877

BOTH tooling gaps closed with real code, live-verified. (a) Collider debug bridge: src/sdk/WorkerEntry.js now handles a real 'DEBUG_COLLIDER_QUERY' worker message performing an actual _ctx.physics.raycast([x,20000,z],[0,-1,0],40000) against the real Jolt heightfield body and posting back {hit,y,bodyId,terrainHeightSource}; client/BrowserServer.js adds queryColliderHeight(x,z) (postMessage+reqId-keyed Promise resolved on 'DEBUG_COLLIDER_RESULT'); client/core/TerrainBackdrop.js exposes window.__colliderProbe(dir) which inverts the single-anchor gnomonic projection (frame.up/east/north) to local x,z and calls window.__client.queryColliderHeight. Live-verified via gm cdp verb on real Chromium WebGPU (http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, isWebGPURenderer:true): window.__colliderProbe is a real function, round-trips through the actual worker, returns real data -- e.g. for -X direction: {x:-74324.07,z:16839.04,hit:false,terrainHeightSource:'gpu-patch'} (a genuine Jolt raycast miss because the streamed heightfield only covers the region near the player, extent~510m, confirmed architectural fact not a bug). For directions >~87deg from anchorDir (-Y,-Z,+X,diag2 in the 8-direction test set) the bridge correctly reports {ok:false, reason:'... exceeds single-anchor local-frame coverage ...'} instead of silently returning wrong data -- a real architectural finding: TerrainPhysics.js's collider is built in a single fixed-anchor local tangent-plane frame (createPlanetFrame, anchorDir default [0,1,0], never re-anchored), so it structurally cannot represent world directions beyond one hemisphere from the anchor; render/CPU height sampling (dir-based, FACE_FRAME-per-quadtree-face) has no such limit. (b) COPY_SRC fix: packages/mapspinner/src/webgpu/patch-grid-render.js createComposeHeightParams's 6 buffers (paramsU, scalarA, scalarB, basis, hpfPool, sculptTex) all gained GPUBufferUsage.COPY_SRC alongside their existing UNIFORM/STORAGE|COPY_DST flags. Live-verified: a real GPU-to-GPU copyBufferToBuffer from the LIVE render's patchGrid.composeHeightParams.{hpfPool,sculptTex} into a fresh GroundProbe's buffers succeeded with zero GPUValidationError (gl.errorTotalCount:0 in every cdp dispatch this session) and zero frame corruption/pipeline-recompile-recovery event -- the previously-reported COPY_SRC-missing crash class is confirmed fixed under real COPY_SRC-flagged buffers. Both blockers closed; the full sweep this tooling was built for ran this same session (see webgpu-terrain-fullglobe-render-collider-cpu-parity-sweep's resolution) and surfaced a new, precisely-isolated real divergence, filed as mut-1790088466240 (webgpu-fractalterrainh-port-diverges-from-cpu-not-continentalbias).
