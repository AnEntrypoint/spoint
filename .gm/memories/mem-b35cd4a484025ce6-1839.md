---
key: mem-b35cd4a484025ce6-1839
ns: default
created: 1790068351999
updated: 1790068351999
---

## Resolved mutable: tsl-vegetation-instancedmesh2-attribute-hookup-unverified

Superseded/moot -- confirmed via live code read against a LATER sibling session (gm-tsl-convert-20260922-vegwire, mem-bfa373bd6a6445e3) that wired Vegetation.js's WebGPU path onto WebGPUInstancing.js, not InstancedMesh2, at all. client/core/Vegetation.js:208-212 (isWebGPU branch): calls createVegWebGPUInstancer(scene,branchGeo0,sp.branchMat,INIT_CAP,VEG_ATTRIBUTE_SCHEMA) -> WebGPUInstancing.js's createWebGPUInstancedMesh -- a plain THREE.InstancedMesh, never @three.ez/instanced-mesh's InstancedMesh2 (InstancedMesh2+initUniformsPerInstance is used ONLY in the WebGL branch, Vegetation.js:214-217, isWebGPU===false). WebGPUInstancing.js:18-28 setAttributeAt: for each attributeSchema entry (VEG_ATTRIBUTE_SCHEMA={windPhase:'float',tint:'float'}, Vegetation.js:22) creates a real THREE.InstancedBufferAttribute and calls geometry.setAttribute(name,attr) directly -- a genuine per-instance vertex buffer attribute, not an InstancedMesh2 per-instance uniform. VegetationTSL.js:14 (buildWindPositionNode) and :31 (applyTintTSL) read attribute('windPhase','float')/attribute('tint','float') via three/tsl's attribute() node, which resolves exactly this kind of real THREE.BufferAttribute -- so the plain-TSL-attribute assumption this mutable flagged is CORRECT for the real, current WebGPU call site, since that call site no longer touches InstancedMesh2 or initUniformsPerInstance at all. The mutable's original premise (VegetationTSL assumes attributes a mechanism InstancedMesh2's initUniformsPerInstance does not expose) does not apply to the architecture as it exists today. Remaining real gap in this area is tracked separately under tsl-vegetation-multi-lod-shadowlod-impostor-parity (LOD/shadow/impostor tiers), a different concern, not reopened here.
