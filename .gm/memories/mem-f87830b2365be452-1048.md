---
key: mem-f87830b2365be452-1048
ns: default
created: 1789396884205
updated: 1789396884205
---

project/streaming-gltf-lod-unload-and-bucket-identity: Asset.evictMeshLod/evictTexLod return freed bytes (via pool._untrackBytes of the exact tracked URL) or null; every eviction path (LodUnloadManager.scanForUnload, ModelPool._enforceBudget) relies on that single untrack, never subtracts descriptor bytes itself. LodUnloadManager evicts by keep priority (LOD idx 0 = coarsest fallback, needs nearest invisible entity > distanceThresholdVeryFar; finer LODs need > distanceThresholdFar), largest first, until under 0.85*budget; mesh AND texture LODs in use by visible entities are protected. Grid decimation for far LODs lives in src/grid-decimate.js shared by lod-worker.js and model-pool.js (finest-first 48..2, getX/Y/Z reads, all attributes rebuilt). Material buckets: stampMaterialBucketKeys writes the full sha1 of a generic gltf-transform attribute signature (materialKey covers TextureInfo, samplers, extensions); MaterialBucketBatcher reuses a bucket only for the same material object or equal renderSignature, else a '<key>~N' sub-bucket.
