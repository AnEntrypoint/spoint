---
key: mem-314b1252501f75a4-451
ns: default
created: 1789393176713
updated: 1789393176713
---

project/physics-jolt-vehicle-ownership-and-wake: src/physics/VehiclePhysics.js: never J.destroy v.constraint/v.tester after RemoveConstraint (it drops both refs; manual destroy = use-after-free trap); never destroy get_mTracks() copies after set_mTracks (trap on next Step). WheeledVehicleControllerSettings.mDifferentials defaults empty = zero wheel torque. A sleeping chassis silently ignores SetDriverInput, so driver input must ActivateBody first.
