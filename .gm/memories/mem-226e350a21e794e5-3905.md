---
key: mem-226e350a21e794e5-3905
ns: default
created: 1790087222186
updated: 1790087222186
---

## Resolved mutable: webgpu-texture-array-atlas-texel-content-vs-sampler-lodbias-parity

BOTH leads falsified with real live+source evidence, no fix applied (nothing broken found). SW unregistered + caches cleared + confirmed empty (browser dispatch witness_marked:18, swCount:0, cachesLeft:0), live WebGPU device confirmed (window.__app.renderer.backend.constructor.name==='WebGPUBackend'). (a) texel content: model-pool.js:261-266 calls buildTextureArray(entries) ONCE and passes the single returned built.arrayTexture to whichever of buildArrayMaterial(WebGL)/buildArrayMaterialTSL(WebGPU) is selected -- one shared THREE.DataArrayTexture object, so texel content cannot differ by backend by construction. Live CPU-resident readback (texture.image.data, 45 layers x 512x512, bypassing the GPU pipeline entirely) of the actual scene's texture-array-atlas-material-tsl mesh: per-layer vertical/horizontal luminance-gradient-energy ratio is near-isotropic (~0.05x-4.7x) for 44 of 45 layers, but layer 41 measures vhRatio=9.740, dhRatio=9.929 -- matching the prior session's live-rendered artifact magnitude (~9-10x) almost exactly. This is genuine baked directional grain in the source art (a plank/corrugated-texture layer, consistent with the visible wood-plank trench walkway in the live screenshot), confirmed via raw pixel data, not GPU rendering. (b) sampler: node_modules/three/src/renderers/webgpu/utils/WebGPUTextureUtils.js updateSampler() builds magFilter=Linear, minFilter=Linear, mipmapFilter=Linear (from texture.minFilter=LinearMipmapLinearFilter/magFilter=LinearFilter, set once at bake in texture-array-atlas.js:65-66, shared object) and maxAnisotropy=texture.anisotropy (only when all filters linear); live-read the actual material's texture under the running WebGPU device: anisotropy=1, colorSpace='srgb', minFilter=1008, magFilter=1006, generateMipmaps=true -- all defaults, never set to >1 by either texture-array-atlas.js or texture-array-atlasTSL.js. WebGL's node_modules/three/src/renderers/webgl/WebGLTextures.js:699 only calls texParameterf(TEXTURE_MAX_ANISOTROPY_EXT,...) when texture.anisotropy>1 -- at anisotropy=1 (this material's real value) NEITHER backend applies anisotropic filtering; GPUSamplerDescriptor has no LOD-bias field in the WebGPU spec at all, so no bias divergence is structurally possible, and lodMinClamp/lodMaxClamp are left at full-range defaults by three's WebGPU backend. Colorspace sub-check: WebGPUTextureUtils.js getFormat() (generic, not gated on isDataArrayTexture) selects GPUTextureFormat.RGBA8UnormSRGB when ColorManagement.getTransfer(texture.colorSpace)===SRGBTransfer -- confirmed live colorSpace='srgb' -- so WebGPU hardware auto-decodes sRGB->linear on sample, matching WebGL's SRGB8_ALPHA8 auto-decode; TSL's TextureNode.js:633 only inserts an ADDITIONAL manual colorSpaceToWorking() when builder.needsToWorkingColorSpace(texture) is true, and WGSLNodeBuilder (node_modules/three/src/renderers/webgpu/nodes/WGSLNodeBuilder.js) does not override the NodeBuilder base (unconditional return false), so zero double-conversion occurs. Both named leads are conclusively falsified at both the source-code and live-runtime level: no colorspace/tonemap mismatch, no sampler/anisotropy/LOD-bias divergence exists between the two backends for this material. No fix applied since no defect was found in either candidate; root cause is real baked source-art grain in atlas layer 41, orthogonal to the WebGPU-vs-WebGL pipeline code. Follow-on narrower mutable filed (webgpu-texture-array-atlas-runtime-mip-lod-selection-vs-webgl): since texel content and static sampler descriptors are now proven backend-identical, the remaining candidate for the prior sessions' WebGPU-only A/B characterization is runtime automatic mip/LOD SELECTION (derivative-based) differing between WGSL textureSample() and WebGL's classic texture(), not sampler construction.
