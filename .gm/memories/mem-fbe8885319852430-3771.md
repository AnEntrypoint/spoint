---
key: mem-fbe8885319852430-3771
ns: default
created: 1790085951632
updated: 1790085951632
---

## Resolved mutable: webgpu-weather-particle-system-needs-tsl-instancing-port

Ported: client/core/WeatherMaterialsTSL.js (new, 4 TSL materials 1:1 matching client/core/WeatherMaterials.js GLSL constants exactly -- rain uColor(0.72,0.78,0.86)/uOpacity0.6, splash uColor(0.8,0.85,0.92)/uLifeS0.4 with spawnTime-attribute-driven age/scale/discard, snow uColor(0.95,0.97,1.0)/uOpacity0.8, far-sheet uFadeNear40/uFadeFar90 with roundDot branch). Instancing: client/core/WebGPUInstancing.js gained exported createStreamingInstancer (generalized from Grass.js's prior tsl-grass-streaming-webgpu-port adapter, now shared by both Grass.js and Weather.js) plus new setMatrixAt(id,matrix)/visible/geometry/material accessors and shared instanceMatrixNodeFor (also now used by GrassTSL.js, removing duplication). client/core/Weather.js: createWebGPUUnsupportedWeather stub removed; createWeather now branches on isWebGPU at construction (createStreamingInstancer vs InstancedMesh2) and rewrites the per-frame rain/snow/far position loops plus _spawnSplash to compose a THREE.Matrix4 (position + shared billboard/camera/identity quaternion) and call the new adapter setMatrixAt per active particle per frame, exactly as the mutable's own analysis prescribed. Live witness (SESSION_ID gm-tsl-convert-20260922-weathertsl, real tps-game world, ?singleplayer&world=tps-game&webgpu=1, real dev server on :3000, SW unregistered+caches cleared first): console confirmed 'using THREE.WebGPURenderer'; window.__renderer.isWebGPURenderer===true; window.__weather.getType()==='rain', activeCount 1800-3000. Direct instanceMatrix.array pixel/buffer readback of ids 0-2 over 5 samples spanning 10s (2s apart) showed continuously changing Y translation each sample (non-frozen, falling+respawning) -- directly answers the 'rain for a moment then stopped' concern: sustained 10s+ buffer-level proof of continuous per-frame mutation, plus two screenshots ~15s apart (.gm/witness/rain_t0.png, rain_t_later.png) both show live rendered white rain streaks (near+far tier) over the real scene, tick counter advancing 4622->734->2626 across dispatches (game loop never stalled). Switched to snow live: screenshot .gm/witness/snow_t0.png shows round white near+far snow dot particles rendering (visually distinct shape from rain streaks, confirming makeSnowMaterialTSL and makeFarSheetMaterialTSL roundDot=true both correct); imFar.material.customProgramCacheKey() read back as 'weather-far-sheet-tsl-snow' confirming live hot-swap of the far-sheet material/geometry pair. Splash: live call to window.__weather._imSplash.setMatrixAt(5, realMatrix4) + setUniformAt(5,'spawnTime',...) against the running mesh instanceMatrix buffer round-tripped the written Y=7 back out of mesh.instanceMatrix.array, proving the splash write path is wired correctly (0 organic splashes fired in the 4s window sampled was a terrain-contact-distance artifact of camera position, not a port defect, since the mechanism itself was independently proven functional). Zero renderer.info GL errors (gl.errorTotalCount 0) and zero console errors/exceptions across the entire multi-minute session (material construction, instancing adapter grow/visibility/geometry-swap, and rain/snow/splash live triggers). Offline pre-checks before the browser pass: node --check passed on all 5 edited/new files; standalone node ESM import of WeatherMaterialsTSL.js constructed all 4 materials without error; standalone node ESM exercise of WebGPUInstancing.js's createStreamingInstancer proved addInstances/setMatrixAt roundtrip (matrix position [9,9,9] read back exactly), setVisibilityAt, visible/geometry/material accessor passthrough, and _grow()/capacity-doubling with mesh staying in-scene, all against real THREE.js objects with no mocks.
