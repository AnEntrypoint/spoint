---
key: mem-43fe7017ec7eae4f-2753
ns: default
created: 1790068366117
updated: 1790068366117
---

## Resolved mutable: webgpu-planet-depth-sharing-under-framebuffer-target-unverified

CONFIRMED BROKEN, not merely unverified -- live GPUTexture-identity witness plus source mechanism. Source (node_modules/three@0.185.1/src/renderers/webgpu/WebGPUBackend.js): the canvas-direct default pass (_getDefaultRenderPassDescriptor, line 435-488) sources its depth attachment from this.textureUtils.getDepthBuffer(renderer.depth,renderer.stencil) at line 452 -- reached only with no explicit output render target. A RenderTarget-backed pass (_getRenderPassDescriptor, line 582-779) instead sources depth from renderContext.depthTexture (line 719) -- a separate code path. node_modules/three/src/renderers/common/Renderer.js: _getFrameBufferTarget() (line 1425-1454) creates its OWN internal RenderTarget with {depthBuffer:depth,...} (line 1444-1454); when THREE renders the scene into it, renderContext.depthTexture = renderTargetData.depthTexture (Renderer.js:994) -- the framebuffer target owns its own depth texture, separate from the canvas's. LIVE WITNESS (gm browser verb, http://localhost:8099/?singleplayer&world=tps-game&webgpu=1, real tps-game world, real Chromium/Dawn backend): renderer.needsFrameBufferTarget===true confirmed; renderer._textures.get(renderer._getFrameBufferTarget()).depthTexture exists, backing GPUTexture (via renderer.backend.get(...).texture) has label ''; renderer.backend.textureUtils.getDepthBuffer(true,false) (the function mapspinner's depth-writeback pass at packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js:247-248 calls) returns a DIFFERENT GPUTexture, label 'depthBuffer'. sameObject:false -- direct object-identity comparison proves two distinct GPUTexture resources (sameDims:true, both 1295x808 depth24plus, coincidental from shared drawing-buffer size, not evidence of sharing). CONCLUSION: under needsFrameBufferTarget=true (the real live tps-game config), mapspinner's depth-writeback pass writes terrain depth into the canvas-direct depth buffer THREE's scene pass never reads in this configuration; THREE's scene objects instead depth-test against the framebuffer target's own separate depth texture, which mapspinner never writes. This is the exact sibling of the already-fixed color bug (webgpu-planet-canvas-color-not-persisted-across-threejs-render, fixed via resolveThreeOwnedColorTarget) but for depth, and it remains UNFIXED. Fix (not implemented this session, out of scope -- investigate+verify only): an analogous resolveThreeOwnedDepthTarget helper reading renderer._textures.get(renderer._getFrameBufferTarget()).depthTexture -> renderer.backend.get(depthTex).texture, mirroring the color fix's pattern; filed as new mutable webgpu-planet-depth-target-mismatch-under-framebuffer-target.
