---
key: mem-4ae34a65d9b4f38f-365
ns: default
created: 1789392847032
updated: 1789392847032
---

project/apploader-hotreload-watch-debounce: src/apps/AppLoader.js _debounceFileChange keeps a 100ms per-app trailing debounce (mirrors ReloadManager) because node fs/promises recursive watch fires 2+ change events per save; each queued a reload, so an app's migrate() export ran again on already-migrated state (live: stats.hits reset to 0, reloads double-counted).
