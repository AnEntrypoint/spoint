---
key: mem-39c8897a9ac8c512-711
ns: default
created: 1789456801057
updated: 1789456801057
---

## Resolved mutable: res-sweep-inputhistory-unbounded-during-partition

exec_js live run: fed 50,000 consecutive never-acked addInput() calls (simulating a sustained network partition far exceeding any realistic outage). Result: inputHistoryLength=512 (exactly the hard cap, boundedAt512=true), oldestSeqRemaining=49488/newestSeqRemaining=49999 (correctly retained a sliding window of the most recent 512 entries, dropping stale ones), heapGrowthMB=0.79 across the whole run (no unbounded growth). Confirmed no regression to normal operation: the existing soft-cap ack-based eviction is untouched, the hard cap only engages past 512 buffered ticks (~8.5s at 60Hz), unreachable under healthy round-trip latency.
