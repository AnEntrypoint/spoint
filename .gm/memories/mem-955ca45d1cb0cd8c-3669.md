---
key: mem-955ca45d1cb0cd8c-3669
ns: default
created: 1790080127891
updated: 1790080127891
---

## Resolved mutable: webgpu-terrain-hardclip-is-gpu-pipeline-not-quadtree

ROOT CAUSE FOUND AND FIXED (real fix, not a threshold/band-aid): packages/mapspinner/src/webgpu/terrain-composeheight-wgsl.js composeHeight() had 'h = h * 1.0;' where the GLSL sibling (packages/mapspinner/src/shaders/terrain.glsl:211) applies 'h = h * (uReliefScale > 0.0 ? uReliefScale : 1.0)'. reliefScale was ALSO never threaded through the WebGPU JS call chain at all: client/core/TerrainBackdrop.js's initMapspinnerPlanetWebGPU call (line 54) omitted reliefScale entirely (its WebGL sibling call on line 56 passes it), so even the uniform plumbing was missing end-to-end. At tps-game's reliefScale=0.001 on the R=63600 tiny planet, the WGSL height amplitude was ~1000x too large (up to +183750m / clamped -350000m vs the intended ~750m on a 63.6km-radius planet) -- wildly degenerate per-vertex world positions (h can exceed defRadius many times over, flipping vertex direction through the planet center for very negative h). This plausibly explains the reported hard-clip: extreme/degenerate RTE-relative vertex positions feeding the WebGPU zero-to-one perspective divide produce near-zero/negative or wildly varying clip-space w for a subset of patches, which Dawn/D3D12's clipper handled differently (hard-discarding) than ANGLE/WebGL's, independent of camera rotation since the corruption is baked into world-space geometry, not view-dependent -- consistent with the seam surviving a full 90deg camera rotation in the disproven quadtree mutable's own testing. FIX: added reliefScale as an explicit WGSL param to composeHeight() (bitcast<f32>(paramsU.w), the one param slot unused by every call site: RENDER_WGSL vs_main, height-bake-compute.js bakeMain, and terrain-composeheight-wgsl.js's own standalone compute main() used by ground-probe-compute.js), replicating GLSL's exact select(1.0, reliefScale, reliefScale>0.0) semantics; threaded opts.reliefScale (default defRadius/63600000.0, matching gl-render.js's own default formula) through createComposeHeightParams (patch-grid-render.js), bakeHeightTileTexture (height-bake-compute.js), GroundProbe (ground-probe-compute.js, new _writeParams/setReliefScale), initMapspinnerPlanetWebGPU (planet-orchestrator-webgpu.js), and TerrainBackdrop.js's WebGPU call site (now passes reliefScale: cfg.reliefScale like its WebGL sibling). LIVE-VERIFIED via gm cdp verb against http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (SW unregistered + caches cleared this session, confirmed navigator.serviceWorker.controller===false and 0 cached keys before test, real Chromium/D3D12/Dawn WebGPU device, isWebGPURenderer:true): (1) screenshot .gm/witness/clipspace-fix-postedit-1.png shows continuous, correctly-relief-scaled rolling terrain (grass field + rock outcrop + distant hills meeting the horizon) rendering across the FULL screen width from x=0 to x=1295, with zero visible seam anywhere including the previously-reported ~80%-width column; (2) a real copyTextureToBuffer+mapAsync row readback at canvas mid-height (patched GPUCanvasContext.getCurrentTexture + GPUQueue.submit to capture the actual presented swap-chain texture, not a re-render) on the real device found non-uniform terrain-like color content extending to x=1291 of W=1295 (99.7% of screen width, both edges), directly contradicting the prior 'nothing past NDC x~0.5995 (column ~1035)' finding; (3) zero WebGPU validation errors and gl.errorTotalCount:0 across every dispatch this session, including an explicit device.pushErrorScope('validation')/popErrorScope() check after the WGSL edit (bitcast<f32> compiled and ran cleanly on the real Dawn/D3D12 backend).
