---
key: mem-9cd01078c403cb03-890
ns: default
created: 1789456507711
updated: 1789456507711
---

## Resolved mutable: conc-sweep-resimulate-wedge-ordering

exec_js live run: forced a large artificial divergence (localState.position[0]=50 while wedged against server x=10), queued 3 buffered inputs with nonzero velocity, then delivered a server snapshot that both re-confirms the wedge and triggers reconciliation.needsCorrection -> resimulate(). Result: posAfterCorrectiveResim=[10,0.942775,0], xStayedAtServerX=true -- resimulate()'s replay of the buffered inputs via predict() correctly stayed gated by the already-updated horizontallyWedged flag, confirming synchronous single-threaded ordering (no stale-flag read) and that resimulate has no separate/duplicated integration path bypassing the gate (it calls the same predict()). This is disjointness too: predict() is the single writer of horizontal position, reused identically by both the per-tick path and the resim replay path.
