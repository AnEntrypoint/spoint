---
key: mem-6958207c3319af70-1262
ns: default
created: 1789391364354
updated: 1789391364354
---

project/ground-depth-cut-is-underwater-ceiling-waterline-crossing: 'ground depth map height changes with camera, occluding from the ground up' (2026-08-21b) was mapspinner's _uw underwater branch in packages/mapspinner/src/gl-render.js water pass: below the water sphere the two-sided underside writes color+depth and drawSky loses, a 1 m boolean snap the TPS pitch=0 camera crosses routinely. Effective local-Y waterline drops by the sagitta d^2/(2R) (~4 m at map edge), so flat offsetY-anchorHeight sea-level consumers were wrong -- fixed curvature-aware in client/core/UnderwaterTint.js, src/netcode/PhysicsIntegration.js _submersionFrac, apps/_lib/buoyancy.js. Second regime: at grazing altitude (eye within ~2 m of the water sphere) the camera-relative VS projection flips the water surface winding so a hardcoded cullFace(FRONT) culled the whole ocean -> black horizon band; fixed with _waterCullFront = !_uw && !(Math.abs(camAlt)<5.0). Tooling: window.__tpOverride teleport (client/app.js), window.__passProbe frame-stage captures (gl-render.js), uWaterDbg mode 10, __cullMode='none' A/B. Method: out-of-band renderer.render captures wipe the raw-GL backdrop and fake a black-void artifact; init-time-only knobs (geomorphLod) need a pre-boot default flip.
