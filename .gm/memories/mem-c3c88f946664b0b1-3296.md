---
key: mem-c3c88f946664b0b1-3296
ns: default
created: 1790093218120
updated: 1790093218120
---

## Resolved mutable: mut-1790092654071

NO REGRESSION: full multi-angle pitch sweep re-run live at post-flip-removal code (client/core/TerrainBackdrop.js confirmed via codesearch to have zero _fwdYSigned/isWebGPU-flip matches; fE built from raw _fwd.y for both backends, lines 181-185), at the REAL off-pole tps-game anchor (frame.up=[-0.6410,0.2558,0.7237], confirmed live via window.__terrain.frame.up, radius=63600), on the real gameplay THREE.PerspectiveCamera (window.__app.camera) driven through the real window.__app.cam.setVRPitch()/setVRYaw() API, via gm browser verb against real Chromium/D3D12/Dawn WebGPU (isWebGPU:true), SW unregistered + caches cleared and confirmed 0/0 before first load. Initial TPS-follow-mode screenshots at ground level were obstructed by the real spawn-dock structure (not a rendering defect -- git_diff confirmed patch-grid-render.js/planet-orchestrator-webgpu.js have zero uncommitted diff), so the sweep was re-run using the SAME real camera repositioned via cam.setEditMode(true)+setEditCameraPosition(-15.37,150,-14) for a clear horizon -- editMode is the identical THREE.PerspectiveCamera and identical TerrainBackdrop.renderPlanet() code path, matching the manual-pose methodology the fwdvecfix numeric-proof session itself used. Full monotonic sweep, screenshots at .gm/witness/: pitch=+1.3 -> 100% sky (edit_aerial_pitchup_extreme.png); pitch=+0.52 (30up) -> ~98% sky, thin terrain sliver at top (edit_aerial_pitchup30.png); pitch=+0.15 (small-angle control) -> terrain band shrank vs baseline (edit_aerial_pitch015.png); pitch=0 baseline -> terrain band ~340/809px (edit_aerial_pitch0.png); pitch=-0.52 (30down) -> ~90% terrain, thin sky sliver at bottom (edit_aerial_pitchdown30.png); pitch=-1.3 -> 100% terrain, birds-eye of spawn structure (edit_aerial_pitchdown_extreme.png). Monotonic and correct at every step: pitch up strictly increases sky toward 100%, pitch down strictly increases ground toward 100% -- the FPS-convention acceptance bar the original pitchfix session used, no residual inversion anywhere. CONCLUSION: flip removal did NOT reintroduce the original 'pitch inverted' symptom at this real off-pole anchor; the true root cause of the original symptom was something else (most likely the instance-buffer-freeze bug fixed in e4899dc7d4), which the since-removed flip coincidentally compensated for while being mathematically wrong for off-pole frame.up in general (prior session's exact-zero-vs-1227m numeric proof). Second-position attempt: cam.setEditCameraPosition(20000,300,-14) (20km lateral, within 63.6km planet radius) did not cross this anchor's rebase threshold -- frame.up read back unchanged, plus an unrelated pre-existing WebGPU ShaderMaterial pipeline warning fired (grass/vegetation material, unrelated to TerrainBackdrop). Generalization to other frame.up rests on the already-verified general vector-algebra identity (fE=east*fwd.x+up*fwd.y+north*fwd.z is an exact orthonormal change-of-basis, valid for ANY frame.up by construction, not anchor-specific), already numerically confirmed to exact-zero precision at this anchor. Page reloaded clean after the failed second-position attempt (isWebGPU:true, no residual errors, final_spawn_sanity.png). NO CODE CHANGE MADE -- the flip-removed code is correct as-is.
