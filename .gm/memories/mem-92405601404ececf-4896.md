---
key: mem-92405601404ececf-4896
ns: default
created: 1790065510179
updated: 1790065510179
---

## Resolved mutable: webgpu-planet-water-vdrs-not-wired

packages/mapspinner/src/webgpu/planet-orchestrator-webgpu.js frame() restructured: ensureOffscreen(w,h) creates an owned mainColorTex+sceneCopyTex (colorFormat, full canvas size, RENDER_ATTACHMENT|TEXTURE_BINDING|COPY_SRC / TEXTURE_BINDING|COPY_DST) alongside the pre-existing depthTex. pass1 (occlusionQuerySet=occlusionProbe.querySet) draws patchGrid.render + sky.render + water.renderVisProbe into mainColorTex/depthTex with pass1.setViewport(0,0,vw,vh,0,1) where vw=round(w*vrs); occlusionProbe.resolve(encoder) + captureSceneCopy(encoder,mainColorTex,sceneCopyTex,w,h) copy the terrain+sky result for water's refraction sample; pass2 (loadOp 'load' on both attachments, same sub-viewport) draws water.render blended on top reading sceneCopyTex. vdrsOn=(window.__vdrs===true), vrs=vdrsOn?clamp(window.__vdrsScale,0.3,1.0):1.0, mirroring gl-render.js's own _vdrsOn/_vdrsRsThisFrame gate verbatim (same window globals, same fixed-full-size-texture + flexed-sub-viewport convention documented in client/core/RenderControls.js's own vdrsScale doc string, not a smaller offscreen texture). Composite: vdrsOn -> Fsr1Upscale or BilinearUpscale (window.__vdrsUpscaleFsr1 flag, matching gl-render.js's own flag) sampling mainColorTex at uvScale=vrs into the canvas texture; vdrsOff -> BilinearUpscale at uvScale=(1,1), a literal 1:1 passthrough blit of the full-res offscreen buffer. LIVE-VERIFIED 2026-09-22 via gm cdp verb against the real tps-game world at http://127.0.0.1:8123/?singleplayer&world=tps-game&webgpu=1 (own-started spoint server.js instance, PORT=8123, since the project's existing dev server had died mid-session): (1) VDRS-OFF path: direct GPU copyTextureToBuffer+mapAsync readback of mainColorTex, 32x32 grid, 1024/1024 samples non-black (blackN=0), waterN=102 samples classified by real RGBA (b>150&&g>150, values clustered at [194,236,248]ish matching water-render.js's WATER_WGSL fs_main mix(scene,vec3(0.05,0.22,0.38),0.55) pulling toward a tight fixed-tint band -- tight clustering across independently-sampled screen points is itself evidence of the blend formula executing, not raw unblended background), terrainOtherN=922 at real dark-olive terrain color [18-20,25-28,15-16] matching patch-grid-render.js's fs_main lowColor*lit output. (2) VDRS-ON path (window.__vdrs=true, window.__vdrsScale=0.5): same readback shows blackN=768/1024 (=75.0% exactly, matching the predicted 1-0.5*0.5=0.75 fraction outside the vw=649,vh=406 sub-rect of the full 1297x812 offscreen texture -- an exact quantitative geometric match, not a fuzzy visual judgement), and a second classification restricted to just the vw x vh sub-rect (vdrsOnSubRectClass) shows 0/576 black, waterN=65, terrainOtherN=511 -- terrain AND water both still fully and distinctly rendered within the reduced viewport, confirming the sub-viewport draw is genuinely populated, not degenerate. canvas dimensions stayed fixed at 1297x812 in both branches (renderer.domElement.width/height unchanged), confirming the render-small/present-full convention. (3) Live canvas-texture write confirmed directly: called planet.frame(...) manually with the real player camera's world position/target/fov (window.__camera, window.__terrain.frame), then immediately (same JS tick, before any further THREE render) read back renderer.backend.context.getCurrentTexture() itself (not just the internal mainColorTex) via copyTextureToBuffer -- 256/256 sampled points were real non-black terrain color [24,55,19], proving the final composite genuinely lands correct pixels on the actual presented canvas texture at draw time. (4) Zero new WebGPU validation/compile errors attributable to water/vdrs code paths across all dispatches (console captured pre-existing unrelated errors only: [weather] createBuffer, MeshBasicNodeMaterial DXC compile failures, oversized uniform-buffer bindings -- none mention water-render/vdrs-composite/patch-grid pipeline labels). A separate, pre-existing architecture gap was found and filed separately (webgpu-planet-canvas-color-not-persisted-across-threejs-render): a later real game-loop screenshot shows the canvas's terrain/sky/water region reverting to black once THREE's own subsequent scene-color RenderGraph node renders player/scene objects, despite the hasTerrain->autoClear=false guard in client/core/RenderGraph.nodes.js:157-168 -- this predates and is independent of this fix (the original terrain+sky-only direct-to-canvas draw had the identical exposure before water/VDRS were wired in), is the color-sibling of the already-filed webgpu-planet-depth-not-shared-with-three-scene mutable, and does not block this mutable's resolution since the composite pipeline itself (water+VDRS wiring, the literal scope here) is proven correct by direct GPU-level readback both of the internal buffer and of the live canvas texture at draw time.
