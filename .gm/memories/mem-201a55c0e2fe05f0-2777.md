---
key: mem-201a55c0e2fe05f0-2777
ns: default
created: 1790081011424
updated: 1790081011424
---

## Resolved mutable: mut-1790073755079

RE-VERIFIED 2026-09-22 in a fresh session (gm-tsl-convert-20260922-dithercheck) after both cited fixes landed (a7068d9f SW-prefers-network-on-localhost, dacd45ca reliefScale-threading). Unregistered SW + cleared caches at session start (confirmed 0 regs/0 caches before load), fresh navigation, gm browser verb against real Chromium D3D12/Dawn WebGPU backend at http://localhost:8099/?singleplayer&world=tps-game&webgpu=1 (renderer.backend.isWebGPUBackend===true). This mutable's own 'persistent diagonal hatch/streak pattern reappeared' component is CONFIRMED STILL PRESENT: .gm/witness/dithercheck-cam2.png (tick 2104, FPS 61) and dithercheck-combined.png (tick 686, FPS 53) show the dense crosshatch immediately on load, not slowly accumulating over a long session -- this rules out the 'same leak recurring more slowly' framing this mutable proposed. Decisive NEW evidence: direct A/B at the IDENTICAL camera pose (app.cam.setPosition(-10.9,8,3)/setTarget(-40,4,-25)) -- WebGPU (dithercheck-cam2.png) shows heavy crosshatch on the rock wall; the SAME URL minus webgpu=1, same pose (dithercheck-webgl-final.png) renders the identical wall completely clean -- CONFIRMED WebGPU-renderer-specific, not a texture-asset or terrain-height defect. reliefScale is correctly ruled out as the cause: the pattern also covers non-terrain GLB model geometry (mesh parented under 'entity_4_model.progressive.glb', material MeshBasicMaterial, confirmed via scene.traverse+getWorldPosition), not just terrain. Console stayed clean across every WebGPU dispatch this session (console_dropped:0, zero 'DXC compile failed'/'Uniform buffer...larger than the maximum' entries) which rules out this project's own already-tracked webgpu-e2e-meshbasicnodematerial-433-dxc-permanent-fail (that bug's signature is flat unlit magenta/pink, not a crosshatch) and webgpu-e2e-uniform-buffer-exceeds-65536-limit (would throw a captured GPUValidationError) as the direct visible cause. The 'floating disconnected rectangular panel/ghost-thumbnail' sub-symptom this mutable also names was NOT re-encountered this session: free-cam exploration stayed confined to the immediate spawn structure because requestPointerLock fails under the gm browser/CDP harness (pre-existing limitation, matches prior session's mut-1790073656058 finding), so the exact world location of the original panel screenshots was not reached this pass. RE-SCOPED rather than closed-as-fixed: real, reproducible, WebGPU-isolated hatch defect confirmed distinct from the two already-tracked pipeline bugs; root-cause-and-fix work plus a renewed panel hunt (via mapspinner's own WebGPU passes, not THREE scene materials) filed as new mutable webgpu-dither-hatch-distinct-from-tracked-pipeline-bugs.
