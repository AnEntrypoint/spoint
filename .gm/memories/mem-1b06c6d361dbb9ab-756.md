---
key: mem-1b06c6d361dbb9ab-756
ns: default
created: 1789396900717
updated: 1789396900717
---

project/world-persistence-worker-identity: singleplayer Worker worldDef has no name and no process.env, so WorldPersistence.resolveWorldName = worldDef.name || ctx.worldName || guarded env WORLD; WorkerEntry sets ctx.worldName = worldDefFingerprint(worldDef) (canonicalJSON + LockstepChecksum fold, computed before init mutates timeOfDay.seed/_minimap). A null==null match would restore one world's snapshot into another: IDB 'world-snapshot' key is shared per origin. ctx.seaLevel is the curvature-aware waterline at the entity x/z (waterlineLocalY in PlanetFrame.js is the single implementation; ctx.seaLevelAt for other points). UGC/any content hash must use src/shared/canonicalJSON.js -- a JSON.stringify replacer ARRAY is also a nested-key allowlist.
