---
key: mem-bfa373bd6a6445e3-3437
ns: default
created: 1790067760744
updated: 1790067760744
---

## Resolved mutable: tsl-extend-webgpuinstancing-to-grass-playervat-vegetation

client/core/Vegetation.js: removed the isWebGPURenderer fail-open no-op; added createVegWebGPUInstancer (id-stable addInstances/removeInstances/setUniformAt/setVisibilityAt/resizeBuffers adapter over WebGPUInstancing.js's createWebGPUInstancedMesh, same shape as Grass.js's createStreamingGrassInstancer but with per-id .visible get/set on the addInstances entity proxy, since Vegetation's applyOcclusion mutates en.branchEntity.visible/en.leafEntity.visible directly rather than calling setVisibilityAt(id,...) like Grass does). Species-build loop branches isWebGPU: WebGPU path builds a SINGLE full-detail LOD tier per species (no addLOD/addShadowLOD/BVH -- confirmed via codesearch that InstancedMesh2's addLOD/addShadowLOD/computeBVH are the only WebGL-only mechanisms Vegetation.js depended on for its D1/D2/D3 mesh-LOD-swap and impostor-swap tiers; VegImpostorTier.js ALSO constructs its own InstancedMesh2 with addLOD for the near-cutoff dissolve, so the shared-impostor billboard tier is left WebGL-only too -- filed as new mutable tsl-vegetation-multi-lod-shadowlod-impostor-parity, not forced). VegetationTSL.js's applyWindTSL/applyTintTSL wired for branch+leaf materials. Found and fixed a real bug during live witnessing, not just this session's own code: WebGPUInstancing.js's createWebGPUInstancedMesh.acquireId() is the only place mesh.count is ever set, but BOTH Grass.js's createStreamingGrassInstancer AND my new Vegetation adapter reimplement id/highWatermark bookkeeping independently and never call rec.acquireId(), so mesh.count stayed permanently 0 (verified live: window.__grass._im.mesh.count===0 and window.__veg._meshes[*].branch.mesh.count===0 despite totalInstances>0 after a fresh page load) -- meaning NOTHING was actually drawn (THREE.InstancedMesh draws only instances [0,count)) despite Grass's prior mutable claiming a live-witnessed render (that witness used a hand-built 2-instance test mesh, not the real streaming adapter path). Fixed in both Grass.js and Vegetation.js: addInstances now does `if (id+1>rec.mesh.count) rec.mesh.count=id+1`, and _grow() now sets `rec.mesh.count=highWatermark` after replaying matrices into the new rec. Live-verified post-fix via gm browser against real tps-game ?webgpu=1: window.__veg._meshes filtered to count>0 all show branch.mesh.count/leaf.mesh.count exactly equal to the tracked count (e.g. Aspen Medium c:331,mc:331,lmc:331; Oak Large c:8,mc:8,lmc:8), window.__grass._im.mesh.count matches similarly (175), buildErrors:0, species:15, and a screenshot plus a live pixel-delta capture (see sibling mutable resolution) both show real geometry rendering, not the flat fail-open black/empty scene from before this session. Also fixed a real bug in VegetationTSL.js's applyTintTSL: it read `material.colorNode` (undefined pre-assignment, since ez-tree returns plain MeshStandardMaterial) instead of the `materialColor` TSL accessor node (which reads the material's own map+color, the pattern already proven in this codebase by client/core/RocksTSL.js line 31 `vec4(materialColor.rgb.mul(tint), materialColor.a)`) -- the original code would have replaced all bark/leaf texture color with flat white*tint. Fixed to use materialColor; live-verified branch/leaf materials now have hasColorNode:true with polygonOffset:true/factor:-4/units:-32 preserved (see sibling mutable's witness).
