---
key: mem-12975b6f8664e430-5007
ns: default
created: 1790068702367
updated: 1790068702367
---

## Resolved mutable: webgpu-thc-sample-consumption-path-untested

Built the missing WebGPU THC-sampling consumption path in packages/mapspinner/src/webgpu/patch-grid-render.js: buildSampleWgsl(gridSize) (lines 91-182) is a second WGSL module mirroring gl-render.js/shaders/terrain.glsl's REAL uThc>0.5 branch (terrain.glsl:319-332) exactly -- thcSample() (line 119) is a 1:1 port of GLSL thcSample()'s hardware-filtered branch (terrain.glsl:261, the uPoolLinear>0.5 case, applicable here since this adapter has float32-filterable) via textureSampleLevel against a texture_2d_array<f32> heightPool + filtering sampler; vs_main takes 5 taps (center + 4 neighbors at 1/gridSize offset, matching terrain.glsl:320-325's duP pattern) and reconstructs the vertex normal via the same cross-product FD formula (terrain.glsl:330-332), replacing per-vertex composeHeight entirely (no composeHeight call in this path). PatchGridRenderer gained mode:'compute'|'sample' (constructor branches at patch-grid-render.js:334-382), bakeLayer() (lines 397-410, GPU-resident copyTextureToTexture from height-bake-compute.js's real bakeHeightTileTexture kernel into an array layer, zero CPU readback -- the exact pattern webgpu-thc-bake-perf-vs-webgl2-thc's own resolution recommended), and buildInstanceDataThc() (lines 224-238, assigns each quad instance its pool layer index) -- this is the runtime A/B toggle. LIVE-VERIFIED via gm browser verb against a real WebGPU device (Chrome, nvidia/ampere, float32-filterable=true, timestamp-query=true, zero WebGPU validation errors via explicit pushErrorScope/popErrorScope around setup+bake and around render+readback): built rendererCompute (mode:'compute') and rendererSample (mode:'sample', 211 real layers) against the SAME real per-frame quad set (211 leaves) produced by literally replaying planet-orchestrator-webgpu.js's own collectQuads code path (same Quadtree/planet-orchestrator-cull.js imports, same call sequence, real camera pose alt=5000m) -- not synthetic patches. Baked all 211 real layers via bakeLayer in 85-155ms (two runs). Confirmed both pipelines render real, distinct, non-degenerate output via colorTex readback (1024x1024 rgba8unorm): compute path 63129/1,048,576 non-black pixels sum=5,478,395; sample path 62701/1,048,576 non-black pixels sum=3,588,668 (same silhouette, different pixel sums from the different normal-reconstruction source, proving neither path is a no-op). Measured real per-draw GPU time isolated to JUST the terrain draw call (own render pass, GPURenderPassDescriptor.timestampWrites bracketing pass begin/end, resolveQuerySet -> mapAsync, ns = end-begin, median-of-N reproduced across 2 independent runs): COMPUTE (per-vertex composeHeight, current shipped default) median=327680ns (0.328ms), mean=348160-354304ns, n=32/32 valid samples both runs. SAMPLE (THC pool textureSampleLevel, this session's new path) median=65536ns (0.0655ms), n=11-12/32 valid (the other ~20/32 draws were faster than the platform's GPU-timestamp quantization floor -- Chrome coarsens WebGPU timestamp-query results to exact multiples of 65536ns/65.5us for fingerprinting resistance, confirmed by every non-zero sample in both runs landing on an exact 65536 multiple: compute values 327680=5x65536 and 393216=6x65536, sample values exactly 1x65536) and were discarded as ns<=0 rather than mis-recorded as zero-cost. VERDICT: pool-sampling WINS the isolated draw-call GPU-time comparison by ~5x (medianSpeedup=5.0, reproduced both runs) on this WebGPU/nvidia-ampere backend -- the OPPOSITE sign from the original 2026-06-15 WebGL2 finding (net-negative, FS-cost-shift). This does not contradict the WebGL2 result: the original regression was specifically about a FS-cost SHIFT (VS cheaper, FS worse, aggregate flat-to-worse) that this isolated per-draw GPU-time metric cannot itself decompose into VS-only vs FS-only cost (WebGPU's timestampWrites brackets the whole render pass, not per-stage; there is no WebGPU equivalent of WebGL2's EXT_disjoint_timer_query_webgl2 uFsCheap short-circuit trick used for the original VS/FS split). RECOMMENDATION: adopt WebGPU THC pool-sampling (mode:'sample') as the default live path once wired into planet-orchestrator-webgpu.js's real per-frame loop (baking layers once per quad-set change, not per frame, mirroring bakeLayer's GPU-resident no-readback pattern) -- the measured ~5x per-draw GPU-time reduction is large enough to survive a plausible FS-side regression of the same class as WebGL2's, but this should be re-confirmed with a full-frame aggregate measurement (not just the isolated draw) once wired end-to-end, since the original regression was an aggregate-frame finding this session did not reproduce the full frame context for (only the isolated terrain draw, per the mutable's own explicit scope: 'bracketing just the vertex/fragment shader execution, not the whole frame'). Not a quality-tier-conditional recommendation -- adopt outright, re-verify at full-frame scope as a follow-up, not a blocker.
